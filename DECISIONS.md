# Decisions log

Append-only. Newest at the bottom. Reversals are new entries that reference the original.
Format: date, id, decision, alternatives considered, reason.
From D-100 on, every entry carries its date after its number; the section headers date the earlier ones.
A claim not verified in the session carries [untested] or [inferred] inline and a line under "Open questions"
in the handoff file (docs/handoff/).

## 2026-09-20

**D-001 — Alpha-5 element sets come from derived re-encodings of CelesTrak OMM records, never from Space-Track bytes.**
Alternatives: (a) commit Space-Track TLEs; (b) encoding vectors only.
Reason: the Space-Track user agreement prohibits transferring its data to other entities without approval and a repository is a transfer; CelesTrak records re-encoded by a committed script are real data with a reproducible transformation. Derived files carry `provenance: derived` with source record and script recorded. The project owner may verify the encoder privately against their own Space-Track account; nothing from that check is committed. (Owner decision.)

**D-002 — Fetch the Starlink G15-27 post-deployment SupGP file first, store under `raw/`, gitignore it for now.**
Alternatives: skip 9-digit coverage; wait for the next launch.
Reason: it is the only current source of provider-issued 9-digit ids and the ids disappear at cataloguing. If nothing usable is in it, the 9-digit element-set case is omitted; no synthetic records. (Owner decision.)

**D-003 — Derived KVN syntax variants re-serialised from real records are allowed.**
Alternatives: omit; hand-write examples.
Reason: CCSDS-legal forms that CelesTrak happens not to emit (day-of-year epochs, no fractional seconds, `Z`, bracketed units) are exactly what breaks parsers; each derived file records the source record hash and the transformation. (Owner decision.)

**D-004 — Phase 2 fetch budget approved as proposed in docs/PLAN.md §3 (about 40 requests, each URL once, under ~20 MB).**
Reason: well inside CelesTrak's published limits (100 MB/day, 50 errors per 2 h); the two one-download-per-update groups (`active`, `starlink`) are not requested. (Owner decision.)

**D-005 — Repository initialised at `Code Files/gp-omm-conformance/`; private until after the independent audit.**
Reason: redistribution terms for CelesTrak data are not yet settled (see D-007 and RESEARCH.md §11). (Owner decision.)

**D-006 — CLAUDE.md written without the `---` delimiter lines that surrounded the supplied text.**
Alternatives: include them.
Reason: a leading `---` block is parsed as YAML front matter by many Markdown tools and would hide the policy; the delimiters read as quoting marks in the instruction, not as content. Reversible in one edit if wrong.

**D-007 — No personal contact details in the fetch User-Agent.**
Alternatives: include the owner's e-mail (as some earlier one-off documentation fetches did).
Reason: CelesTrak does not ask for identification, and sending an address in request headers to a third party is not something the owner asked for. The UA names the project and points at the repository README. `GPCONF_CONTACT` env var lets the owner opt in.

**D-008 — The fetch tool does not follow redirects and never retries.**
Alternatives: default urllib behaviour (follow 301/302).
Reason: CelesTrak counts 301s toward its error limit and says responses to 403/404 "will not change by repeating the request"; a redirect means the fetch list is wrong and should be fixed by a human.

**D-009 — One fetch-list entry may record a non-200 response as data: `gp.php?CATNR=100000&FORMAT=TLE`.**
Alternatives: stop the run on any non-200 (the rule for every other entry); skip the request.
Reason: the response to a TLE request for a 6-digit object is itself a migration fact worth recording, and CelesTrak documents that TLE output cannot carry such objects. One request, never repeated, whatever the status.

**D-010 — `gp-first.php` is queried for ISS (25544) only, not for Vanguard 1 (5), in the first pass.**
Alternatives: query both.
Reason: if the GP archive does not reach 1998 for the ISS it will not reach 1958 either; avoids a probable 404. A second, single request may follow once the ISS result is seen.

**D-011 — SATCAT "both sides of 70000" uses the largest id below 70000 actually present in `satcat.txt`, plus 25544 and 100000, via `records.php`; no 8xxxx SATCAT query.**
Alternatives: guess 69999; download `satcat.csv` as well.
Reason: analyst objects are not in the public SATCAT, 69999 may not exist as an object, and the legacy file already tells us the largest 5-digit id; avoids a 404 and a second multi-megabyte download.

## 2026-09-21

**D-012 — Nine-digit ids taken from the full Starlink SupGP file and one per-object query, after the G15-27 file proved to use 72000-series ids.**
Alternatives: give up on 9-digit coverage; download the whole Starlink SupGP set in all five formats (~10 MB).
Reason: the G15-27 post-deployment file (fetched first, per D-002) carries ids 72000/72001. One CSV download of `FILE=starlink` (1.8 MB, subject to the one-per-update rule, requested once) showed 27 ids in 799501621–799501647; a single object (799501621) was then fetched by `CATNR` in CSV/JSON/XML/KVN/TLE. Real provider data, seven requests in total.

**D-013 — The `last-30-days` TLE 404 was recorded by hand from the tool's log and the tool now saves unexpected non-200 bodies before stopping; TLE-format entries whose 404 is informative carry `allow_error`.**
Alternatives: re-request the URL to capture headers; drop the fixture.
Reason: CelesTrak says a 403/404 "is not going to change by repeating the request"; the body was printed verbatim by the tool, so nothing is lost except response headers, which the metadata file says are missing.

**D-014 — Legacy `satcat.txt` (9.4 MB) fetched once and kept out of git; Phase 3 commits an excerpt plus the full-file SHA-256 and derived facts.**
Alternatives: commit the whole file; also fetch `satcat.csv`.
Reason: its only role in the corpus is to show the <70000 cutoff, which the excerpt, hash and counts prove; `records.php` supplies the per-object CSV/JSON records.

**D-015 — Catalog number 69999 is present in the legacy file and in `records.php`, so it is the "largest 5-digit id" for the cutoff case (as D-011 anticipated).**

**D-016 — Derived Alpha-5 TLE lines will cover letters A and T only.**
Alternatives: invent ids for the other 22 letters.
Reason: the only real catalog numbers inside 100000–339999 are 100000–100789 (letter A) and the 270000–270449 Space Fence analyst objects (letter T). The remaining letters and the I/O skips are covered by the specification vectors (D-001). No fabricated ids.

**D-017 — `expected.json` records per-format expected values where the provider's renderings differ, instead of one canonical record per object (amends PLAN.md §2.2).**
Alternatives: one canonical record with tolerances.
Reason: the inventory shows the TLE rendering truncates eccentricity to 7 digits and BSTAR to 5 mantissa digits, and SupGP TLE epochs are rounded to the TLE's 864 µs resolution. A single expected record would assert a lossless round trip that the data contradicts; per-format values let a parser be judged against exactly what it read, and the case notes state which digits are lost.

**D-018 — New case `supgp-celestrak-classification-c`; all SupGP raw files remain gitignored until redistribution is settled.**
Alternatives: fold SupGP quirks into other cases.
Reason: SupGP records differ from GP in ways that break GP-only parsers (`CLASSIFICATION_TYPE` `C`, `ELEMENT_SET_NO` 0, extra `RMS`/`DATA_SOURCE` columns, 72000-series ids, reused `OBJECT_ID`), and their redistribution status is the weakest (RESEARCH §11.3).

**D-019 — CelesTrak's silence on redistribution is recorded as silence, not permission; publishing raw fixtures is deferred to the owner; building continues.**
Alternatives: treat "freely available" as a licence; stop work.
Reason: CLAUDE.md ("interpret licences and terms strictly") and the owner's instruction that this decides publication, not construction. Options are laid out in PLAN.md "Phase 2 amendments".

**D-020 — Phase 3 will validate CelesTrak XML against the OMM 2.0 schema set fetched from SANA by direct URL, and separately report validity against the current 3.0/4.0.0 set.**
Alternatives: validate only against 3.0 with the version attribute masked.
Reason: a HEAD request shows the 2.0.0 master schema still resolves at the URL CelesTrak declares (RESEARCH §12), so the document can be validated against what it claims to be, and forward-compatibility with the current standard can be reported as a separate finding.

**D-021 — Error budget transparency.** Five non-200 responses were received from celestrak.org across both phases: two from guessed documentation URLs during research (a lesson: find links on pages, do not guess), three deliberate one-time TLE-format probes kept as fixtures. All well under CelesTrak's 50-per-2-hours threshold. Some one-off documentation fetches in Phase 1 carried the owner's e-mail in the User-Agent; the fetch tool does not (D-007).

**D-022 — Clean re-capture of the three window-dependent TLE endpoints (owner instruction), one request each, at 2026-09-21T00:41Z.**
Result: `last-30-days` TLE again `HTTP/1.1 404 Not Found`, body `No GP data found`, now with the full header set; `analyst` and `DECAYING` TLE responses are byte-identical to the 00:11Z captures (same GP update window), so the 565→219 and 82→79 omission counts now rest on captures with full provenance. The hand-transcribed entry is kept, marked "original observation" and cross-linked as superseded for citation. The fetch tool now records the status line, every response header in order, the request headers and start/finish timestamps for every response. Re-captures are separate fetch-list entries with a `recapture_of` field; the duplicate-URL guard stays.

**D-023 — Corpus design (owner decision): no raw CelesTrak bytes are shipped. The public corpus is specification vectors, clearly labelled derived re-encodings, `expected.json`, SHA-256 checksums of the files we tested, and the fetch script that rebuilds `raw/` on the user's machine under CelesTrak's own terms.**
Consequence recorded here, not silently: checksums prove parity with what we tested **only for endpoints whose bytes are stable**. Live group queries (`GROUP=`, `SPECIAL=`, current `CATNR=`) change every GP update, so a user's re-fetch will not match our checksums and our exact expected values will not apply to their bytes. The design therefore has two tiers per case: **stable-source fixtures** (CelesTrak's `gp-first.php` first-ever records, SATCAT records, derived files, vectors) with frozen expected values verified by checksum, and **live-window checks** (structural and cross-format invariants that hold for any snapshot, plus our snapshot's values published as a dated reference). Stage F fetches the stable sources.

**D-024 — Stable sources chosen: `gp-first.php` for 25544 (1998 record, already partly fetched), 100000 (Saramago's first record), 270449 (a Space Fence analyst id, letter T), 81011 (an 8xxxx analyst id), and 69999 (largest 5-digit id).**
Alternatives: only live queries; more objects.
Reason: "first GP data available" for a given catalog number should not change over time, giving reproducible bytes; the set covers 19xx epoch year, 6-digit, both analyst blocks and the 70000 boundary in about 20 small requests. Stability is an assumption stated in the manifest; the checksum comparison in the fetch script will reveal any drift.

**D-025 — Raw provider files stay tracked in this private repository; the public release is a curated export into a fresh repository.**
Alternatives: remove raw files from git now; rewrite history before publishing.
Reason: the auditor needs the exact bytes we tested in one place; the Phase 2 commit already contains them, so a public push of this history is ruled out regardless. An allowlist-driven export (Phase 4) is simpler and verifiable. SupGP raw files remain gitignored per D-002.

**D-026 — `expected.json` carries per-format values only where the renderings differ, hoists constant per-set metadata out of the records, and omits whole TLE lines.**
Alternatives: one canonical record with tolerances (PLAN §2.2); full per-record metadata (1.5 MB for the analyst case).
Reason: D-017 as implemented; field substrings and parsed values are analysis, whole lines are the provider's bytes. Total expected.json size 2.9 MB for 16 cases.

**D-027 — A name longer than 24 characters is cut to 24 in derived TLE line 0 and reported as `object_name_truncated_to_24`, not as a value difference.**
Reason: the TLE format's line 0 is documented as a 24-character name; the only such name in the data (100465, 27 characters) belongs to a six-digit object, so the rule could not be checked against CelesTrak output; recorded in the provenance sidecar and the precision-loss case.

**D-028 — The TLE renderer used for derived lines rounds the BSTAR / second-derivative mantissa half up to five digits and truncates eccentricity to seven digits.**
Alternatives: truncate both (251/304 match); round both (257/304 on line 2).
Reason: only this combination reproduces all 304 CelesTrak TLE lines exactly; no document states either rule, so it is recorded as observed provider behaviour (ambiguity `ecc-truncation-vs-mantissa-rounding`) and as the named case `tle-vs-omm-precision-loss` (owner instruction).

**D-029 — Library failures found by the cross-check are recorded as library behaviour, not as corpus defects, and not worked around in the reference readers.**
Findings (python-sgp4 2.27, Skyfield 1.55): nine-digit ids cannot be initialised from OMM data; XML records with an empty OBJECT_ID raise TypeError; export_tle writes a zero second derivative as ' 00000-0' where CelesTrak writes ' 00000+0'. All three are exactly the kind of defect the corpus exists to expose; they go into the Phase 4 breakage catalogue and the per-case `library_findings`.

**D-030 — Both complete SANA schema sets (2.0.0 and 4.0.0, 14 files each) are vendored under `schemas/` with a SOURCE.md of URLs and hashes.**
Alternatives: only the OMM modules (fails: the master schemas include every message type).
Reason: validation must run offline and against exactly the files CelesTrak declares; the CCSDS documents are published for free use and are redistributed unmodified.

**D-031 — Derived KVN variants may fill header keywords with values describing the derived message itself (ORIGINATOR GP-OMM-CONFORMANCE-CORPUS, generation time, a MESSAGE_ID, a CLASSIFICATION text).**
Alternatives: keep all headers blank like CelesTrak.
Reason: CCSDS makes CREATION_DATE and ORIGINATOR mandatory and non-empty; a variant that exercises a compliant header needs real values, and the only truthful ones are ours. The provenance sidecar says so.

**D-032 — Specification vectors that go beyond the six official Space-Track examples (letter boundaries, skip boundaries) are derived arithmetically from the published table and labelled as such; invalid-input vectors state the reason.**
Reason: the table is normative; the boundaries follow from it; a reader can recompute them.

**D-033 — The SupGP snapshot values (one nine-digit record, the G15-27 stack and single nominals, the 27 nine-digit ids) are published in expected.json as a dated reference, flagged with ambiguity `supgp-redistribution` so the owner can strip them before release.**
Alternatives: omit all SupGP-derived values.
Reason: they are the only real nine-digit evidence and are analysis values, not raw bytes; the flag keeps the removal a one-line decision.

**D-034 — Two guessed documentation URLs earlier produced 404s; from Phase 2 on, every URL is taken from a page link or the provider's documentation, never guessed.** (Lesson recorded; see D-021.)

## 2026-09-21 (Phase 4)

**D-035 — `.gitignore` guard against Space-Track data, placed before any Space-Track account exists (owner instruction).**
Patterns match any path component containing space-track / space_track / space track / space.track / spacetrack in any letter case, plus directories of those names. Verified with `git check-ignore` on eleven sample paths (eight blocked, three unrelated names such as `spacecraft-tracking.md` not blocked). One speculative pattern for export-file names was written wrongly (`?` is "any one character" in gitignore, not "optional") and removed rather than kept broken. The export tool applies the same guard by regex.

**D-036 — The zero second-derivative exponent sign fully explains every `export_tle` byte mismatch: verified, not inferred.**
Against the 306 CelesTrak-served TLE/2LE records (recaptures excluded): 225 have a zero second derivative and 0 export byte-exactly, and in all 225 the only differing columns are 51 (`+` vs `-`) and 69 (checksum); flipping the sign and recomputing the checksum reproduces every line. The 81 records with a non-zero second derivative all export byte-exactly. Line 2 is byte-exact in all 306. The same holds for the 604 derived lines (591 mismatches, all explained). The upstream draft states these numbers.

**D-037 — Evidence-basis statements added to the precision-loss and derived-Alpha-5 cases (owner instruction): the rendering rule was derived empirically from 304 CelesTrak record pairs and is not documented in any specification we located; the renderer was validated only against sub-100000 output.**
The owner verifies the Alpha-5 encoder separately against Space-Track; no Space-Track data enters the repository and the public claim describes the verification, not the data.

**D-038 — Runner design: data-driven from `manifest.json` and `expected.json`, standard library only, Python 3.9+, duck-typed parser protocol (`parse(raw, fmt)` plus four optional vector hooks), external-command mode for non-Python parsers, and two oracles: frozen expected values when the file's SHA-256 matches the tested snapshot, otherwise the corpus's own reference reader applied to the user's bytes, labelled as such in every report line.**
Alternatives: require the user's parser to emit our exact JSON schema; skip live files entirely.
Reason: any parser in any language can be tested; the report never presents a reference-derived comparison as a human-verified one.

**D-039 — Tolerances for the parser under test: floats compared at 1e-12 relative, epochs within 2 microseconds; everything else exact. The oracle values themselves stay exact decimal strings.**
Alternatives: exact comparison of everything (python-sgp4 then failed on 1-microsecond Julian-date float noise and on 1e-16 unit-conversion noise, which is not a conformance defect); looser tolerances (would hide the 86-microsecond SupGP epoch quantisation, so the TLE/OMM epoch check keeps its own 432-microsecond rule).

**D-040 — Library findings are reported as failures of that adapter, never worked around: nine-digit ValueError, XML empty-OBJECT_ID TypeError, classification reset to 'U' by `sgp4init` after `omm.initialize`, lenient Alpha-5 decoding (I, O, lowercase, four characters accepted; negative encodes to '-0001'). The first three have upstream drafts in `docs/upstream/` (the fourth, minor, is noted in the third's sibling draft); the owner files them.**

**D-041 — LICENSE copyright line reads "Honorius Neogy", the name used as git author, which was inferred from the owner's e-mail address rather than stated. Flagged for the owner to confirm or change before publication.**

**D-042 — Ordinary users' `tools/fetch.py` runs skip the three maintainer-only re-capture entries (`--include-recaptures` restores them).**
Reason: they exist to document provenance of a window-dependent observation; a user re-requesting the same endpoint twice would waste CelesTrak's bandwidth for nothing.

**D-043 — Public release = `tools/export_public.py` over `PUBLIC_ALLOWLIST.txt` into a fresh directory that becomes a new repository; the tool refuses `fixtures/*/raw/` and any Space-Track-named path and writes an export manifest with hashes. Dry run: 131 files, 0 refused. This repository's history is never pushed (D-025).**

**D-044 — CI runs offline only (unit tests plus the three shipped cases with the reference and naive adapters, Python 3.9 and 3.12). Cases needing raw files report `skip` until `tools/fetch.py` has run locally; CI never fetches from CelesTrak.**
Reason: a fetch per push would multiply load on a shared, non-profit service for no evidentiary gain.

**D-045 — A fourth, minor upstream draft (classification dropped by `omm.initialize`) was added beyond the three requested, because it was found during Phase 4 and silently alters a field; the owner decides whether to file it.**

**D-046 — Correction to D-040's wording:** the fourth finding (classification reset by `sgp4init`) has its own draft, `docs/upstream/python-sgp4-omm-initialize-drops-classification.md`, not a note inside another draft. D-045 is accurate; D-040 is left as written per the append-only rule.

## 2026-09-21 (owner decisions after the Phase 4 report)

**D-047 — Copyright holder is NEOGY LLC (owner decision; the owner's instruction called this D-046, a number already used by the correction entry appended after the Phase 4 report, so it is recorded here as D-047). LICENSE updated; the README names Honorius Neogy (NEOGY LLC) as maintainer.** The personal name remains the one used as git author since Phase 1, which was inferred from the owner's address and not corrected by the owner when flagged (D-041).

**D-048 — Tolerances approved (D-039) with a reporting requirement (owner decision): every check now yields `exact` or `pass-tolerance` separately, and a `pass-tolerance` item carries per-field count, mean signed difference and maximum, so a systematic sub-tolerance bias is visible.**
Implementation: three-state comparison (`exact` / `tolerance` / `mismatch`) with signed differences; CLI table gains a `tol` column; the reference adapter must be exact and the test suite enforces it. The TLE-versus-OMM epoch rule (432 µs) is the provider's own quantisation and is reported as an observation on an exact pass, not as a parser tolerance; treating it as one made the reference adapter look tolerance-dependent, which was wrong.

**D-049 — SupGP-derived snapshot values are withheld from the public release (owner decision). Definition: the element values (epoch, mean motion, eccentricity, angles, BSTAR, first and second derivative) and raw TLE field substrings of CelesTrak supplemental records. Catalog numbers, object names and ids, counts, hashes, URLs and aggregate statistics are metadata and stay.**
Mechanics: `tools/public_scrub.py` (single definition), applied by `tools/export_public.py` on the way out (records of the two SupGP cases emptied and marked `records_withheld`; SupGP pair items removed from the two pairs cases, summaries kept), by `tools/make_failures.py` (value details withheld for those cases) and enforced by `tests/test_export.py`, which exports to a temporary directory and fails if any distinctive SupGP value string appears anywhere. `docs/INVENTORY.md` is dropped from the allowlist because it quotes SupGP field values. The runner falls back to the reference-reader oracle for the withheld cases and labels it. The leak test's distinctive set excludes four-decimal angles, seven-digit eccentricities and zero fields because identical values were found in unrelated GP records (false positives, verified), leaving epochs, mean motions and non-zero drag terms. The catalog-id text vectors gained a `parse_catalog_id` hook so nine-digit parsing is exercised without any SupGP element set.

**D-050 — export_tle report reproducer replaced (owner instruction) by an ordinary five-digit TLE: the CCSDS 502.0-B-3 annex G example rendered in CelesTrak's layout with a zero second derivative written `+0`; verified to differ from `export_tle` output at columns 51 and 69 only. One variable per report.**

**D-051 — Empty-OBJECT_ID report now offers a pull request; the patch is prepared, not submitted (owner instruction).**
`docs/upstream/patches/0001-omm-tolerate-empty-or-UNKNOWN-OBJECT_ID.patch`: `parse_xml` treats empty element text as `''`; `initialize` derives `intldesg` only from a `YYYY-NNNP...` value (empty and `UNKNOWN` give `''`; today `UNKNOWN` yields `'KNOWN'`); two tests in the style of `sgp4/tests.py`. Verified on a temporary copy of the installed package: applies cleanly; before the patch the test body raises `TypeError`; after it the new tests and all 47 functions of python-sgp4's own test module pass. Upstream sources for the diff were fetched from master (`omm.py` identical to the released 2.27). An earlier "before" check in the session used the wrong OBJECT_ID string and proved nothing; it was redone.

**D-052 — Next step is an independent audit in a fresh session (owner decision); this session stops after committing.**

**D-053 — Correction to D-049's last sentence: the leak test's distinctive set is epochs, mean motions and TLE epoch fields only. Five-digit drag terms were also removed after they matched unrelated objects' eccentricity and BSTAR strings (verified false positives in `analyst-objects`, `alpha5-tle-derived` and `bstar-and-derivative-forms`). The withholding definition (all SupGP element values) is unchanged; only what counts as proof of a leak is narrower.**

## 2026-09-21 (independent audit and its resolution)

**D-054 — Independent audit received (`AUDIT.md`, produced by a separate AI session with no access to the building context, instructed to trust nothing and recompute independently). Result: no wrong expected value (65,887 field comparisons, 60/60 sample); three critical documentation/tooling findings, eight major, a set of minor. Owner instruction: fix every finding, record each resolution here, annotate AUDIT.md without altering the auditor's text, then stop for review.**

**D-055 — C1 resolved by implementing the drift check, not by retracting the claim.** `tools/fetch.py` now has `manifest_sources`, `drift_report` and `print_drift`: after every run, and on demand with `--check-drift` (no fetching; exit code 2 if any stable source drifted), every file on disk is compared with the SHA-256 recorded in `manifest.json`; a stable-tier source whose bytes differ is printed as `DRIFT` with the consequence (frozen expected values no longer apply; gp-first stability assumption violated or snapshot out of date); live-tier differences are only counted. `tests/test_fetch_drift.py` covers match, live drift, stable drift, missing files, tier precedence and the real snapshot. README, `docs/PLAN.md` and the `gp-first-stability` ambiguity now describe the implemented behaviour. This supersedes the unimplemented promise in D-024 and resolves M5 (same defect, decision-log side).

**D-056 — C2 resolved: the float tolerance is now genuinely relative, |got − want| / max(|got|, |want|) ≤ 1e-12, with no floor. Exact zero on either side requires exact zero** (no relative scale exists for zero, and a parser that returns a tiny non-zero for a zero drag term is wrong, not approximately right). README table and D-039's wording corrected in the README. All three adapters re-run: reference still 16/16 exact; python-sgp4's case statuses and every item status are unchanged by the change (before/after report diff: none); FAILURES.md regenerated.

**D-057 — C3 resolved.** RESEARCH.md §13 corrected with a marked note (all 565 current analyst records carry the literal OBJECT_NAME `UNKNOWN`; 563 have an empty OBJECT_ID); the analyst case's coverage line and interpretation now say so explicitly and cannot be read as "real names".

**D-058 — M1 resolved.** The 24-character line-0 limit is now described as a rule of the CelesTrak format document, not an observed rendering behaviour: it occurs in none of the 304 provider pairs and only in one derived line (100465). Removed from the precision-loss interpretation's list of observed rules, added as a coverage gap, README case row corrected.

**D-059 — M2 resolved by test.** `tests/test_roundtrip.py` parses every TLE record present (CelesTrak-served, including re-captures, and the 604 derived lines: 1,208 records in 15 files), expresses it as OMM keyword text, renders it with the corpus rules and compares bytes: all 1,208 byte-exact. The claim "TLE → OMM → TLE round-trips" is kept and now cites the test.

**D-060 — M3 resolved.** README and the supgp case now state what primary sources support: the legacy range ends at 69999 (CelesTrak); 80000–89999 is the analyst range (Space-Track); other uses of the 70000–99999 block, including the 72000-series ids, rest on a secondary source and are labelled as such. "Pre-catalog" removed.

**D-061 — M4 resolved: reversal of D-014.** No excerpt of `satcat.txt` exists and none will: under D-023 (no raw provider bytes shipped) an excerpt of the legacy file would itself be raw provider bytes. What the excerpt was meant to prove (line count, id range 1–69999, contiguity, absence of ids ≥ 70000, the full-file SHA-256, parsed lines for 25544 and 69999) is in `fixtures/satcat-70000-cutoff/expected.json`, and the runner's data check recomputes the facts from the user's own fetch.

**D-062 — M6 resolved.** Final fetch-error tally: eight non-200 responses from celestrak.org across all phases — two guessed documentation URLs (Alpha-5.php, privacy.php; Phase 1) and six deliberate one-time TLE-format probes kept as fixtures (saramago.tle, last-30-days.tle and its owner-instructed re-capture, starlink-38381 TLE, saramago-first.tle, analyst-270449-first.tle). D-021's "five" was correct at the end of Phase 2 and stands as that phase's figure; RESEARCH.md §12 updated with a marked note.

**D-063 — M7 resolved.** The export_tle draft now says nine queries returned records (five further TLE-format requests returned HTTP 404); the record count 306 was and is correct.

**D-064 — M8 resolved.** The README's `docs/` row no longer lists INVENTORY.md (it is deliberately not exported, D-049); `docs/README-draft-ai-assistance.md` removed from the public allowlist (kept privately as the Phase 3 draft record).

**D-065 — Minor findings, all fixed.** Version unified to `0.1.0.dev0` (manifest `corpus_version`, README, pyproject, package). Fetch-list duplicate guard is case-insensitive; entries carrying `recapture_of` are the only permitted repeats. `--skip-case` added to `tools/fetch.py`; README fetch text corrected ("never re-fetches a file it already has unless `--force` and older than two hours"). "29 of 29" restated as 28 distinct records (29 file-records counting one object's CSV and XML). Uncited statements cited or softened: epoch-tolerance rationale now cites the cross-check's observed ≤1 µs; "served by Space-Track only" → "Space-Track serves it, CelesTrak does not"; "since 2020" restricted to CelesTrak (May 2020, primary) and Space-Track's GP class without a date; "XML libraries" → "Python's ElementTree (verified); other libraries may do the same". `docs/FAILURES.md` now carries its generation time and, per adapter, the run time, gpconf version, corpus version and parser from the report JSON (the runner writes `generated_at`). The four KVN variants the naive adapter fails are named (v02, v03, v04, v05; v01 and v06 pass), verified from the naive report. The omm-xml-schema text notes the dependency on SANA continuing to serve the 2.0.0 files at their direct URLs and points to the vendored copies. `tools/_fetch-run-*.log` removed from the allowlist. Correction to D-030: the 2.0.0 schema set has 12 files, the 4.0.0 set 14.

**D-066 — Publication of the audit.** `AUDIT.md` is committed with a resolution-notes section appended after the auditor's text, which is unaltered, and added to the public allowlist. Pre-allowlisting check: it contains no Space-Track data (the term appears only as a word in findings about the guard and the sources); its appendix sample contains one row from the nine-digit SupGP case, which `tools/public_scrub.py` now withholds from the exported copy with a note (the private original unaltered); the auditor's leak-scan sentence quotes a BSTAR string that an analyst GP record shares with a SupGP record, which identifies the GP record's value and stays. The README's AI-assistance section states that the audit is complete, links AUDIT.md, and says precisely that it was performed by a separate AI session with no access to the building context and was not a human review.

## 2026-09-21 (post-audit review)

**D-067 — Public copy of AUDIT.md (owner decision): the one Appendix A row from the `nine-digit-supgp-launch-nominals` case is withheld in the exported copy, per D-033 as implemented by D-049.** `tools/public_scrub.py` now inserts a notice under the appendix heading and a pointer under the title stating exactly what is withheld and that the private original is intact; `tests/test_export.py` checks for the notice and the absence of the row. The maintainer's resolution-notes preamble in AUDIT.md and the README's audit paragraph were qualified so that no public document describes the public copy as unaltered without that qualification (D-066's "which is unaltered" refers to the private original; this entry is the precision).

**D-068 — Record-count correction: 1,208, not 1,210.** The round-trip test and the CROSSCHECK table both cover 1,208 TLE records: 604 CelesTrak-served line pairs (306 distinct plus the 298 in the two owner-instructed re-capture files) and 604 derived Alpha-5 lines. The figure 1,210 appeared once in the repository (`docs/PLAN.md`, Phase 3 verification summary) and in session reports; it was a miscount, now corrected in place with a marker. No expected value or test depended on it.

**D-069 — Space-Track verification tool (owner instruction): `tools/verify_against_spacetrack.py`, standard library only.** Design: credentials only via `input()`/`getpass()`, never arguments, never logged or written; POST login to `/ajaxauth/login` with a cookie jar, exactly four GET queries (current GP for 100000–100020 and 270000–270020; first `gp_history` record of 100000 and of 270449; `format/tle`, `emptyresult/show`) at least 3 s apart, then `/ajaxauth/logout`; responses saved only under `~/spacetrack-verify/`, and the tool refuses to run if that path resolves inside the repository; comparison against `derived/alpha5-tle/*.tle` by catalog field and epoch (byte compare with differing column numbers when both match; encoding-field, international-designator and 0.05° inclination checks when only the field matches; line-2 field must equal line-1 field; strict Alpha-5 decoding; decoded id must lie in the queried range); output limited to catalog fields, verdicts, column numbers and summary counts; distinct non-zero exit codes for login failure, HTTP error, empty result, verification defect and refused output path. The comparison logic is unit-tested with the repository's own derived lines on both sides (`tests/test_verify_tool.py`); no Space-Track output is fabricated. The agent never ran the tool and never reads its output. Guard consequences: the tool's file name matches the Space-Track name guard, so `.gitignore` carries a single negation for that path and `tools/export_public.py` a single named exception; data paths (`spacetrack-verify/`, `space-track/…`, `SpaceTrack_*.tle`) remain blocked, verified with `git check-ignore`. The README gained a subsection explaining that anyone with their own account can run the tool and that the corpus redistributes no Space-Track data.

**D-070 — Owner's Space-Track verification run (2026-09-21) and what it changes.** The owner ran `tools/verify_against_spacetrack.py` and shared the tool's printed fields, verdicts, column numbers and summary (no element values, no lines; the agent never read the saved responses). Result: 44 records (21 current GP records 100000–100020, 21 for 270000–270020, the first historical record of 100000 and of 270449); summary `exact 0, same_epoch_differs 21, encoding_only 2, no_derived_line 21, invalid_field 0, line2_field_mismatch 0, out_of_range 0, defects 0`. Interpretation by TLE column layout: (a) the Alpha-5 encoding is corroborated by provider output for both letters that exist in real catalog numbers, A and T: every field decoded to the queried id and every line-2 field equalled line 1; (b) no line was byte-identical because of two rendering conventions, not encoding: Space-Track writes a zero second derivative as `00000-0` (column 51, with the checksum at 69) where CelesTrak writes `00000+0`; and Space-Track's line-2 eccentricity differs from CelesTrak's truncated rendering in its last one or two digits (columns 32–33, on 9 of 21 same-epoch records), consistent with rounding; (c) the first historical record of 100000 on Space-Track differs from CelesTrak's `gp-first` record at the same epoch in the international designator (columns 10–16) while line 2 is identical, so a CelesTrak "first record" may carry metadata updated after first publication; (d) 21 records had no derived counterpart (100001–100020 predate the 30-day snapshot; 270002 was not in the analyst group) and two matched by field only because Space-Track holds newer epochs. Consequences recorded: the precision-loss rule is now described as CelesTrak's rendering, not the TLE format's; the derived-Alpha-5 case's evidence basis and gap, the `ecc-truncation-vs-mantissa-rounding` and `gp-first-stability` ambiguities, and the README gap and tool paragraphs describe the verification; python-sgp4's `export_tle` sign convention is Space-Track's, which the upstream draft should say when filed (it already notes Space-Track was not checked by the project; the owner may add this observation). No Space-Track data entered the repository; the claim describes the verification only.

## 2026-09-21 (after the owner's Space-Track run)

**D-071 — Rounding confirmed by prediction; count corrected from nine to eight.** Using repository data only, the eccentricity text of the CelesTrak OMM source behind each of the 21 same-epoch derived lines (analyst.csv for T0000–T0020, saramago-first.csv for A0000, analyst-270449-first.csv for T0449) was rounded half up to 7 digits and compared with truncation. Rounding differs from truncation for exactly 8 records, T0000, T0003, T0008, T0011, T0016, T0018, T0020 and T0449, three of them with a carry into the sixth digit (T0003, T0020, T0449), and those are exactly the 8 records whose line 2 differed at column 33 in the owner's run, with the three carries at columns 32–33; the other 13, where rounding equals truncation, matched (A0000 among them). The prediction holds 21 of 21, so the precision-loss case now states Space-Track's eccentricity rounding as confirmed rather than "consistent with". All 21 derived lines carry a zero second derivative, so the column-51 difference on every record is the +0/−0 sign. D-070 said "9 of 21"; the correct count is 8, and my report of the run said nine: a miscount from the screenshot, corrected here.

**D-072 — export_tle report withdrawn as a bug report (owner decision) and rewritten as an informational note marked not to file.** Reason: python-sgp4 writes a zero second derivative as ` 00000-0`, which is Space-Track's rendering, and Space-Track is the outlet of the organisation that issues the element sets; CelesTrak's ` 00000+0` is the outlier. Measured only against CelesTrak lines the behaviour looked like a library defect; measured against the issuing organisation's output it is a provider divergence with python-sgp4 on the issuer's side. README, the cross-check interpretation, the precision-loss case's library note and the patches README now say so; the note keeps the measurements and the reproducer as documentation of the divergence.

**D-073 — Record of who launched the verification tool (owner instruction to record that the agent launched it despite the instruction that the owner would run it).** Owner's account: the agent launched the tool. Session record available to the agent: the agent made no tool call that executed `tools/verify_against_spacetrack.py`; both invocations reached the session as terminal-pane input, the first from the wrong directory and the second after the agent supplied the corrected command in a shell-tagged block, which the desktop app renders with a Run button, so the agent's act was presenting a runnable command that the owner's action executed. The discrepancy is recorded rather than resolved silently; the owner may amend this entry after reading it. On either account the verdict-only output design held: no credentials and no element data entered the session through the tool. The only tool output the agent saw was the screenshot the owner chose to share, which contains catalog fields, verdicts, column numbers and counts.

**D-074 — gp-first records are historical in their elements but can carry metadata assigned after first publication (owner's finding while checking the designator; categories only, no values).** For catalog 100000: Space-Track's earliest record has a blank international designator; Space-Track's current record carries the same designator as CelesTrak; CelesTrak's `gp-first` record shows that designator back-filled; the line-2 elements are identical across all of them. The `gp-first-stability` ambiguity now says so and tells parsers to treat OBJECT_NAME and OBJECT_ID in a gp-first record as current metadata.

**D-075 — Incident record (owner's report, recorded without data).** While checking the designator the owner ran a grep in the Claude Code terminal panel that printed two Space-Track TLE lines. The panel has been cleared; no Space-Track data entered the repository; future inspection of `~/spacetrack-verify/` happens only in a standalone terminal. The agent did not read, quote or use anything from that terminal scrollback (no terminal-read call was made at any point in the session) and will not; it has never opened anything under `~/spacetrack-verify/`.

## 2026-09-21 (publication preparation)

**D-076 — Amendment to D-073 (owner's clarification): the owner launched the verification tool themselves, via the Run button on the command block the agent had posted.** That reconciles the two accounts in D-073: the agent presented a runnable command; the owner's click executed it in the owner's terminal. D-073 is left as written per the append-only rule; this entry is the account of record. The design consequence stands: no credentials and no element data entered the session through the tool.

**D-077 — Version 0.1.0 for publication; CITATION.cff added; Zenodo DOI per release.** `CITATION.cff` names the corpus, NEOGY LLC as copyright holder, the maintainer as author, MIT, version 0.1.0, release date 2026-09-21 and a repository URL placeholder the owner fills in. To keep the citation, the manifest, the package and the README consistent (an audit finding, D-065), `corpus_version`, `pyproject.toml`, `gpconf.__version__` and the README now all say `0.1.0`. The README states that a Zenodo DOI will be minted per release and carries a badge placeholder until the first DOI exists; CITATION.cff gains the DOI then. CITATION.cff is on the public allowlist.

**D-078 — Migration writeup drafted at `docs/writeup/post.md` for publication on neogy.dev; not part of the public export** (the allowlist names the docs files individually and does not include `docs/writeup/`). Every claim in it cites a case id, a DECISIONS entry or a test; it states the AI-assisted method and the independent audit; it contains no Space-Track values. Under 1,500 words.

**D-079 — Public export produced and verified, unpushed (owner instruction).** `tools/export_public.py` writes the allowlisted subset of the private repository (at the commit that adds this entry) into `../gp-omm-conformance-public`, which is initialised as a new repository with a single initial commit authored by the owner and no remote. Verification, run on a trial export and again on the final one with an independent script outside the repository: tests pass in a fresh `git clone` of the public repository and the reference adapter reports 16 of 16 cases exact; no `fixtures/*/raw` directory, no file byte-identical to a private raw file, no verbatim raw TLE line or CSV row anywhere; none of the SupGP signature strings (from the private expected values and the raw SupGP files) anywhere; no Space-Track-named path other than the verification tool's own source, and no TLE-looking line outside `derived/alpha5-tle/` other than the CCSDS annex example (catalog 23581) used in the upstream notes; no e-mail address; every file matches a `PUBLIC_ALLOWLIST.txt` pattern, plus the tool-generated `EXPORT-MANIFEST.txt` (path, bytes, SHA-256 of every exported file). Five files were scrubbed on the way out (four expected files and the public copy of AUDIT.md, which states what it withholds). Publication itself (adding a remote, pushing, minting the DOI, filling the repository URL in CITATION.cff and README) is the owner's step.

**D-080 — Correction to D-079 (found during the final verification): in a fresh clone of the public repository, before `tools/fetch.py` has run, the reference adapter reports 3 cases exact (the shipped vectors, derived Alpha-5 lines and KVN variants), 12 skipped for missing raw files and 1 not exercised; "16 of 16 exact" holds in the private repository with the tested snapshot on disk, and, after a user fetches, for the stable-tier sources whose hashes still match, while live-tier sources are compared against the reference reader.** The public export was regenerated so that its copy of this log carries the correction; the independent verification script's file walk was also fixed (it had excluded `.gitignore` and `.github/workflows/ci.yml` by a prefix filter) and re-run over every file.

## 2026-09-21 (publication)

**D-081 — Publication authorised by the owner (employment-agreement check complete), scoped to: confirm the GitHub account, fill the public URL, regenerate and verify the export, create the public repository with description and topics, push; no release or tag until Zenodo is enabled.** Account check: the GitHub CLI is authenticated as `hneogy` (a user account, display name Honorius Neogy; the token scopes were `repo`, `workflow`, `read:org`, `gist`, and the owner added `user` so verified addresses could be listed); the account's primary verified address, which is also the private repository's git author address, was used as the commit author. The public URL `https://github.com/hneogy/gp-omm-conformance` was filled into CITATION.cff, the README (citation and clone line) and the writeup; the Zenodo badge placeholder remains until the first DOI. The public export was regenerated from the private commit that follows this entry as a single commit authored by the owner and re-verified from a fresh clone with the independent script (D-079/D-080 procedure).

**D-082 — D-081 was reworded minutes after being written, before publication, to describe the commit-author address instead of spelling it out, so that no literal e-mail address appears in a published document (the README's maintainer line deliberately carries none; the address is visible only as ordinary git author metadata, which the owner chose). Recorded here because the log is append-only and the earlier text was changed.**

**D-083 — Published.** Owner confirmed that `hneogy` is their personal GitHub account and authorised creation and push. `CITATION.cff` already carried `affiliation: "NEOGY LLC"` on the author entry (Zenodo reads it for the DOI record); verified present in the export rather than added twice. The public repository `https://github.com/hneogy/gp-omm-conformance` was created public on 2026-09-21 with the description "Conformance corpus for orbital-data parsers crossing the five-digit catalog-number boundary: Alpha-5, six- and nine-digit NORAD_CAT_IDs, CCSDS OMM as served by CelesTrak. No raw provider data redistributed." and the topics orbital-mechanics, tle, omm, satellite, sgp4, celestrak, conformance-testing, alpha-5; `main` was pushed as the single export commit authored by the owner; no release and no tag were created (Zenodo must be enabled first; the first tag mints the first DOI). The private build repository remains the source of truth and is never pushed; future public updates are regenerated exports.

**D-084 — Release v0.1.0 created (owner authorisation): tag `v0.1.0` at the published export commit `fdf4043`, title "v0.1.0 — initial release", published 2026-09-21T04:36:16Z, notes drawn from the README (what the corpus is, coverage, headline findings, verification and audit, what is not included, how to run); no data values in the notes. A first attempt failed GitHub's validation because the release target was given as a short commit hash; nothing was created by it, and the retry targeted the `main` branch, whose head is that commit. Zenodo was enabled by the owner before the release, so this tag mints the first DOI; the owner will supply it, after which it goes into `CITATION.cff` (`identifiers`) and the README badge in the private repository, followed by a regenerated export and, with separate authorisation, a push.**

**D-085 — DOIs recorded and second public push (owner authorisation).** Zenodo minted the concept DOI 10.5281/zenodo.22867654 (all versions; resolves to the latest release) and the version DOI 10.5281/zenodo.22867655 (v0.1.0). `CITATION.cff` now carries the concept DOI as its top-level `doi` and both under `identifiers` with descriptions; the README badge placeholder was replaced by the concept-DOI badge and the citation note names both DOIs and when to use each; the writeup gained the concept DOI. The public export was regenerated and committed on top of the existing public history as a second commit authored by the owner, pushed to `main` without force; the `v0.1.0` tag stays on its original commit `fdf4043`, which is what the version DOI archives. Verified from a fresh clone before the push with the independent script (tests, raw bytes, SupGP signatures, Space-Track data, e-mail addresses, allowlist).

**D-086 — Upstream issue filed (owner authorisation, one issue, from the owner's GitHub account): https://github.com/brandon-rhodes/python-sgp4/issues/171 — the empty-OBJECT_ID XML import bug, body taken from `docs/upstream/python-sgp4-xml-empty-object-id.md` from its Environment section onward, without the draft-status line and the internal patch-location note, with the offer to submit a PR with fix and tests (no PR opened, no patch attached), a one-line cross-reference to #168 (the opposite, export-direction bug, whose own text says initialize() accepts an empty OBJECT_ID, which holds for CSV but not for XML), and the closing line naming the corpus. No labels, mentions or assignees. Gate checks before filing: PyPI's latest python-sgp4 is still 2.27 (released 2026-07-03), so the reproduction on 2.27 is current; searches of the repository's issues and pull requests (OBJECT_ID, parse_xml, NoneType, empty element, omm, initialize, intldesg) found no prior report of the import-path failure. Also noted for the owner: the corpus's other finding, nine-digit catalog numbers, was independently reported upstream as #169 (2026-08-20) with an open PR #170, so the corpus's nine-digit draft should not be filed as a new issue.**

**D-087 — Nine-digit upstream draft marked superseded (owner instruction).** The defect was independently reported as brandon-rhodes/python-sgp4#169 on 2026-08-20 (before this corpus existed) and PR #170 has been open since 2026-09-01; filing a second issue would duplicate both. The draft stays in `docs/upstream/` with a superseded banner and links, as the record of the corpus's evidence; the corpus contributes a local test of PR #170 instead (D-088), drafted as a comment and not posted (D-089).

**D-088 — PR #170 tested locally (owner instruction); result: the PR as it stands does not fix the nine-digit defect.** Throwaway environments only, nothing installed in the repository's own environment: the PR branch of brandon-rhodes/python-sgp4 was cloned to the session scratch directory at head `5e4f308` (four commits on top of master `bf25b00`, all dated 2026-09-01) and installed twice, with the C++ extension (`api.accelerated == True`) and without it (`PYTHON_SGP4_COMPILE=never`); the 2.27 source release was installed the same two ways as the baseline. Python 3.14.4, macOS. Findings: (1) the PR's net diff against master is `sgp4/tests.py` +34 and `NOTES.txt` deleted; commit `95f7f4e` changed `omm.initialize` to pass `str(satnum)` to `sgp4init` and commit `5dd0040` reverted it, so no library code change survives. (2) The no-network reproducer (CCSDS 502.0-B-3 annex G example values, `NORAD_CAT_ID` 799501621) still raises `ValueError: satellite number cannot exceed 339999` on both PR builds, exactly as on 2.27. Boundary: 339999 loads and `export_tle` renders `Z9999`; 340000 is refused by `initialize`, so the owner's question whether `export_tle` still refuses numbers above 339999 cannot be exercised through the public API: `initialize` refuses first, on every build. (3) The PR's two new tests skip on the accelerated build and fail with the same `ValueError` on the pure-Python build; the remaining 44 tests pass on every build (one test needs numpy, not installed). (4) Corpus runner, python-sgp4 adapter, all 16 cases, private raw files: identical before and after the PR on each build; 19 failing items accelerated (the `docs/FAILURES.md` baseline) and 21 pure-Python, in both versions; 0 fixed, 0 regressions, no case status changes. Side finding, unrelated to the PR: the two extra pure-Python items are the supplemental-classification case's `.tle` file, because the pure-Python `twoline2rv` resets `C` to `U` in `sgp4init` (`sgp4/model.py`) while the accelerated one preserves it; added as a section to `docs/upstream/python-sgp4-omm-initialize-drops-classification.md`. `docs/FAILURES.md` is unchanged: its baseline is the accelerated build and that number (19) is confirmed. Reports kept in scratch only (`report-sgp4-pr170.json`, `report-sgp4-venv-227-pure.json`, `report-sgp4-venv-pr170-pure.json`); nothing from the throwaway environments was committed.

**D-089 — PR #170 comment drafted, not posted; README gains an Upstream section (private only).** The comment is `docs/upstream/pr170-test-comment.md`: setup, net diff, the CCSDS-example reproducer, boundary behaviour, the PR's own tests, corpus counts (6 fail / 7 pass-tolerance / 2 pass / 1 skip; 19 and 21 failing items; 0 fixed, 0 regressions), one link to the corpus, no SupGP element values, no raw provider data, no Space-Track data (the nine-digit id 799501621 is a catalog field already present in the public nine-digit draft, not an element value). Posting requires the owner's explicit authorisation. The README's new `## Upstream` section lists #171 (filed), #169/#170 (independently reported, tested locally), the `export_tle` note (not a defect, D-072) and the classification note (draft only); it sits in the private repository for the next public update. Nothing was posted and nothing was pushed; the public repository and its export directory were not touched.

**D-090 — PR #170 comment revised for tone (owner instruction), still not posted.** The first draft read as a verdict on another contributor's work. The revision names the exact commit tested (5e4f308) so it stays accurate if more commits arrive, credits the two new tests as useful and correctly targeted, frames the missing fix as "at this commit the first commit's library change is no longer present" with the second commit's stated reason (C++ accelerated compatibility) rather than "does not work", states the CI risk neutrally (both tests skip on the accelerated build, so green CI would not show the nine-digit case loading), keeps the evidence to three lines (reproducer raises on both builds, pure-Python build fails both new tests, 16-case corpus shows no fixes and no regressions), offers to test follow-up commits, links the corpus once, and stays under 200 words. Same data rules as D-089. Not posted; nothing pushed.

**D-091 — PR #170 comment posted (owner authorisation, from the owner's GitHub account, verbatim, specific nine-digit id kept):** https://github.com/brandon-rhodes/python-sgp4/pull/170#issuecomment-5755703491. Checks before posting: `gh` authenticated as the owner's account; PR open at head `5e4f308`, the commit the comment names, with no prior comments; body extracted from `docs/upstream/pr170-test-comment.md` below the rule, 176 words, nothing added. Only the comment text was posted; the note about the id stayed in the private repository. No review verdict, labels, mentions or assignees. The draft file's status line now records the URL. Nothing pushed to the public repository.

**D-092 — Pull request for #171 prepared on the owner's authorisation; fork created and branch pushed; PR not opened.** The maintainer replied on #171 (2026-09-21) inviting a PR. Steps taken exactly as authorised: forked brandon-rhodes/python-sgp4 to hneogy/python-sgp4; branch `omm-empty-object-id` from upstream master `bf25b00` (unchanged since 2.27); applied `docs/upstream/patches/0001-omm-tolerate-empty-or-UNKNOWN-OBJECT_ID.patch` unmodified (2 files, +28 −2: `parse_xml` empty element → `''`; `initialize` derives `intldesg` only from `YYYY-NNNP...`, else `''`, covering `UNKNOWN`; the `parse_xml` change is the OBJECT_NAME guard, since `initialize` never reads OBJECT_NAME; two tests beside the existing OMM tests in `sgp4/tests.py`; nothing else). Conventions checked: no CONTRIBUTING file; CI runs `python -m sgp4.tests`; commit subjects are sentence-case imperative with backticked code names; the changelog lives in README.rst and is written by the maintainer at release time (contributor commits such as the `gravconst` one did not touch it), so no changelog entry was added. One commit `d281247`, authored with the same identity as the public corpus commits (name and e-mail already public there) and the Co-Authored-By trailer those commits carry; message ends "Fixes #171". Verification: `python -m sgp4.tests` on Python 3.14.4, 47 tests OK on the compiled C++ build and on the pure-Python build (`PYTHON_SGP4_COMPILE=never`, numpy installed so no test is skipped for a missing module); the two new tests are collected; the README doctests pass. Branch pushed to the fork only. PR title and description drafted in `docs/upstream/pr171-pull-request.md`: short, "Fixes #171", no corpus mention. The pull request is not opened; that awaits the owner's approval.

**D-093 — Correction to D-092's sequence.** D-092 was written while the push command was still pending and reported the doctests and the push as done; at that moment the first doctest invocation had failed (run from inside the source tree, Python imported the pure-Python package and README's `accelerated` example printed False; a second run from outside the tree lacked the `sample_omm.*` files) and the chained push had therefore not executed. Re-run correctly, from a directory holding README.rst and the three sample files with the compiled build installed: all 71 README examples pass. The two new tests are collected by `python -m unittest sgp4.tests`. The branch was then pushed: `hneogy/python-sgp4` branch `omm-empty-object-id` at `d281247`, one commit ahead of `brandon-rhodes:master`, touching only `sgp4/omm.py` and `sgp4/tests.py`. The PR is not opened. Lesson recorded: log an action after its verification output, not before.

**D-094 — Pull request opened (owner approval): https://github.com/brandon-rhodes/python-sgp4/pull/172.** From `hneogy:omm-empty-object-id` (commit `d281247`, one commit ahead of `brandon-rhodes:master` at `bf25b00`, files `sgp4/omm.py` and `sgp4/tests.py` only) into `brandon-rhodes/python-sgp4` `master`, from the owner's GitHub account, title and description taken verbatim from `docs/upstream/pr171-pull-request.md` (the description body read back from GitHub is identical to the draft). Pre-checks: account `hneogy`; branch one ahead, zero behind; no pull request existed for the branch. No labels, reviewers, assignees or mentions. The draft file's status line now records the PR URL. Nothing else was posted or pushed.

## 2026-09-22 (writer-side cases, first commit)

**D-095 — Writer-side case `tle-writer-alpha5` (new kind `writer`), adapter hook `write_tle`, external `--write-cmd`; inputs are records already frozen in the corpus; the precision rule accepts either provider's convention; secondary fields are information only; version constants unchanged.**
Alternatives: (a) test writers only through the existing `alpha5_encode` vector hook; (b) compare written lines byte for byte with CelesTrak's rendering; (c) fetch new records for the case; (d) bump the version now.
Reason: the corpus tested readers only, while every newly catalogued object needs an Alpha-5 field and amateur tools generate TLEs for new launches (owner's brief, 2026-09-22). The hook mirrors `parse` (same record keys; raise to refuse); `CommandParser.write_tle` runs `--write-cmd` with one JSON record on stdin (exit 3 = unsupported, other non-zero = refused). Inputs: the 604 derived Alpha-5 records (603 distinct ids, letters A and T; 270449 is in two derived files), the first ISS record (1998 epoch, negative first derivative, non-zero second derivative, zero BSTAR), the first record of 69999 (negative BSTAR and first derivative) and of analyst 81011 (blank designator, empty OBJECT_ID): 606 records whose canonical values are already frozen in v0.1.0's expected files, so the case runs offline with no new CelesTrak request and no new raw bytes; each input records the frozen file it was copied from, the source file and its SHA-256. Checks: `tle-checksums-valid` (reused), `tle-writer-catalog-field` (strict: five digits, or one letter of the table plus four digits; both lines equal), `tle-writer-round-trip` (the lines read back by the reference reader equal the input at each field's resolution by truncation or by rounding half up, because CelesTrak truncates the eccentricity and Space-Track rounds it, D-070/D-071; the convention observed is reported per field; this is the format's resolution, not a tolerance, and the reference writer stays exact), `tle-writer-refuses-unencodable` (D-096), and two information-only items, `tle-writer-secondary-fields` (derivatives, element set, revolution, classification, designator, name, and the sign written for a zero second derivative) and `tle-writer-matches-provider-rendering`, because fitting tools regenerate those fields by design and a rendering under the other provider's convention is not a defect. Wording rule (owner instruction): a refusal for a number the TLE field cannot represent is described everywhere, including `docs/FAILURES.md`, as the correct output, never as a capability gap. Adapters: reference = `gpconf.tle.render` (CelesTrak conventions; 606 of 606 fields byte-identical to the provider or derived rendering); naive = the catalog number through an integer format (six digits and 70-character lines above 99999, never refuses); python-sgp4 = `omm.initialize` + `exporter.export_tle`. `tools/make_expected.py` now accepts case ids so the new file could be built without rewriting the sixteen frozen ones (none was rewritten; `tools/make_manifest.py` regenerated the other case.md files identically). Version constants stay at 0.1.0; the number is assigned at tag time after the site is updated (owner instruction: merge, leave unreleased, update the site, then tag; the site, the forum post and the Zenodo record all say sixteen cases).

**D-096 — Three synthetic-derived refusal inputs (owner approval 2026-09-22; PLAN.md §2.1 reserves this provenance for owner approval): the SARAMAGO first record with NORAD_CAT_ID replaced by 340000, 799501621 and -1, the values of `vectors/alpha5.json` `encode_unrepresentable`.**
Alternatives: refusal tested only through the `alpha5_encode` hook.
Reason: the failure that matters is at line level, where the vector hook cannot see it: an integer-formatted writer emits six digits in a 70-character line, and strf's helper emits a blank field (D-097). The elements are real, the id is a specification vector no catalogued object carries, and every such input says so and carries the source record's SHA-256. Observed: the reference writer refuses all three; the naive writer writes lines for all three (`34000` in a 70-character line, `79950` in 73, `-0001` in 69); python-sgp4 2.27 refuses 340000 and 799501621 in `omm.initialize` and writes `-0001` for -1 (`to_alpha5` has no lower bound, as `alpha5-encoding-vectors` already records), so its writer-case status is fail on that one id.

**D-097 — strf investigation record: what was tested, what was inferred, the settled reachability statement, and why there is no adapter. No bug is claimed for the rffit binary.**
Target: strf (https://github.com/cbassa/strf, GPL-3.0, HEAD 92d2425 of 2026-03-06), the writer behind satno2tle (https://github.com/hobisatelit/satno2tle, HEAD 538269b of 2026-06-14). Both were cloned into the session scratch directory, outside both repositories; no strf code entered this repository (GPL boundary; an adapter may only call the binary). Where strf writes: `format_tle` (rffit.c lines 111-149) writes the catalog field as `%5s` of the string returned by `number_to_alpha5` (satutl.c lines 45-58: the table character at index `number/10000`, 34 characters with I and O skipped, no range check), classification `U`, a zero first derivative, a zero second derivative with the `-` sign, ephemeris type 0, element set 0 and revolution 0; the eccentricity through `%07.0f` (rounded) and BSTAR through `%11.4e` (five digits, rounded); the checksum counts digits and `-` (lines 130-146); `print_tle` (lines 1853-1890) writes the name, the two lines and a `#` trailer with LF endings, on the `w` key (lines 902-907). Alpha-5 reading was added 2024-12-22 (c1eb772, d3f5ab0) and writing 2024-12-24 (dbc250f); strf's own tests cover decoding only. Build: the rffit binary needs PGPLOT, X11, GSL and gfortran, none present here; nothing was installed. Instead strf's unmodified `satutl.c` and `rffit.c` were compiled with clang in the scratch directory (`main` renamed by a macro, gnu89, an empty stub `cpgplot.h`, undefined symbols resolved lazily) and linked to a 40-line harness of the agent's own that calls the two functions directly. Tested at function level: 5→00005, 99999→99999, 100000→A0000, 109999→A9999, 110000→B0000, 179999→H9999, 180000→J0000, 229999→N9999, 230000→P0000, 270449→T0449, 339999→Z9999, all as the corpus vectors; the real SARAMAGO first record with 100000 gives two 69-character lines with valid checksums whose field decodes to 100000 in the corpus reader, differing from the corpus's CelesTrak-style line only at columns 40-42 (first derivative zeroed), 51 (`-0`) and 65-69 (element set, revolution, checksum); 270449 gives T0449 with the eccentricity rounded (0045560, where CelesTrak truncates to 0045559) and the BSTAR mantissa rounded (10753-3); 69999 keeps `-70517-5`; for 340000, 350000, 999999 and 799501621 the helper returns an empty string (first byte NUL: index 34 is the table's terminator; above 34 the read is past the array, undefined behaviour) and the lines carry five blank columns 3-7, still 69 characters with a valid checksum and exit 0; -1 gives `0-001`. Side observation outside the catalog-number scope: `%2d` of `ep_year-2000` writes ` 5` for 2005 and `-2` for 1998 in columns 19-20. Reachability (code trace plus a decoder harness; one answer): every copy of a parsed catalog entry into the orbit (rffit.c lines 169, 207, 232, 418, 856, 929) goes through `alpha5_to_number`, whose arithmetic caps the result at 33×10000+9999 = 339999 (harness: Z9999→339999, ZZZZZ→330000, I0000→0); the `-i` option (line 333) only selects such an entry (lines 411-420); the only route for a larger number into `format_tle` is the interactive `c` menu item 9 "Satellite ID" (line 769, `atoi` without a range check). Inferred, not run: rffit has no non-interactive write path (options `-d -c -i -s -g -m -F -h`; display hard-coded `/xs`; keys through `cpgband`), so an adapter would need an X display and keystroke injection, which is what satno2tle's auto.sh does with xterm and xdotool (lines 293-303); auto.sh line 212 passes the raw catalog-field text of TLE line 2 to `rffit -i`, which applies `atoi`, so an Alpha-5 field would become 0. Decision: no strf adapter (owner's rule: only if buildable and runnable here); the writer case exercises the reference, naive and python-sgp4 writers, and the case text describes strf's behaviour as function-level observations. A hardening note for upstream is a separate decision (later entry, draft to the owner first).

**D-098 — Writer-result wording, so that a failure confined to the synthetic refusal inputs cannot read as a general failure (owner instruction after commit 1).**
Alternatives: leave the status table's "fail (1 fail)" to speak for itself; list passing items for every case in `docs/FAILURES.md`.
Reason: python-sgp4's writer-case status is fail because of the synthetic -1 vector alone, while it passes all 606 real records and both real-range refusals; a reader of the status row or of the single listed failure could not tell. Changes, none of them to check semantics: the runner's writer details now open with what passed ("2 of 3 numbers the TLE catalog field cannot represent (synthetic inputs, D-096) correctly refused (340000, 799501621); written instead of refused: id -1 ..."; "3 of 606 catalog field(s) correct; ..."); for the writer case `docs/FAILURES.md` lists, under each adapter, the checks it passed with their record counts, and its reading guide says why; the python-sgp4 finding in the case document states the 606-of-606 result, both refusals and the single synthetic failure in so many words. The three adapters' statuses are unchanged; the failure catalogue's other sixteen cases are unchanged.

**D-099 — `python -m gpconf check-tle FILE... [--against RECORDS] [--json OUT]`: the writer checks applied to a TLE file a tool wrote (owner instruction: its own commit, because it is what strf and satno2tle users would run).**
Alternatives: adapters only, with documentation for tools that cannot be wrapped; a separate script under `tools/`.
Reason: rffit is interactive and cannot be driven by `write_tle` or `--write-cmd` (D-097), but the file it writes can be checked. Design: a subcommand of the runner over the same functions in `gpconf/writer.py` (no second implementation); records are extracted leniently, any `1 ` line followed by a `2 ` line whatever their lengths, so a 70-character line is reported rather than skipped, with the preceding line as the name unless it is blank, an element line or a `#` comment (rffit's trailer, satno2tle's added lines); the format checks (69 characters, checksums, a strict five-digit or Alpha-5 field identical on both lines) run on every record; a six- to nine-digit number in the catalog columns is named with the Alpha-5 form it should have had or, above 339999, with the statement that a refusal was the correct output; the round trip and the secondary-field report run only when `--against` supplies the source records (any format the reference reader knows), matched by catalog number, and a missing source record is said so rather than failed; the zero second-derivative sign is noted. Exit 1 on any failing record or when no record is found; `--json` writes the per-record results. Tests use a file in rffit's layout produced by the corpus renderer, a naive six-digit line, a nine-digit line and a blank-field line; no output of an external tool is shipped.

## 2026-09-22 (writer-side cases, third commit: documentation)

**D-100 (2026-09-22) — Documentation of the writer-side work: `docs/WRITERS.md` public and allowlisted (owner decision), `CHANGELOG.md` with an `## [Unreleased]` entry and no number, a README section; version constants unchanged; release sequencing recorded.**
Alternatives: a private-only note; recording the findings in this log alone; bumping the version now.
Reason: the strf observation is the kind of claim that must be reproducible by anyone, so the document carries the harness source (the agent's own, about forty lines) and the exact compile commands, while strf itself is cited by file and line and never quoted (GPL boundary). The changelog entry carries no number because the number is assigned at tag time under the README's rule, and the tag waits: the public site says "sixteen cases" in four templates and its timeline, and the forum post and the Zenodo record say sixteen cases with thirteen failing under the naive parser, so a seventeen-case release before the site is updated would contradict all of them at once. Sequence, owner's steps: merge these commits, leave the corpus unreleased, update the site (its own S-decision), then tag; regenerating the public export and pushing remain separately authorised and were not done. The local memory notes on this computer were written (the folder was empty here) and the synced copies in `docs/handoff/memory/` refreshed; the handoff file gained an update section.

**D-101 (2026-09-22) — Upstream hardening note for strf drafted, not sent (owner instruction: decide separately, draft to the owner first).** `docs/upstream/strf-number-to-alpha5-range-check.md`, about 270 words (a minute's read), in the owner's order: Alpha-5 writing correct across the representable range (tested at function level); no range check above 339999, where the lookup lands on the table's terminator and the lines carry a blank field (tested: 340000); an out-of-bounds read from 350000 upward (tested on one machine, labelled undefined behaviour); the single reachability route, the interactive Satellite ID entry, with the statement that no real catalog number reaches 339999 today. Prose only, no diff, no strf code, no claim of a bug in the binary; the suggestion is a range check in the helper or a refusal in the write path, with a patch offered. Whether and where to send it (issue, pull request or message) is the owner's action; nothing was sent, and the corpus's public documents say so.

**D-102 (2026-09-22) — Release 0.2.0 prepared (owner instruction), not committed, tagged or pushed: version constants bumped from 0.1.0 to 0.2.0 (`tools/make_manifest.py`, `pyproject.toml`, `gpconf/__init__.py`, README, `CITATION.cff`), the changelog heading set to 0.2.0 with today's date, manifest and failure catalogue regenerated, export and test suites run.**
Alternatives: 0.1.1 (rejected on 2026-09-22: an added case is a minor bump under the README rule); waiting for the site (already updated, S-018 in the site repository, uncommitted there).
Reason: the sequence the owner set, merge, leave unreleased, update the site, then tag, has reached its last step. Two statements of honesty added with the bump: the README status line and `CITATION.cff` now say the independent audit covered v0.1.0 and the writer-side case was not separately audited; the sixteen v0.1.0 cases and their expected values are unchanged (`make_expected.py` was not re-run for them). The v0.2.0 version DOI does not exist until Zenodo mints it at the tag, so `CITATION.cff` keeps the concept DOI and the v0.1.0 version DOI with a comment marking the slot, and the README says the same; adding the new DOI is the owner's step after the release, as for v0.1.0 (D-085). `docs/writeup/post.md` (private, not exported) still describes sixteen cases and is left for the owner. Regenerating the public export into `../gp-omm-conformance-public` and pushing remain separately authorised.

**D-103 (2026-09-22) — Publication of 0.2.0 carried out as far as this machine allows (owner authorisation for commit, export, push and tag): private release commit bbcfeee; public export commit b54c3b8 and annotated tag v0.2.0 created locally in `../gp-omm-conformance-public`; push not possible from here.**
What was done: the prepared release diff (13 files, D-102) was reviewed and committed on private main as bbcfeee; the export was regenerated from it into the public working copy (145 files, 5 scrubbed, 0 refused; 34 paths changed against 78ba712, among them CHANGELOG.md, docs/WRITERS.md, the strf note, the writer case, gpconf/writer.py and tests/test_writer.py; no raw directory; constants and manifest 0.2.0; CHANGELOG headings 0.2.0 and 0.1.0) and committed as b54c3b8 with the owner's public identity, set per command because this computer has no git identity configured (it would have written a machine-local placeholder address, which the private commits of 2026-09-22 carry); a fresh clone of the public repository at b54c3b8 passed 37 tests, the two CI commands (naive exit 1) and the offline reference run (4 pass, 12 skip, 1 not exercised). Why nothing was pushed: no GitHub credential in the keychain, no gh CLI, and SSH to github.com refused (no key), so the push and the tag push are the owner's, with `git push origin main` then `git push origin v0.2.0` from the public working copy. Recorded for the owner: Zenodo's GitHub integration archives a GitHub release, not a bare tag; v0.1.0 was created as a release (D-084), so minting the v0.2.0 DOI needs a release created for the tag once it is pushed. CITATION.cff, the README and the site were not touched further, as instructed.

## 2026-09-23 (v0.2.0 published, version DOI, strf note filed, analyst-count correction)

**D-104 (2026-09-23) — v0.2.0 published (owner authorisation): public `main` pushed from `78ba712` to `b54c3b8` and the annotated tag `v0.2.0` (object 0136589 → b54c3b8) pushed, both confirmed from the remote on 2026-09-22; no GitHub release yet.**
How: the owner ran the push from the app's terminal with a fine-grained personal access token (Contents: read and write on the one repository), because this computer had no stored credential, no gh and no SSH key (D-103); the first attempt with the account password failed, as GitHub no longer accepts passwords for git over HTTPS; the token was entered only at git's prompt, never in the session. The tag push completed under the credential the keychain stored after the successful push. Confirmed from the remote: `refs/heads/main` = b54c3b8, `refs/tags/v0.2.0` present, public main's manifest reads 0.2.0 with 17 cases; the GitHub releases API lists v0.1.0 only. Next, owner's steps: create the GitHub release for v0.2.0 (that, not the bare tag, is what Zenodo archives to mint the version DOI; v0.1.0 was created as a release, D-084); then supply the DOI so CITATION.cff and the README can carry it (regenerated export and a further push, separately authorised); then commit and deploy the site (S-018, S-019), filling the timeline entry's publish time and commit b54c3b8.

**D-105 (2026-09-23) — GitHub release v0.2.0 published at the owner's request ("can you do the release"), from the app's built-in browser in the owner's own GitHub session: title "v0.2.0 — writer-side case", existing tag v0.2.0 at b54c3b8, label Latest, not a pre-release, published 2026-09-23T00:16:27Z.**
How: the owner signed in to GitHub in the browser pane themselves (the agent entered no credentials); the form was filled with the title and the body drafted from CHANGELOG.md (checked against the writer case's expected.json: 606 frozen inputs = 603 Alpha-5 ids, 257 with letter A and 346 with letter T, plus 25544, 69999 and 81011; three synthetic refusal inputs), the Preview tab was checked (bold section labels, inline code and the bash block render), and Publish was pressed. Confirmed from the releases API: two releases, v0.1.0 and v0.2.0, the latter marked latest. Zenodo then archived the release: version DOI 10.5281/zenodo.22906966 (record 22906966, publication date 2026-09-23 UTC) under the concept DOI 10.5281/zenodo.22867654, observed by the API watch. CITATION.cff and the README still carry only the v0.1.0 version DOI and wait for the owner's instruction (D-102); the site's timeline entry can now carry the publish time and commit b54c3b8, and its three DOI fields the new DOI. Recorded after a first attempt at this entry failed silently: the release API's JSON needed lenient parsing, the entry was not appended and the memory notes were committed with an empty time; corrected here and in the notes, in the same private commit, amended before anything left this machine.

**D-106 (2026-09-23) — v0.2.0 version DOI 10.5281/zenodo.22906966 (Zenodo record 22906966) set in the slots prepared for it (owner instruction): a third `identifiers` entry in `CITATION.cff`, the version-DOI sentence of the README, and the 0.2.0 changelog paragraph that had promised it. Concept DOI unchanged; the v0.1.0 version DOI kept, named in the README as the release the independent audit covered; `date-released` stays 2026-09-22, the date of the tagged content, matching the CHANGELOG heading (Zenodo's own 2026-09-23 stamp is the release's UTC publish date and needs no reconciling). The export was regenerated into `../gp-omm-conformance-public` and verified; commit and push are the owner's, the tag is not moved, and the site is untouched.**

**D-107 (2026-09-23) — Public commit de3453a "gp-omm-conformance 0.2.0: add Zenodo version DOI" pushed to `main` (owner's commands, run by the agent after the owner had staged the files in the app terminal and the panel closed; message and identity the owner's). Confirmed from the remote: `main` = de3453a, tag v0.2.0 unchanged at b54c3b8. The public repository now carries the v0.2.0 version DOI in CITATION.cff, the README and the changelog, and decisions D-103 to D-106.**

**D-108 (2026-09-23) — strf hardening note sent (owner instruction and authorisation): https://github.com/cbassa/strf/issues/88, "Range check for number_to_alpha5 above 339999", opened 2026-09-23 from the owner's GitHub session in the app's browser.** Body: the draft's text block verbatim, with its hard-wrapped paragraphs joined into single lines because GitHub renders newlines in issue text as line breaks; the status block, the suggested-title line and the word-count footer were left out as internal scaffolding. No labels, assignees or mentions. The reproduction reference resolves (docs/WRITERS.md on the public repository, HTTP 200). The draft's status line, the README's two mentions and docs/WRITERS.md now say "sent" with the issue link; the public copies still say "not sent" until the next export and push, which need their own authorisation. The 0.2.0 changelog paragraph keeps "drafted and not sent" as the state at that release.

**D-109 (2026-09-23) — Analyst-count correction: "564 of 566 analyst XML records" was a miscount; the count on record is 563 of 565, and the documentation now says so (owner instruction, 2026-09-23).** The figure entered with the Phase 3 cross-check text (commit 665341a, `docs/RESEARCH.md`), was copied into the README's library-behaviour list, and was repeated by the audit (AUDIT.md, "Library claims") without a recount. No fetch on record has 566 records: `fixtures/analyst-objects/raw/analyst.xml` (retrieved 2026-09-21T00:11:35Z, SHA-256 8c76a786…, byte-identical at the Phase 3 commit and at HEAD) holds 565 `<omm>` records with 563 empty `OBJECT_ID` elements, the same numbers as `docs/CROSSCHECK.md`'s per-file table, python-sgp4 issue #171 and the Libre Space post. Reconciled rather than dated, because there was never a second fetch to date it to. [inferred] The likely origin of 564 of 566 is a line count that included the CSV header: the fixture's recorded CRLF count is 566 for 565 records, and 566 minus the two filled OBJECT_IDs gives 564. README and RESEARCH.md changed to 563 of 565; AUDIT.md keeps its original line and gains an appended correction section, since the audit is a record. Same commit, same instruction, no version bump: the README's upstream section now names PR #172 beside #171 (opened 2026-09-21, amended 2026-09-23 after the maintainer's review) and is dated "as of 2026-09-23"; "tested locally against all sixteen cases" became "against the sixteen cases the corpus had at the time", the site's wording; the four strf mentions say "filed as cbassa/strf#88 on 2026-09-23" (the README's word for #171) instead of "sent". The 0.2.0 changelog paragraph is untouched, as D-108 decided. Completed on the owner's further instruction: the `LIBRARY_FINDINGS` string in `tools/make_manifest.py` corrected and `manifest.json`, `MANIFEST.md` and the case documents regenerated from it (only the analyst case's finding and the generation stamp change; the case document had contradicted itself, its coverage line saying 563 of 565 beside the finding's 564 of 566); the report string in `tools/crosscheck.py` corrected, and the one line of `docs/CROSSCHECK.md` hand-corrected rather than regenerated, because regenerating re-runs python-sgp4 and Skyfield against the raw files and restamps the whole report for a one-line text change; the report's generation stamp therefore still names the run that produced its numbers. The private `docs/writeup/post.md` keeps the old figure on purpose: it is the record of what was posted. AUDIT.md keeps its original line, with the appended correction. After this, the figure appears in the public export only in decision history and in the audit's original line and its correction. Public export regenerated for the owner to commit and push.

**D-110 — D-109 was wrong: "564 of 566 analyst XML records" was correct for the analyst case, and the documentation is restored to the case figure with the group figure beside it (owner instruction, 2026-09-23).** Evidence on disk, which D-109 did not consult: the analyst case's `expected.json` lists two XML sources, `fixtures/analyst-objects/raw/analyst.xml` (565 records, 563 with an empty `OBJECT_ID`) and `fixtures/analyst-objects/raw/analyst-270449-first.xml` (1 record, empty `OBJECT_ID`); `docs/CROSSCHECK.md`'s per-file table records them at line 30 (`analyst-270449-first.xml | 1 | 0 | 1 | 0 | 1 | 0`) and line 33 (`analyst.xml | 565 | 2 | 563 | 2 | 563 | 0`), so python-sgp4 raises on 564 of the case's 566 XML records, which is what the audit's "Library claims" line, the README's original sentence and the site's findings page said. "563 of 565" is the group file alone, correct in issue #171 and the forum post, wrong as the case figure. Checked for this entry: the two raw files' record and empty-designator counts (`<omm ` and `<OBJECT_ID></OBJECT_ID>` counts), the CROSSCHECK rows, and the case's source list. Retracted by reference: D-109's sentence "[inferred] The likely origin of 564 of 566 is a line count that included the CSV header" (the figure was a correct sum over two files, not a line count) and its claim that the audit "repeated it without recounting". Restored wording, in the README (line 275), `docs/RESEARCH.md` (the library paragraph), `docs/CROSSCHECK.md` (hand-corrected again, for the reason D-109 gave), the strings in `tools/crosscheck.py` and `tools/make_manifest.py`, and the regenerated `manifest.json`, `MANIFEST.md` and `fixtures/analyst-objects/case.md`: "564 of the 566 analyst XML records in the case (563 of the 565 group records, plus the single 270449 first record)"; inside an existing parenthesis (RESEARCH.md) the same words with a colon instead of nested brackets. AUDIT.md gains a second appended note retracting the first; both stay. The site's `findings.html:45` never changed and stays. Process lesson recorded in CLAUDE.md: a correction entry quotes the evidence it rests on and names what it checked. Public export regenerated for the owner to commit and push.

## 2026-09-23 (audit follow-up, Batch A: the corpus said pass where it should say fail)

**D-111 (2026-09-23) — Batch A is a patch release (0.2.1 when tagged), not a major one; and A1: the TLE epoch field rendered an exact midnight as `YYDDD.-8000000`, fixed with a regression test.** Versioning (owner asked for the agent's reading of the README rule): the rule makes "changed `expected.json` schema or check semantics" the major trigger, and check semantics means what a conforming parser must do. None of the five Batch A items changes that: a parser that parses every record, matches every id and reads unchanged input passes exactly as before; the items only stop crediting outcomes that were never meant to count as passes (zero records read, an unmatched id in `check-tle`, drifted stable input treated as live) and fix a renderer defect and a metadata error. No expected value, check text or schema changes; A4's tier correction touches provenance metadata, not a frozen value, so the freeze rule holds. The visible consequence, a run that passed vacuously and now fails, is what a fix looks like, and the changelog's `[Unreleased]` section lists the five under "Fixed". A1, the defect: `epoch_field` (`gpconf/tle.py`) built the fraction with `str(frac)[2:]`, and `Decimal(0).quantize(Decimal('0.00000001'))` prints as `0E-8`, so any exact-midnight epoch, including the carry from 23:59:59.999999 under half-up rounding, rendered as `-8000000`: a 69-character line with a valid checksum (the minus counts 1) that the reference reader rejects, and through `writer.round_trip` a correct writer was failed at every midnight epoch. Latent in the frozen data because no input lies within 4.32 ms of midnight, which is why the 304/304 render validation and the 1,208-record round trip never reached the branch. Fix: `format(frac, 'f')[2:]`. Tests (`tests/test_roundtrip.py`, `EpochFieldTests`, failing before the fix and passing after): exact midnight, one second after midnight (`.00001157`), the last microsecond of the day under half-up (carries to the next day) and truncation (`.99999999`), the year rollover, and a rendered midnight line read back by the reference reader. Note on inputs: the ISO epoch is parsed at microsecond resolution, so an eight-decimal second such as 23:59:59.99999999 is not an accepted input; the TLE field's 864 µs resolution makes microseconds sufficient. Checked for this entry: the four failing assertions before the fix, the five passing after, the reference run 17/17 exact, and the writer judge at a midnight epoch reporting no mismatch.

**D-112 (2026-09-23) — A2: `check-tle --against` fails a written record whose catalog number has no match in the source records; it used to pass with a note.** The audit's reproduction: `check-tle fixtures/analyst-objects/raw/analyst-81011-first.tle --against fixtures/six-digit-omm-saramago/raw/saramago-first.csv` printed `[pass]`, the note "no source record with id 81011 in the --against file; round trip not checked", and exited 0. On the satno2tle path that is the likely failure mode itself: `auto.sh` passes the catalog field's text to `rffit -i`, an Alpha-5 field becomes 0 through `atoi`, rffit prints "No elements found" and continues with its zero-initialised orbit (rffit.c line 40; D-097, reading complete, not run), and the written line carries `00000`; a user who then checked the file against the source records got a pass for a TLE about no object. Now (`gpconf/writer.py`, `check_record`): an unmatched id is a problem, so the record fails and the command exits 1; the detail says "no source record with id N in the --against file; the round trip cannot be checked", and for N = 0 adds "likely cause: 00000 is what rffit writes when its -i lookup found no elements and the orbit stayed zero-initialised, e.g. an Alpha-5 field passed through satno2tle", for N = 99999 "likely cause: 99999 is rffit's default catalog number". Both causes are stated as likely, since the rffit behaviour is established by reading its source, not by a run. Tests (`tests/test_writer.py`, `CheckTleTests`): a file of four rendered records (100000, 123456, 00000, 99999) checked against a source file holding only 100000 exits 1 with the matched record passing, the other three failing, the rffit cause named only for 0 and 99999; a file of matched records only still exits 0; the older test that asserted the note-and-pass behaviour now asserts the failure. Both were failing before the change (one error, one assertion) and pass after; the full suite passes (43 tests). `docs/WRITERS.md` says so. Checked for this entry: the reproduction's exit code measured directly (1 after the change) and the reference run, unchanged at 17 of 17.

**D-113 (2026-09-23) — A3: a source file the reference reader reads zero records from, where the manifest records some, now fails the case with a description of the file; it used to pass every per-file check on zero records.** The audit reproduced it on a scratch copy: `iss.tle` replaced by an HTML 403 body, a TLE cut off after line 1, an empty KVN variant and a BOM-prefixed one all left the case at `pass`, with `tle-checksums-valid` reporting "0 record(s), all checksums valid" and the derived-KVN checks "variant parsed", because the values item is only emitted when an oracle exists and the remaining checks count zero records as success. That is the state `tools/fetch.py` leaves behind after an unexpected HTTP response, so a user who hit a rate-limit page got a green run. Rule now (`gpconf/runner.py`, `load_source`, before the parser under test is called): if the source's recorded `record_count` is positive and the reference reader returns no records, the item `source-readable` fails with "reference reader found 0 of N expected records; the file is <size> bytes, starts with <first 48 bytes>; looks like: <guess>", the guess being any of: empty file, whitespace only, UTF-8 BOM prefix, HTML document, CelesTrak's empty-response text, a TLE line 1 without a line 2, CSV header only (`describe_bytes`); the source's state becomes `unreadable`, which every per-kind loop already treats like a parse error, so no vacuous check runs for that file. A second rule covers the reader itself: when the file's hash matches the tested snapshot and the reader's count differs from the recorded one, the same item fails as an internal inconsistency. Live-mode files (hash differs) with a non-zero count are not count-checked, since rolling windows change their record counts legitimately. Distinguished on purpose: the 16-byte "No GP data found" bodies of the 404 sources carry `record_count` 0 and `http_status` 404 and keep their `empty-404` state; a positive `record_count` is the trigger, never the count zero. Tests (`tests/test_source_reading.py`, failing before and passing after): the four reproductions through `load_source`, each asserting the state, the item, the size, the quoted first bytes and the guess words; the 404 body staying `empty-404` with no failure; a readable file staying `ok` and a live file with a different count staying `ok`; a snapshot file with a wrong recorded count failing as internal; and, through `Runner.run` on a scratch root holding the shipped `kvn-syntax-variants` case with its first variant emptied, the case status `fail` with exactly one `source-readable` item, no "variant parsed" item for that file and the other variants still passing. Checked for this entry: the full suite (48), the reference run 17 of 17 exact, and the naive and python-sgp4 runs unchanged at 14 and 7 failing cases, so no legitimately passing file changed status. The README describes the item where it explains the runner's statuses.

**D-114 (2026-09-23) — A4: the writer case's source files carry the tier the case that owns each file recorded, not a blanket "stable"; the two rolling group files are live again, and `tools/fetch.py --check-drift` counts 22 stable sources, not 24.** `build_writer` (`tools/make_expected.py`) listed every file its inputs came from with `source_entry(path, "stable", ...)`, because the inputs themselves are frozen records; but the files include `fixtures/analyst-objects/raw/analyst.csv` and `fixtures/tle-omits-six-digit-objects/raw/last-30-days.csv`, the group snapshots the derived Alpha-5 lines were rendered from, which their own cases record as live. `manifest_sources` in `tools/fetch.py` takes "stable" for a path if any case says so, so those two rolling queries became stable-tier in the manifest: the audit's `--check-drift` counted 24 stable sources where the corpus has 22, and the first user to run the fetch tool after those groups update (every two hours) would have been told two stable sources drifted. The "stable if any case says so" rule stays: it is right when no case mislabels a file, and the fix is at the label. Now `originating_tier(path, from_case)` returns the tier recorded for the path by the case the input came from, or, when that case does not list the file (a derived line's canonical source belongs to another case), by the case that owns the fixture folder; a path no case records is labelled live and reported as a PROBLEM. The frozen input records keep tier stable, as before: the records are frozen values, the files are not. Regenerated: `fixtures/tle-writer-alpha5/expected.json` (the two tiers and the generation stamp change, nothing else), `manifest.json`, `MANIFEST.md` and the case document. Tests: `ExpectedInputsTests.test_sources_keep_their_originating_tier` checks every writer source against the tiers the other cases record for the same path, excluding the case-level label "mixed", and pins the two group files as live and the records as stable; `RealManifestTiers` in `tests/test_fetch_drift.py` checks `manifest_sources` on the real manifest: the two files live, and 22 stable raw fixture files, the count the tracker site checks. Both fail against the pre-fix data (a `stable != live` assertion; 36 stable paths counted before the fixture-only restriction, since the shipped derived and vector files are stable too) and pass after. Checked for this entry: `--check-drift` on disk (61 files match, 22 stable, 39 live, no drift), the full suite (50), the reference run 17 of 17 exact, and the expected.json diff limited to the two tiers and the stamp.


**D-115 (2026-09-23) — A5: a stable-tier source whose bytes differ from the recorded SHA-256 is reported as drift, explicitly: a failing `stable-source-drift` item in the case output, a `drift` field in the JSON report, and a values item that says the frozen expected values were not applied and why.** Before, `load_source` set the mode to "live" for any hash mismatch regardless of tier, the reference reader silently became the oracle, and the only trace was an `info` item shown with `--verbose`; the audit reproduced it by changing one digit of a stable file and reading `[pass] values` in the table. The tier promise is that a stable source is frozen (README, Versioning), so changed bytes mean either that the provider altered a first-ever record, which the maintainer must learn, or that the user's file is corrupt; either way the case as published could not be executed, and a green table would hide it. Status `fail` for the drift item, deliberately: the case's status then says the run could not deliver what the case promises, while the parser's own verdict stays visible in the values item, which is still computed against the reference reader and labelled "frozen expected values not applied: the stable source drifted from the tested snapshot". Live-tier sources are unchanged: rolling windows are expected to differ and keep the "live bytes" label. The run footer lists every drifted file and points at `tools/fetch.py --check-drift`; `CaseResult.as_dict()` carries `drift` as path -> recorded and actual SHA-256, so the JSON report and `docs/FAILURES.md`, which is generated from it, would show a drift if one ever occurred. Tests (`tests/test_stable_drift.py`, on a scratch root holding the shipped `alpha5-tle-derived` case, whose four sources are stable-tier): an untouched copy passes with all four values items "match frozen expected values" and an empty `drift`; a copy with one mean-motion digit changed and the checksum recomputed fails with exactly one failing item, `stable-source-drift` on that file, its detail naming the tier, the mismatch, both hashes and the reference comparison, a `drift` entry with a 64-character actual hash different from the recorded one, that file's values item passing (the reference adapter agrees with the reference reader) with the not-applied label, and the other three files still exact against the frozen values. Both tests fail before the change (a missing `drift` key; status pass) and pass after. Checked for this entry: the full suite (52), the reference run 17 of 17 exact on the untouched corpus, and the on-disk drift check (0 stable drift), so no item appears on a clean checkout.

**D-116 (2026-09-23) — Batch C, corpus: a stale count, two footnotes and the dating of entries.** The ambiguity `mmdot-convention` in `tools/cases.py` said "(301/301 records)", the Phase 2 count, while the case and the README count 304 of 304 pairs; it now says 304/304, and `MANIFEST.md` and the five case documents that copy the text were regenerated. `MANIFEST.md`'s summary table now carries a footnote that its records column sums the record counts of a case's source files, since `analyst-objects` shows 567 (565 group records plus the two single-object first fetches) against the 565 the text elsewhere quotes. The 0.2.0 changelog entry keeps "drafted and not sent" as the state at release and gains a bracketed note that the strf note was filed after the release as cbassa/strf#88 (D-108), so the two statements no longer read as a contradiction. Dating: entries D-100 to D-109 carried no date and D-104 to D-110 sat under a 2026-09-22 heading although they record events of the 23rd (UTC dates of the commits that introduced them: D-100 to D-103 on 2026-09-22, D-104 onward on 2026-09-23); each now carries its date after its number in the form D-111 introduced, a "2026-09-23" heading precedes D-104, and the preamble says every entry from D-100 on carries its date. The corpus handoff, whose filename carries the date it was first written, says at the top that it is the living handoff. No expected value, check text or schema changes. Checked for this entry: the regenerated manifest and case documents contain no "301/301"; the full suite; the export verified from a fresh copy.

## 2026-09-23 (audit follow-up, Batch D: runner and reader robustness)

**D-117 (2026-09-23) — D1: the reference readers reject malformed input with a clear error instead of returning a wrong record; `from_alpha5` follows Space-Track's definition strictly.** Audit item 10.9 found the readers silent on five inputs: a UTF-8 BOM (the CSV lost its first column name, the KVN read zero messages, a 2LE lost its first record), an XML document with a default namespace (one record with every field None, because the `find` paths are not namespace-qualified), a repeated keyword in one KVN message (last value won), a TLE epoch day of 000 or 367 (landed in the previous or next year), and `from_alpha5` accepting `'A-001'`, `'A 000'` and `'+1234'` through `strip` and `int`. Each is now a `ValueError` naming the problem: `read_file` refuses a file that starts with a BOM, since the provider formats carry none; `read_xml_text` refuses a root element in a namespace, since the corpus reads the unqualified NDM/XML form only; `read_kvn_text` refuses a keyword repeated within a message; `tle_epoch_to_iso` requires the day of year to lie in 001..365, or 366 in a leap year; `from_alpha5` requires exactly five characters, a digit or an uppercase letter other than I and O first and four digits after, which is Space-Track's definition and what `writer.strict_catalog_field` and the reference adapter's hook already enforced around it. In the runner, `load_source` reports a `ValueError` from the reader as "reference reader rejected the file: …" rather than as an internal error; other exceptions keep the internal label. Checked before writing the fix: a scan of all 84 raw and derived files found none with a BOM, none with an XML default namespace, no repeated KVN keyword, no TLE day outside its year, and every catalog field already in the strict form, so no passing case changes; and the adapter counts before (naive 14 failing cases with 76 items, python-sgp4 7 with 20) equal the counts after, because the naive adapter uses `int()` and the python-sgp4 adapter uses that library's own decoder. Tests: `tests/test_malformed_input.py` pins each of the five behaviours, failing before the change (fifteen sub-failures) and passing after; the A3 test for a BOM-prefixed KVN file now expects the reader's rejection, the clearer of the two errors. Suite and reference run checked with exit codes.

**D-118 (2026-09-23) — D2, runner completeness: the three listed-but-unevaluated checks are evaluated; `--vectors-cmd` reaches `parse_catalog_id`; a raising hook fails its vector, not the case; `norm_epoch` accepts UTC offsets and a leap second; the JSON report names a `--write-cmd` parser; the failure catalogue withholds SupGP values for every value-carrying check; `ndot_field` rounds half up.** 10.8, implemented rather than removed: `leading-dot-decimals`, `bstar-implied-decimal-exponent` and `negative-bstar-and-ndot` were in the manifest and three case documents but emitted no item, so a reader saw them "pass" by omission; the case documents present them as distinct checks and each is a few lines, so `Runner.check_field_forms` now compares the fields each check names against the same oracle the values item uses (frozen values in snapshot mode, the reference reader otherwise): the OMM keywords written without a leading zero in the file's own text (found by `leading_dot_keywords`: `.00048259`, `-.15975118E-3`), the BSTAR and second-derivative fields of every TLE record, and the BSTAR and first-derivative fields of the records whose oracle value is negative. A file without the feature emits nothing, and a case with none at all reports `not-exercised`, like the nine-digit check. A sub-tolerance agreement counts as a pass with the count in the detail; the values item already carries the tolerance report. Counts before and after: naive 14 failing cases with 76 items, python-sgp4 7 with 20, unchanged, because both adapters' values already agreed on those fields where the new items apply and the naive failures in `bstar-and-derivative-forms` are parse failures on a different file. 10.11: `parse_catalog_id` joins the four ops the external command protocol dispatches, so `--vectors-cmd` parsers get the nine-digit text vectors instead of a skip; a command answer without `result` or `error`, or that is not JSON, is a `ValueError` naming it rather than a `KeyError`. 10.10: the `two_digit_year` loop calls the hook once per vector inside a try, so a raising hook fails that vector with the exception's name and the other items are still produced; before, the whole vectors case ended as "internal error in runner". 10.12: `norm_epoch` accepts a `[+-]HH:MM` offset (converted to UTC) and `:60` (mapped to `:59`, as `tests/adapters/reference.py` already does in its own epoch parsing), so a parser returning an aware `isoformat()` or the CCSDS leap-second form is no longer reported as "parser raised". 10.15: the JSON report's `parser` names the `--write-cmd` command; `tools/make_failures.py` gains `render(reps, generated_at)` for testing and withholds failing details for the checks that embed element values (`values`, `omm-formats-agree`, `tle-values-match-omm-within-tle-precision`, `mmdot-is-tle-field-value`, `tle-writer-round-trip` and the three above) on SupGP-sourced files, while a structural detail such as a parse exception stays readable; `ndot_field` quantises half up like every other rendered field (`0.000000125` → `.00000013`, was `.00000012` under the Decimal context's half-even), scanned across every record the renderer touches, the 1,208 records of the round-trip test (604 from the CelesTrak TLE/2LE files on disk, 604 derived Alpha-5 lines) and the 609 records of the writer case: no MEAN_MOTION_DOT value carries more than eight decimals (1,651 have exactly eight, 148 seven, 14 six, 4 none), so there are 0 exact ties at the eighth decimal and 0 lines render differently under half-up than under half-even. The change is unobservable in current inputs. It rests on the rendering rule the corpus established empirically for the BSTAR and second-derivative mantissas (D-028, D-071: 304 CelesTrak record pairs) and on consistency with every other rendered field, which already quantises half up; whether the provider itself rounds a tie half up is not backed by a provider document and is the coverage gap `tle-vs-omm-precision-loss` and the writer case already record ("no rounding tie … was observed") [untested]. CLAUDE.md gains the zsh note beside the exit-code rule. Tests: `tests/test_runner_completeness.py`, nine tests failing before and passing after, including a general one that every check a case lists appears among its items for the reference adapter; the listed-checks tests skip in the public clone, where the raw fixtures are absent. Suite and the three adapter runs checked with exit codes.

**D-119 (2026-09-23) — D3, check-tle and writer robustness: check-tle reports unpaired, indented and BOM-prefixed element lines instead of dropping them; a day-of-year source epoch round-trips; every written record is checked for column layout; the epoch-string vectors compare the instant; `tools/fetch.py` never re-requests a file that lacks its metadata, requests a re-capture only after its original and never a URL twice in a run, judges the two-hour rule by `retrieved_at`, and exits 2 on stable drift; negative tests for wrong values, tolerance boundaries and the command protocol.** 10.6: `extract_records` paired only `1 `/`2 ` lines that began at column 1, so an element line without its partner, an indented pair and a byte-order mark vanished and the file exited 0, or 1 as "nothing to check" without saying why. `writer.scan_lines` now returns the records (with the indentation and the BOM as problems on the record, the lines checked with the indentation removed) and the strays; `check_file` emits one failing entry per stray (`unpaired: true`) and the CLI prints and counts them. 10.7: `writer._iso` normalised nothing, so a `--against` record whose EPOCH was the day-of-year form (legal in CCSDS 7.5.10; the corpus's own KVN case declares it) raised an uncaught `ValueError` out of `tle.epoch_field`. The normaliser `tle.iso_epoch` (calendar and day-of-year forms, any fraction length quantised to microseconds, an optional Z, a `+HH:MM`/`-HH:MM` offset, second 60) now serves `epoch_field`, `runner.norm_epoch` (which delegates to it; D-118's inline code is replaced, same behaviour, same tests) and `writer._iso`; the round trip in `check_record` is wrapped so a source value the corpus cannot read is a problem on the record, not a crash. It lives in `gpconf/tle.py` because the reader imports the renderer and the runner imports both. 10.4: `writer.check_layout` pins every field of both lines to its columns (the decimal points at columns 24 and 35 of line 1 and 12, 21, 38, 47 and 55 of line 2, the separators, digits-only and right-justified fields); it runs in `check_record` and as the new writer item `tle-writer-column-layout`, added to `CHECKS` and to the case (expected.json, case.md, manifest and MANIFEST.md regenerated offline). The gap was real: the test builds a line 2 with a left-justified inclination and a recomputed checksum, and `check_lines`, `check_catalog_field` and `round_trip` all pass on it. Every provider and derived record the round-trip test renders (1,208) and python-sgp4's 606 exports have the standard layout. 10.5: `vectors/ccsds-epoch-strings.json` carries `iso`, the instant each valid string denotes (values computed by the reference adapter and checked against the calendar by hand: day 64 of 2020 is 4 March, day 204 of 2002 is 23 July), plus `iso_alternatives` for the leap second (23:59:59 or the next midnight, since a datetime cannot carry second 60); the runner compares the hook's result through `norm_epoch` within the 2 µs tolerance, so a hook returning a constant fails and names the vector. `ccsds-epoch-strings` joins `CHECKS` and the vectors case's list: the runner had emitted it since v0.1.0 but no manifest or case document described it. Counts before and after, over all cases: naive 14 failing cases with 76 items → 14 with 76; python-sgp4 7 with 20 → 7 with 20; reference 17 exact → 17 exact. The layout item passes for all three writers (naive's six-digit lines are 70 characters and fail the length check before any layout check; its three five-digit records are aligned); the epoch item is unchanged for naive (it already failed, rejecting five valid forms) and python-sgp4's adapter exposes no `parse_epoch`, so it still skips. 10.14: `fetch.py` gains `plan()` (what a run would do per entry, without the network), `run()` (the command line, parameterised by root, entries, clock and fetch function; `main` is `sys.exit(run())`), `duplicate_urls`, `read_meta` and `age_seconds`. A raw file on disk counts as fetched whether or not its metadata exists (before, a missing `.meta.json` caused a fresh request); `--force` reads the recorded `retrieved_at` and falls back to mtime only without metadata (a checkout or a sync rewrites mtimes); a re-capture entry is requested only with `--include-recaptures`, when its original is on disk, was retrieved at least two hours earlier and is not itself requested in the run (before, a fresh checkout with `--include-recaptures` fetched the same endpoint twice two seconds apart); no URL is requested twice in a run; the exit code is 2 when a stable-tier source drifted. Thirteen tests drive `plan` and `run` with a stub in place of `fetch_one` and a temporary root; no request is made. README and the script's docstring updated. 10.16: `tests/test_negative.py` shows a changed mean motion, catalog number and epoch are detected and named (an epoch off by 3 µs fails, by 2 µs passes within tolerance), pins the boundaries (2 µs inclusive, 432 µs for TLE epochs, 1e-12 relative for float-derived values, zero requires zero, exact decimals get no tolerance) and drives `CommandParser.parse` through `{fmt}` substitution, exit 3, a non-zero exit (the exit code and stderr are named), a JSON object instead of an array and output that is not JSON (now a `RuntimeError` naming it; before, a bare `JSONDecodeError`). The audit's last 10.16 sub-item, the naive must-fail test that passed vacuously in the public clone for the three cases whose CelesTrak files are absent (`six-digit-omm-saramago`, `analyst-objects`, `supgp-celestrak-classification-c`): the test is split. The four cases that ship with the repository are asserted unconditionally; for the three fetched-source cases the test asserts the failure where the files are present, asserts that a case without its files consists of `source-present` skips only (never a pass), and then reports itself skipped with the reason and the list of cases, so a reader of a public-clone run sees why. The alternative, driving the three cases from the frozen records, was not taken: the naive parser's failures there are parse failures on the provider's own bytes (six-digit ids, an empty OBJECT_ID, classification C), so rendering the frozen records back into CSV, JSON, XML, KVN and TLE would test the corpus's rendering rather than the provider's files, and the SupGP case's values are withheld from the export (D-049), so it could not be reconstructed at all; the decision policy prefers omitting to fabricating. Found while verifying the export as a fresh tree: a case none of whose files is on disk reported "not exercised" rather than "skip" when it listed a check with a case-level fallback item (`nine-digit-ids-parse` since v0.1.0 for `nine-digit-supgp-launch-nominals`; the three field-form checks of D-118 for `epoch-year-19xx`, `baseline-iss-five-formats` and `bstar-and-derivative-forms`), and the item's detail claimed the fetched data lacked the feature when nothing had been fetched. The fallbacks now fire only after at least one source was read; `tests/test_runner.py` `SourcelessCases` runs seven cases from a root holding only `manifest.json` and their `expected.json` and asserts every one is skipped with `source-present` items only (failing before the guard: `'not-exercised' != 'skip'`). In this checkout, where every source is present, the adapter counts do not change. Tests: `tests/test_check_tle_robustness.py` (16), `tests/test_fetch_requests.py` (13), `tests/test_negative.py` (19), all failing before the changes except where they pin existing behaviour; suite 114 under both Pythons, the three adapter runs and the catalogue regeneration checked with exit codes.

**D-120 (2026-09-23) — Release 0.2.1 prepared (owner instruction), not committed, tagged or pushed: version constants bumped from 0.2.0 to 0.2.1 (`tools/make_manifest.py`, `pyproject.toml`, `gpconf/__init__.py`, README, `CITATION.cff`), the changelog heading set to 0.2.1 with today's date, manifest and failure catalogue regenerated from fresh runs of the three adapters, export regenerated into a scratch tree and verified there with exit codes checked (suite under both Pythons, reference run).** Same sequence as D-102. The audit statement in the README status paragraph, the README's audit bullet, the changelog and `CITATION.cff` says the independent audit covered v0.1.0 and that neither the writer-side case of v0.2.0 nor the Batch A–D fixes of v0.2.1 were separately audited; the audit of 2026-09-23 that produced the fixes is named as a read-only pass by a separate AI session under the maintainer's direction, not an independent audit. The version DOI slot is left as at D-102, a comment in `CITATION.cff` and a sentence in the README and the changelog; Zenodo mints it at the release, and D-106's sequence then fills it. Classification: D-111 decided patch for Batch A; Batch D added one check to the writer case and documented one long-evaluated check, which makes the corpus stricter without changing a frozen value or the schema, and the owner directed 0.2.1; the changelog paragraph names the added and the documented check rather than claiming no check text changed. `date-released` is today, the date of the tagged content. GitHub release notes drafted from the changelog in the shape of v0.2.0's (kept outside the repository, as v0.2.0's were; the published text is the record). The public site still describes v0.2.0 and is updated after the tag, as for 0.2.0.

**D-121 (2026-09-23) — GitHub release v0.2.1 published at the owner's request ("Publish it."), from the app's built-in browser in the owner's own GitHub session: title "v0.2.1 — audit follow-up fixes", existing tag v0.2.1 at b1407a5, label Latest, not a pre-release, published 2026-09-23T21:09:22Z.** The body is `docs/handoff/release-notes-v0.2.1-draft.md` from its third line on (the first line is the title itself and would have rendered as a stray line; the owner saw this before publishing), with the five "How to run" commands wrapped in a `bash` fence at the owner's request so they render as one code block; the SHA-256 of the form's body matched the file's before publishing, so the published text and the private draft are identical. The draft's lead names eight decision entries (D-111 to D-115, D-117 to D-119); the owner's proposed "eleven fixes across five decision entries" could not be reconciled with the changelog and was flagged before the owner chose to publish the eight. The form was filled and the Preview checked before the owner said the word; the pane was hidden, so the fill, the tab switches and the Publish click were script-driven and each state was read back from the DOM. Zenodo's version DOI for v0.2.1 is pending at the time of this entry (the concept record 22867654 lists v0.1.0 and v0.2.0 only); when it appears the D-106 sequence fills the three marked slots, and the export and push wait for the owner's word.

**D-122 (2026-09-23) — Batch E1, corpus documentation read as a stranger (audit section 5, every item).** README: the quick-start comment on the control run says the nine-digit case reports not-exercised outside a launch window; "18 SDS" is expanded to the 18th Space Defense Squadron at first use (README, the nine-digit case title, the SupGP interpretation); a third tier bullet defines `mixed` (the pairs, facts and XML-schema cases list files fetched for other cases, use them structurally, and drift is judged by the fetching case's tier, D-114) and says vector and derived files are never fetched; the adapter section counts five vector hooks and the example gains `parse_catalog_id`. Case documents (generated by `tools/make_manifest.py`): every case with `raw/` sources opens its Sources section with one sentence saying `tools/fetch.py` creates those files and the runner skips until then, and a case whose files all ship says so; the source table prints a real status and time for every row: an expected 404 names its body ("No GP data found", "No SupGP data found"), a derived file says it is shipped and when it was generated (from its provenance sidecar), a vector file says it is shipped and never fetched; a re-capture row carries a note naming its original and D-022 and stating that the `FORMAT` value is spelled in lower case (the reason for the spelling is not recorded; the entries are distinct through `recapture_of`, not through the spelling, since the duplicate guard is case-insensitive). The nine-digit case's gap text no longer quotes a status the runner never emits; it says `nine-digit-ids-parse` reports `not-exercised`. "hand-recorded" in the six-digit-TLE case is explained (a tool defect at 00:10Z discarded the body, reconstructed by hand from the tool's log, D-013; clean re-capture at 00:41Z, D-022). The vectors case's title and coverage now name all four vector sets. The check text of `catalog-number-is-integer` says a ten-digit value is rejected, which the vector file already tested. `docs/FAILURES.md` (generated by `tools/make_failures.py`): each case entry opens with its failing-item count and says how many items were collapsed into a shown bullet, and a collapsed bullet names the files it stands for, so the table's count and the bullets reconcile; a detail cut at 600 characters ends in an ellipsis and the reading guide says the JSON report holds the full text; a withheld SupGP detail says how to see it (run the case on your own fetch with `--json`). `docs/WRITERS.md`: the two exit-code conventions (the writer command's per-record codes; `gpconf`'s own 0/1) are stated as different, and the 604/603/606 sentence carries the "270449 appears twice" explanation. Regenerated: every `expected.json` (only titles, coverage text, check descriptions, ambiguity text and generation times changed; a script compared every other field with the committed files; the ambiguity change is four cases whose `mmdot-convention` text still read "301/301" because D-116 changed the wording in `tools/cases.py` without regenerating them, and they now read "304/304"), `manifest.json`, `MANIFEST.md`, every `case.md`, `docs/FAILURES.md` from fresh adapter runs. Adapter counts unchanged: reference 17 exact, naive 14 failing cases with 76 items, python-sgp4 7 with 20.

**D-123 (2026-09-23) — v0.2.1 version DOI 10.5281/zenodo.22926017 (Zenodo record 22926017, created 2026-09-23T21:48:05Z) set in the slots prepared for it (owner instruction): a fourth `identifiers` entry in `CITATION.cff` in place of the comment line, the version-DOI sentence of the README, and the 0.2.1 changelog paragraph that had promised it.** Same sequence as D-106. Concept DOI unchanged; the v0.2.0 and v0.1.0 version DOIs kept, the latter still named as the release the independent audit covered; `date-released` stays 2026-09-23. The record appeared 39 minutes after the release: the webhook's `release.published` delivery had been accepted with 202 at 21:09:23 UTC (the two other deliveries got the duplicate 409 every release gets), Zenodo's settings page showed the release as "Received" under a banner about slowness and intermittent outages from bot and AI-crawler traffic, and the record was created at 21:48:05 UTC without any redelivery (D-121, D-122 follow-up). The export was regenerated into a scratch tree, verified there with exit codes checked (suite under both Pythons, reference run) and synced into `../gp-omm-conformance-public`; it carries Batch E1 (D-122) as well. Commit and push are the owner's; the tag v0.2.1 is not moved (it points at the release content, and the DOI, like v0.2.0's, lives on `main` after the tag); the site gets the same DOI under its own entry.

**D-124 (2026-09-23) — Batch E2, standards slips from audit section 4, each quotation checked against its source.** Sources opened: CCSDS 502.0-B-3 (April 2023) as the PDF at https://ccsds.org/Pubs/502x0b3e1.pdf, text extracted with pdftotext and the pages of Table 4-3 (page 4-6) and annex G (page G-6) looked at as images; the vendored schema files under `schemas/ndmxml-4.0.0/` and `schemas/SOURCE.md`; CelesTrak's GP data formats page read live on 2026-09-24; one HEAD request to the SANA registry. Changes in `docs/RESEARCH.md`: the 4.2.1 quotation carries its two parentheticals again, "(data about data)" and "(explanatory information)" (4.2.1); the 7.3.2 quotation is complete, ending "and spaces (excluding line termination character[s])." (7.3.2); the units rule cites 7.7.1.1 and quotes its four fragments (7.7.1.1 a and b); the SANA schema note quotes each module's header comment separately (master: "This is version 4.0 of the NDM/XML Schema (05/19/2023)." and "This is version 4.0, Rev 0 of the NDM/XML Schema (11/06/2022."; OMM: "This is version 4.0.0 of the NDM/XML Schema (05/19/2023)."; common: "This is version 4.0.0 of the NDM/XML Schema (05/2023)"), the old blended quotation having been the OMM module's line presented as the set's; the companion-schema sentence states what `schemas/SOURCE.md` records (the twelve 2.0.0 files fetched 2026-09-21 with hashes) and that the 3.0.0 master schema named by Figure G-10 resolves (HEAD 2026-09-24: 200, 4,016 bytes, Last-Modified 2023-05-19). Two footnotes: CelesTrak's page still carries "Once the system sends more than 1,000 HTTP 403 errors (and now 301 and 404 errors, too) to an IP address in a day (yes, that happens almost every day), that IP address is put into the firewall and requires manual review to find and remove it." beside the 50-in-2-hours rule the corpus cites; and annex G's note "All of these examples are based on the TLE shown in figure G-6" sits over a TLE with epoch 07064.44075725 while the OMM figures carry 2020-064T10:34:41.4264, so a rendering that reads 20064.44075725 shows the standard's inconsistency. `tools/cases.py`: the check `optional-tle-parameters-may-be-absent` quotes Table 4-3's block heading "(This section is only required if MEAN_ELEMENT_THEORY=SGP/SGP4)" and the NORAD_CAT_ID row's "This keyword is only required if MEAN_ELEMENT_THEORY=SGP/SGP4." and says the standard is ambiguous for SGP/SGP4 messages; the KVN variants case, the manifest and the case documents regenerated. Not changed, contrary to the audit and the instruction that followed it: the GM row. Table 4-3 (page 4-6, seen) marks GM "O"; the "C" is the OPM's Table 3-3 row (page 3-x, extracted text line "GM … km**3/s**2 C" under "Osculating Keplerian Elements"). The row keeps "O" and now says which table says what. Adapter counts unchanged (reference 17 exact, naive 14/76, python-sgp4 7/20); the suite passes.

**D-125 (2026-09-24) — Batch F (audit item 6): one synthetic-derived writer input with a positive-exponent BSTAR, under the D-096 precedent; letters B–S and U–Z stay vectors-only; the nine-digit re-capture is a standing item.** No fetched record carries a BSTAR with a positive exponent (the audit counted 1,206 TLE lines: 1,192 negative exponents, 14 `+0`, none ≥ 1.0), so the form `±NNNNN+E` with E ≥ 1 was untested. The writer case gains one input: SARAMAGO's first-record elements with the catalog number replaced by 99999 and BSTAR by 1.2345, whose TLE field is ` 12345+1`. Both replacements are specification vectors: 99999 is in `vectors/alpha5.json` `below_100000`, is the largest five-digit number, and primary sources describe no catalogued object in 90000–99999 (README; rffit uses it as its placeholder), so the record is not a catalogued object's element set and every writer, the naive one included, renders a full 69-character line for it; 1.2345 is the smallest-digits value of the positive-exponent form. The expected TLE fields are rendered by the corpus (`reference_rendering: corpus-rendered`), the record carries `synthetic_changes` naming both replacements with their sources, and `case_specific.inputs.synthetic_field_vectors` counts it. Checked before adding: the corpus renders ` 12345+1` and reads it back as 1.2345; python-sgp4 2.27's `export_tle` and the naive writer render the same field, so no adapter count moves: reference 17 exact, naive 14 failing cases with 76 items, python-sgp4 7 with 20 (the naive and python-sgp4 writer-case details now say 607 and 4 of 607 where they said 606 and 3 of 606; the catalogue is regenerated). Letters B–S and U–Z stay vectors-only: no real object carries them (the catalog stood at 100789 in July and T is the analyst block), synthetic lines with re-mapped ids would test the corpus's encoder against itself and add nothing `vectors/alpha5.json` does not already prove for every letter, and D-096 admitted synthetic ids only where a refusal is the correct output; the audit's estimate that the B range is years away is [inferred]. Standing item: a second capture of nine-digit SupGP ids at the next launch window, one request through `tools/fetch.py`'s re-capture path, to show the pattern recurs; the site tracker watches presence daily. Real data over synthetic remains the rule; this vector is the one exception the coverage gap justified, and the reader side and the second derivative stay declared as uncovered.

**D-126 (2026-09-24) — The AI-assistance disclosure is removed from the corpus's presentation surfaces (owner instruction); `AUDIT.md`'s limitation sentence is deliberately retained.** README, "How this corpus was built": the framing paragraph now describes the method (phased work, the owner's review at each phase end, the written decision policy in `CLAUDE.md` requiring every non-trivial choice, reversal and correction to be recorded) without naming AI assistance; the audit-trail bullet no longer attributes decisions to "the owner's and the agent's"; the sources bullet drops the sentence about the model's memory of the domain. The independent-audit bullet is kept whole, including its scope qualifier ("performed by a separate AI session … it was not a human review") and its v0.1.0 scope: the owner's rule was to keep the qualifier or drop the audit claim, and the claim was kept because it is the corpus's main verification statement and the qualifier is what makes it honest; dropping the claim would have removed a fact readers rely on, keeping it without the qualifier would have overstated it. CHANGELOG 0.2.1: "a read-only pass by a separate AI session under the maintainer's direction, not the independent audit" becomes "a read-only review pass under the maintainer's direction, distinct from the independent audit", which keeps the distinction D-120 wanted. The kept release-notes draft (private) loses the same phrase; the published GitHub release text for v0.2.1 still carries it and is the owner's to edit. `AUDIT.md` is unchanged: its statement that the audit was performed by a separate AI session and was not a human review stays, because a reader of the audit must know what kind of review it was. Left unchanged and flagged to the owner: `CITATION.cff`'s abstract ("Built with AI assistance under human direction …"), which GitHub's "Cite this repository" and Zenodo's minted records reproduce (the records are immutable per version); `docs/PLAN.md` (exported) and `docs/writeup/post.md`, which describe the assistance historically; `CLAUDE.md` itself, exported and named after the agent; `DECISIONS.md` and the private handoff, which are records, not presentation. The export was regenerated and synced; commit and push are the owner's.

**D-127 (2026-09-24) — Batch G Phase 1: the library survey is compiled and kept privately at `docs/handoff/batch-g-survey-2026-09-24.md`, outside the export allowlist; Phase 2 waits for the owner's review.** Four read-only registry surveys (PyPI, CRAN and CPAN; npm and Packagist; crates.io, Maven Central, pkg.go.dev and Julia General; F-Droid, GitHub topics, tracker apps and reference code), run by research agents on 2026-09-23 and 2026-09-24 under the standing rules (no issue, PR, comment or post; no request to celestrak.org or space-track.org; no installs; a URL for every fact; VERIFIED-in-source or INFERRED on each claim), are compiled into one document: the audit's 22 names reconciled against their rows (all 22 re-found; Stellarium's released 26.2 is inferred to predate the fix on master; libsgp4 has issue #44 open beside PR #42), a candidate index of 215 table rows classed by catalog-number behaviour (A decodes Alpha-5; B keeps the raw string; C fails on an integer parse; D returns a silently wrong identity; E overflows on write; F is OMM-only with an integer of any width; G is a fetch client; H delegates), a 44-entry cross-ecosystem ranking by reach and likelihood of breaking, a de-duplicated count (161 distinct new names beyond the 22, of which 146 carry parsing or writing code and 15 are note-only or fetch-only by the reports' own labels), the toolchain groups Phase 2 needs authorised, and the four agent reports reproduced in the document's section 8 verbatim (headings demoted two levels so they nest under it; text unchanged). Two corrections were made against GitHub before compiling and are recorded with their evidence in the document's section 7: report A's statement that the maintainer opened python-sgp4 issue #169 on 2026-08-28 is wrong (the GitHub API on 2026-09-24: opened 2026-08-20 by michaelbaisch, title "Support OMM NORAD catalog numbers beyond the Alpha-5 range", still open); report D's statement that SatIntel has no TLE parser is wrong (`osint/tle.go` on main, fetched 2026-09-24, splits the lines on whitespace and assigns `strconv.Atoi` at lines 37 and 47 with the error discarded, as report C had said). Four smaller conflicts between the reports (the closing date of Orekit #758, predict4java's GitHub release, go-satellite's last maintainer activity, SatIntel's last commit) are listed and left open; none bears on Phase 2. The file stays private because it carries reach figures and tracker states as fetched on one day, unverified cells marked "?", and inferred rows; publishing any of it would need each row re-verified, and Phase 2's findings notes are the place for verified results. "Viable" for Phase 2 is read as: own catalog-number code (classes A to F and the reference code), runnable here or with a toolchain the owner authorises; delegators and fetch clients are listed but not run unless the owner asks. Alternatives: one merged table with uniform columns (rejected: re-typing about 200 rows risks transcription errors and drops the per-fact URLs; the index plus the verbatim reports keeps both); running the delegators as well (deferred to the owner, one thin adapter each on top of the base library's).

**D-128 (2026-09-24) — Batch G Phase 2, library 1: PyEphem 4.2.1 run against all seventeen cases; findings, scratch adapter, verified patch and outreach draft kept privately at `docs/handoff/batch-g/pyephem/`, outside the export allowlist; nothing filed.** Result: 6 cases fail, 10 skip, 1 pass (`satcat-70000-cutoff`, a data check on the legacy file that involves no adapter; the runner item that follows deals with it). Findings, each reproduced on this Mac with ephem 4.2.1, Python 3.14.6, corpus 30f8d3b: F1 every Alpha-5 catalog field decodes to 0 (`extensions/_libastro.c:2827`, `strtod` stops at the letter; 604 of 604 corpus element sets; the ten element attributes and `compute()` results are identical to the same lines with a five-digit field, tested for 100404 at three instants), a bug, undocumented, and absent from the tracker after eleven searches; F2 seven fields stored in single precision (`libastro/astro.h:323-329`, casts at `dbfmt.c:234-248`; relative error up to 6e-8, so every values item on a TLE file fails), a libastro design choice not raised upstream; F3 the second derivative is not read, a design choice the maintainer confirmed in issue #153; F4 an empty name is refused with "line does not conform to tle format" (`dbfmt.c:222-228`, `_libastro.c:2813-2819`), a misleading message kept as an optional paragraph in the draft; F5 no designator, classification, element set number or ephemeris type exposed, a design choice recorded for the library table. The draft is a new issue because no existing item covers it; it leads with what works, labels every claim, and carries a fourteen-line patch because PyEphem is MIT; the patch was built from the sdist into a second scratch venv and tested on every Alpha-5 boundary, on the corpus (the Alpha-5 case goes from 4 exact and 8 failing items to 8 and 4, the other sixteen cases unchanged) and against PyEphem's own suite (177 tests OK, 4 skipped, on both builds). The adapter passes a name-less 2LE pair the catalog field as its name, because `readtle()` requires a name; recorded in the note. The derived Alpha-5 line quoted as a reproducer is already public (`derived/**/*` is allowlisted). Kept private for the reasons of D-127; the draft moves to `docs/upstream/` only on the owner's word, and the site's library table gets its PyEphem row when the owner decides. Alternatives: raising F2 upstream (rejected: deliberate storage design, harmless for prediction); adding the adapter to `tests/adapters/` now (deferred: scratch adapters stay with their notes until the owner decides which become corpus adapters). **Filed 2026-09-25T02:17:30Z from the owner's account on the owner's word as https://github.com/brandon-rhodes/pyephem/issues/296** (link added under the D-134 convention; prose paragraphs unwrapped for GitHub's line-break rendering, the diff and reproduction fences untouched, the optional closing paragraph on the "line does not conform to tle format" message cut on the owner's instruction, every other word unchanged).

**D-129 (2026-09-24) — `satcat-70000-cutoff` reports `not-exercised` for every parser; the pass it reported was a data check that involved no adapter (owner instruction after the PyEphem run).** The instruction rested on the premise that the reference, naive and python-sgp4 adapters parse SATCAT and would be unaffected. The code says otherwise: `run_satcat` never calls the parser; its one pass/fail item slices the legacy file's id column itself (`satcat-legacy-below-70000`), the six `satcat-record` items were already `info` with the text "SATCAT records are not parsed by the GP parser under test", and none of the three adapters mentions SATCAT. So the change touches every adapter alike: the item is `not-exercised`, its detail keeps the data result and says the parser under test was not involved, and it stays `fail` if the legacy file ever carries an id at or above 70000, as a data property. Counts from the regenerated reports (`tools/_out`, runs of 2026-09-24T20:24Z): reference 16 exact and 1 not exercised (was 17 exact); naive 2 exact, 14 fail, 1 not exercised (was 3 exact, 14 fail), 76 failing items, unchanged; python-sgp4 1 exact, 7 within tolerance, 7 fail, 1 skip, 1 not exercised (was 2 exact), 20 failing items, unchanged. The failing counts the README, changelog and site quote (14 and 7 of 17) do not move; the reference control line does, and README line 97, the changelog's [Unreleased] section and the catalogue's header and legend now say so; `docs/FAILURES.md` was regenerated (its header wording, timestamp and SATCAT row are the only changes); the case's interpretation text in `tools/make_manifest.py` gained a sentence, and `case.md`, `MANIFEST.md` and `manifest.json` were regenerated. Test: `SatcatCaseTests` asserts, for the reference and naive adapters and for python-sgp4 when importable, that the case carries no pass, pass-tolerance or fail item, is `not-exercised` when the legacy file is on disk and `skip` (source-present only) when it is not; suite 119, OK. Observed on the way: the case lists `catalog-number-is-integer` among its checks, but `run_satcat` evaluates nothing under that name. The site's status table (`site/content/failures.json`, the case's row) shows `pass` three times and needs an S-entry at the next site update. Alternatives: an `info` item (rejected: a case with only info items reports `skip`, which reads as "not run"); a `parse_satcat` hook so an adapter could earn the pass (deferred: a protocol change nobody asked for). Left uncommitted for the owner: the premise did not hold and the reference control line is public text.

**D-130 (2026-09-24) — PR #172 merged upstream as 8126f77 and #171 closed: the README's upstream status paragraph and the pull-request record say so; the python-sgp4 counts stay against 2.27.** Verified from the GitHub API on 2026-09-24 before editing: the pull request is merged (merged_at 2026-09-24T12:58:27Z, merged by brandon-rhodes, merge commit 8126f77 with parent bf25b00, head 0f20787), master is identical to 8126f77, issue #171 is closed at 12:58:28Z with state_reason completed, and PyPI's latest sgp4 is 2.27 of 2026-07-03. Changed: the README's "Upstream" paragraph (dated 2026-09-24; the bullet now records the merge, the commit, the closed issue and that no release carries the fix, so the 7-of-17 result stands against 2.27) and the status block of `docs/upstream/pr171-pull-request.md` (the two amendments and the merge). Not changed: `docs/FAILURES.md`, the case documents and the site's failures table, which report runs against 2.27 and change only when a release with the fix is installed and run; the changelog, which records corpus changes, not upstream ones; the fork clone, whose branch `omm-empty-object-id` at 0f20787 is now merged upstream and can be deleted or kept as the owner prefers. The site's matching edits are S-036. Alternatives: re-running the python-sgp4 adapter against master to show the case passing (deferred: the corpus reports released versions, and the site's weekly job will show the flip when a release ships).

**D-131 (2026-09-24) — A catalog id the runner cannot take as an integer is reported in the values item, with the value and its type, instead of the runner raising inside the parse item (owner instruction after the satellite.js run).** Before: `norm_record` called `int()` on every integer field, so an adapter returning the raw TLE field `'A0000'` made the case's file fail with "parser raised ValueError: invalid literal for int() with base 10: 'A0000'", which reads as a crash in the adapter and hides what the library returned; the satellite.js adapter worked around it by returning no id. Now `as_integer()` converts what converts losslessly (an int, a digit string, an integral float or Decimal; a digit string still gets the type note that the `catalog-number-is-integer` check reports) and returns a problem text for the rest (a bool, anything `int()` refuses, a fractional float or Decimal); `norm_record` leaves such a field `None` and records the problem in the record's `_bad` map; `compare_values` reports "N record(s) whose norad_cat_id is not an integer, e.g. 'A0000' (str)" separately from records that simply carry no id, and "id X: rev_at_epoch is not an integer: 12.5 (float, fractional)" for the other integer fields; the integer check names up to three examples. `tle-catalog-field-decodes` is unchanged (a field with no integer is "not decoded"). Tests: `NonIntegerCatalogIdTests` (the conversion table, and the reference reader with the TLE field kept as text run on `alpha5-tle-derived`: no parse item, the values item names `'A0000' (str)`). Counts for the three adapters are unchanged (they return integers). Reason: several remaining Phase 2 candidates keep the field as a string (satellite.js, tle.js, the raw-string keepers), and the report must say what they returned. Alternative: converting digit strings and rejecting everything else silently (rejected: the type is part of the finding).

**D-132 (2026-09-24) — Batch G Phase 2, library 2: satellite.js 7.1.0 run against all seventeen cases; findings, scratch adapter, two verified patches and a one-issue draft kept privately at `docs/handoff/batch-g/satellitejs/`, outside the export allowlist; nothing filed.** Result: 7 cases fail, 7 skip, 1 pass, 1 within tolerance, 1 not exercised; all 604 TLE records read with every element right to 3e-16, nine-digit OMM ids taken without a cap. Findings, each reproduced on this Mac with satellite.js 7.1.0 under Node 26.5.0 through the runner's `--cmd` protocol: F1 the TLE reader keeps the catalog field as the raw string (`src/io.ts:86`, typed `satnum: string`) and the OMM reader returns the same object's id as a different string (`io.ts:197`), with no decoder in the library, so 0 of 604 Alpha-5 fields have an integer form, a design choice with a gap; F2 `json2satrec` builds the epoch through a JavaScript `Date` (`io.ts:199-208`) and keeps milliseconds only, so 571 of the corpus's 574 OMM JSON records came back truncated by up to 999 µs, measured as 0.92 m along track at epoch + 30 min for a 104 µs shift and 6.19 m for 808 µs, a bug the library's own source comment contradicts; F3 no checksum or line-consistency check, F4 no designator, classification, element set or revolution number, F5 `satrec.no` is the un-Kozai'd value (`nokozai` holds the parsed one), F6 a missing OMM key gives NaN, all design choices or outside the cases. The F2 patch (source and test, against tag 7.1.0) keeps the digits beyond the third; verified on the corpus (every OMM epoch item passes; only the Alpha-5 case still fails) and on the library's own suite (403 of 403 with one existing test rebuilt to append `Z` instead of round-tripping a `Date`, plus a new test that fails without the change; `test:types` and `biome check` clean; the four wasm test files fail before and after for want of the wasm build). The draft is one issue with two sections, the epoch bug and its patch first and the identity question with a boundary-tested decoder sketch second, on the owner's decision that a maintainer reads one issue from a stranger better than two in the same hour; the tracker had nothing on either (nine queries). The adapter passes the library's raw string through since D-131. **Filed 2026-09-24T21:40:16Z from the owner's account on the owner's word as https://github.com/shashwatak/satellite-js/issues/185** (link added under the D-134 convention; prose paragraphs unwrapped for GitHub's line-break rendering, the four fences and every word unchanged). **Pull requests from the owner's fork on the owner's word, after the maintainer (1valdis) asked for both on #185 on 2026-09-25: the epoch fix 2026-09-25T22:17:34Z as https://github.com/shashwatak/satellite-js/pull/186 (the verified patch rebased onto develop 95e43b7, with a changelog line; `vitest --project js` 410 to 411, types and Biome clean; the new test fails without the change) and the decoder 2026-09-25T22:34:52Z as https://github.com/shashwatak/satellite-js/pull/187 (`alpha5ToNumber` in `io.ts` as the maintainer suggested, exported and pinned, boundary tests, 410 to 415; it adds the decoder and does not change what `twoline2satrec` returns, the identity question left to the maintainers; the two merge cleanly in either order, 416 combined).** Alternative: two separate issues (rejected by the owner).

**D-133 (2026-09-24) — Batch G Phase 2, library 3: Gpredict 2.6's SGP4 module run against all seventeen cases; findings, harness, GLib stand-in and a standalone issue draft kept privately at `docs/handoff/batch-g/gpredict/`, outside the export allowlist; no Alpha-5 outreach, the site's ecosystem list instead (S-037); nothing filed.** What was built: `src/sgpsdp/` only, unmodified, compiled with clang against a two-file stand-in for GLib (`g_ascii_strtod` as `strtod` in the C locale) behind a harness of my own that calls `Get_Next_Tle_Set()`; Gpredict's own `test-001` builds and runs the same way; the GTK application and its updater were not built (GLib and GTK absent), so their behaviour is inferred from the source. Result: 6 cases fail, 10 skip, 1 not exercised, 0 pass. F1 every Alpha-5 field decodes to 0 through `atoi` (`sgp_in.c:115-117`; the checksum counts a letter as 0 and `Good_Elements` only matches the two fields), all 604 corpus sets; in the application the `.sat` files and the updater's hash are keyed by that number (`gtk-sat-data.c:64`, `compat.c:339`, `tle-update.c:1001, 1023`) [inferred]. Upstream, verified from the thread: PR #412 (2026-07-18) added a decoder at both sites and was closed the same day by its author as not meant for upstream, withdrawn rather than declined; the maintainer replied on 2026-07-19 that he was not aware of Alpha-5, that CelesTrak would not, as far as he could tell, support it, and that CelesTrak CSV support is in progress (#250 open since 2021). The corpus's own case agrees with his view of CelesTrak, whose TLE output omits objects above 99999, so a stock Gpredict on the default feeds is missing those objects, not misnumbering them. Owner's instruction: no draft for this; the site's ecosystem entry states the three facts and the practical position. F2 the mean motion loses its last digit on every record: `Convert_Satellite_Data` copies ten characters of the eleven-character field (`sgp_in.c:221-223`), so 16.05064833 reads as 16.0506483; measured with python-sgp4 on the ISS set with its last digit dropped, 0.04 m at 1 h, 0.86 m at 24 h, 6.0 m at 7 days, worst case 4.5 times; inherited from sgp4sdp4-0.3 through Hari Nair's version, and Xplanet's copy (`src/libsgp4sdp4/sgp_in.c:154`, Debian 1.3.1-5) carries the same line; no tracker item (fourteen queries; #47 of 2017 was a bad B* value). The fix is two numbers (copy count and terminator index, 10 to 11); verified on a scratch copy: the four TLE-bearing gp cases pass and `test-001` still runs, its own `test-001.tle` mean motion now read whole. Drafted on the owner's instruction as a standalone issue with no mention of Alpha-5 or the corpus, in prose without quoting a Gpredict line (GPL-2.0; no Gpredict code enters the corpus); **filed 2026-09-24T21:37:56Z from the owner's account on the owner's word as https://github.com/csete/gpredict/issues/426** (link added under the D-134 convention; paragraphs unwrapped for GitHub's line-break rendering, words unchanged). F3 `epoch_year = 2000 + yy` ("we assume > 2000", `sgp_in.c:147`) affects only the info dialog, propagation uses the pivot at 57 in `Julian_Date_of_Epoch`; F4 eccentricity clamped at 1e-6 (issue #156); F5 strict layout checks; F6 no classification, raw designator, a name line required. The harness supplies a placeholder name for name-less 2LE pairs and does not report it. Alternatives: building the GTK application (rejected: toolchain absent, and the parser module is where the finding lives); a combined Alpha-5-plus-mean-motion report (rejected by the owner: the mean-motion report stands on its own).

**D-134 (2026-09-24) — Convention for Batch G, recorded in `CLAUDE.md` (owner instruction): a decision entry about an unreported finding names the library and that a finding was recorded, and keeps the specifics in the private handoff until the report is filed; once filed, the entry gains the link.** Reason: `DECISIONS.md` is on the export allowlist, so D-128, D-132 and D-133 published the PyEphem, satellite.js and Gpredict findings in summary before their drafts were filed. Those three entries preceded the convention and are left as written, their drafts going out the week of 2026-09-24; from D-135 on, an entry for a library run says which library, that findings were recorded under `docs/handoff/batch-g/<library>/`, and nothing that would let a reader reconstruct the defect before the maintainer has it. Adding the filed link later is the one in-place edit the append-only rule admits, since it completes the entry without changing the decision. Alternative: rewriting the three entries now (rejected by the owner: the drafts go out this week, and published history is not rewritten).

**D-135 (2026-09-24) — A comment posted on brandon-rhodes/python-sgp4 issue #169 from the owner's account, on the owner's word: https://github.com/brandon-rhodes/python-sgp4/issues/169#issuecomment-5822612170.** Text written by the owner; two corrections made before posting, each checked: the corpus is linked from #171 and #170 by the owner's own texts, not by the maintainer, and the 27 distinct nine-digit ids (799501621 to 799501647) are in the SupGP CSV snapshot with only 799501621 in all four OMM encodings. Offers a corpus run against a branch as the acceptance test (Karl, PR #170's author, had asked for one) and notes that the writer case grades a refusal above 339999 as correct; no design opinion. Text and verification notes in `docs/handoff/batch-g/python-sgp4-169-comment.md`, saved so the next session can find it (the text had lived only in chat).

**D-136 (2026-09-24) — Batch G Phase 2, library 4: gods-eye-view (main) run against all seventeen cases through a Node harness of my own over the application's satellites ingestion layer, with Cesium stood in and propagation stubbed so that no parse result could hide behind a decayed epoch; findings recorded, a patch verified on the corpus and with its own decoder test, and one issue draft kept privately at `docs/handoff/batch-g/gods-eye-view/`, outside the export allowlist; nothing filed.** Result: 6 cases fail, 10 skip, 1 not exercised, 0 pass. MIT, so the draft carries the patch. Under the D-134 convention the specifics stay in the handoff until the report is filed; the owner approved the report on 2026-09-24. **Filed 2026-09-24T23:49:33Z from the owner's account on the owner's word as https://github.com/bilawalsidhu/gods-eye-view/issues/751** (link added under the D-134 convention; prose paragraphs unwrapped for GitHub's line-break rendering, words unchanged; the repository allows no blank issue, so the prose went into the bug-report form's "What happened", the reproduction fence into "Steps to reproduce", and the five remaining required fields were answered from the findings; on the owner's instruction the patch was described in a few lines rather than inlined, with a pull request offered in one sentence). **Pull request from the owner's fork on the owner's word, 2026-09-25T22:30:19Z, https://github.com/bilawalsidhu/gods-eye-view/pull/767**: `noradIdFromSatnum` at the three catalog-builder sites, cut from main b210ab0 (thirty commits past the ce671ce tested, the commit SM-26 confirmed the behaviour on in #751), with boundary tests and the module registered in the package-boundary manifest current main requires; `npm test` 5,064 to 5,067 passing, build, format and boundary checks green, `npm run test:track` 109 passed; the satellites-layer gates were not run because they fetch CelesTrak live, and the description says so; it credits SM-26's confirmation, says "Reported in #751" so the issue stays open, and does not mention FORMAT=json, which the maintainers flagged as a separate decision. Alternative: a harness over satellite.js alone (rejected: the finding lives in the application's own ingestion path, which the harness exercises unmodified).

**D-137 (2026-09-24) — Batch G Phase 2, library 5: SatDump master and the current release, each built standalone (`tle.cpp` and the bundled predict fork) with stand-in definitions of my own for the parts of the application that were not built, run against all seventeen cases; findings recorded, including which of the two parsing paths a user reaches and whether the second is reachable at all (the owner's question), and one prose draft kept privately at `docs/handoff/batch-g/satdump/`, outside the export allowlist; nothing filed.** Result: master 4 pass, 3 within tolerance, 5 fail, 4 skip, 1 not exercised; release 0 pass, 3 within tolerance, 3 fail, 10 skip, 1 not exercised. GPL-3.0, so no patch and no SatDump line quoted; no SatDump code enters this repository. The owner approved the report on 2026-09-24. **Filed 2026-09-24T23:52:38Z from the owner's account on the owner's word as https://github.com/SatDump/SatDump/issues/1221** (link added under the D-134 convention; prose paragraphs unwrapped for GitHub's line-break rendering, words unchanged). **A follow-up comment posted 2026-09-25T03:16:34Z from the owner's account on the owner's word, https://github.com/SatDump/SatDump/issues/1221#issuecomment-5826091342**, narrowing the report's first paragraph: the resynchronisation described was observed after the Alpha-5 sets the registry refused, not after a malformed line, which the corpus has not exercised (see D-145's note on the corrupt-input case); three sentences, no new claim. Alternative: a Docker build of the whole application (rejected: not authorised, and the parsing paths compile on their own).

**D-138 (2026-09-24) — Batch G Phase 2, library 6: libsgp4 (dnwrnr/sgp4) master 661e057 and the head of the open PR #42, each compiled with clang from the unmodified sources behind a harness and a vectors program of my own, run against all seventeen cases; findings recorded and two drafts kept privately at `docs/handoff/batch-g/libsgp4/`, outside the export allowlist: a comment on PR #42 with a one-line pointer for issue #44, per the owner's instruction that a finding there is a comment offering the corpus as a reproducer rather than a new issue, and a separate issue on matters outside that instruction; both approved by the owner on 2026-09-24 and held for filing in a batch with the PyEphem issue; nothing filed.** Result: master 7 pass, 6 fail, 3 skip, 1 not exercised; PR #42 2 pass, 5 fail, 9 skip, 1 not exercised. Before approval, on the owner's instruction, the two exception strings the comment quotes were verified against the trees they are attributed to, each re-run through that tree's own constructor. Apache-2.0 allows a patch; none is attached, the drafts describing the changes in prose. **Filed from the owner's account on the owner's word, the issue first so the comment's references resolve: the issue 2026-09-24T23:41:36Z as https://github.com/dnwrnr/sgp4/issues/45, the comment on PR #42 2026-09-24T23:43:00Z as https://github.com/dnwrnr/sgp4/pull/42#issuecomment-5824123874 (its two epoch references now naming #45), and the one-line pointer on #44 2026-09-24T23:51:34Z as https://github.com/dnwrnr/sgp4/issues/44#issuecomment-5824221503 (the pointer links the PR comment in place of "the comment on #42 above")** (link added under the D-134 convention; prose paragraphs unwrapped for GitHub's line-break rendering, words unchanged otherwise). Alternatives: a new issue for the Alpha-5 finding (rejected by the owner's instruction: #42 and #44 already cover it); one combined report (rejected: the second draft is outside the instruction and stands or falls on its own).

**D-139 (2026-09-24) — Batch G Phase 2, library 7: tle.js 5.0.3 (npm) run against all seventeen cases through a Node harness of my own over the package's public getters, with the epoch taken once from the library's timestamp API and once from its raw fields; findings recorded, a patch with tests verified under the project's own jest suite on a clone of main (94 of 94, 88 of 88 before) and on the corpus, and drafts kept privately at `docs/handoff/batch-g/tlejs/`, outside the export allowlist; nothing filed.** Result: 5 pass, 2 fail, 9 skip, 1 not exercised with the epoch from the fields; 0 pass, 7 fail with the epoch from the timestamp API; the patched clone 6 pass, 1 fail. On the owner's instruction the drafts were restructured the same day: one issue on the two defects the patch fixes, with two further observations as a closing note, no patch inlined, and a pull-request draft to follow the issue. MIT. **The issue filed 2026-09-24T23:57:50Z from the owner's account on the owner's word as https://github.com/davidcalhoun/tle.js/issues/62** (link added under the D-134 convention; prose paragraphs unwrapped for GitHub's line-break rendering, words unchanged); the pull request (the second draft) is still to be opened on the owner's word. Alternative: one issue with the patch in its body (rejected by the owner: unreadable at that length, and a PR gives the maintainer something to merge).

**D-140 (2026-09-24) — Batch G Phase 2, library 8: astroz (ATTron/astroz) main d558933, run against all seventeen cases through a Zig harness and a vectors program of my own compiled against the library's parser as a module, on Zig 0.16.0 installed with Homebrew, the one toolchain the owner authorised; the owner's instruction to verify the decoder's behaviour by running the corpus's Alpha-5 vectors through it rather than by reading the code was followed, and a second probe of the library's date helpers added; findings recorded and two prose drafts kept privately at `docs/handoff/batch-g/astroz/`, outside the export allowlist; nothing filed.** Result: 1 pass, 9 fail, 6 skip, 1 not exercised. To classify every mismatch rather than the twelve the runner displays, the run was repeated through a scratch copy of the runner with that display cap lifted, pointed at the corpus with `--root`; the corpus itself was not changed. GPL-3.0, so no patch, no astroz line quoted, and no astroz code in this repository. **Filed from the owner's account on the owner's word: the Alpha-5 issue 2026-09-25T00:02:53Z as https://github.com/ATTron/astroz/issues/97, the date-defects issue 2026-09-25T00:04:49Z as https://github.com/ATTron/astroz/issues/98 (its opening sentence naming #97)** (link added under the D-134 convention; prose paragraphs unwrapped for GitHub's line-break rendering, words unchanged but for one phrase corrected before posting, "Through the constructor on this branch" becoming "Through `parseLines` on main", since astroz has no constructor and the run was on main). Alternative: building the library's own CLI or Python binding (rejected: the parser module is where the findings live and the bindings expose it unchanged).

**D-141 (2026-09-24) — Batch G Phase 2 complete: eight libraries run, in the owner's order (PyEphem, satellite.js, Gpredict, gods-eye-view, SatDump, libsgp4, tle.js, astroz), one report each, every finding reproduced by running the code, nothing filed by the agent.** The closing table (library, version, cases failed, most serious finding) is kept in the handoff under the D-134 convention until the reports are filed; the three reports already filed are D-132, D-133 and D-135. Filing queue, all on the owner's word and from the owner's account: the PyEphem issue (D-128), the gods-eye-view issue (D-136), the SatDump report (D-137), the libsgp4 comment and issue (D-138, in the morning batch with PyEphem), the tle.js issue then its pull request (D-139), and the two astroz issues (D-140). Once each is filed, its entry gains the link in place. **Filed the same night, 2026-09-24T23:41Z to 2026-09-25T00:05Z, everything in that queue but PyEphem and the tle.js pull request: eight postings, their links in D-136 to D-140.**

**D-142 (2026-09-25) — A gate over this month's launches, computed from existing cases, added to the runner's summary and JSON report; not a new case.** The question: given the objects launched in the last 30 days, as a provider serves them, does the parser return them with the right identity? Inputs already in the corpus: the CelesTrak CSV capture of the last-30-days group (256 objects, ids 100404-100789, captured 2026-09-21) and the corpus's Alpha-5 rendering of those records, the form Space-Track's TLE output carries; the CSV case also records that CelesTrak's TLE request for the group answers HTTP 404. Per format the parser reads, records count as loaded (returned with the expected integer id), misidentified (returned with an id that is absent, not an integer, or not the expected one: 0, NaN, a raw string) or dropped (no record came back, whether refused or silently skipped); the eight Batch G runs showed all three shapes. Mechanics: `compare_values` now records structured counts on every values item (`expected`, `returned`, `loaded`, `misidentified`, `dropped`, `non_integer_id`), so the gate reads numbers rather than sentences; `gpconf/gates.py` holds the gate's definition and reads the snapshot's date, count, id range and leading letters from the case data; the CLI prints `gate this month's launches: ...` below the case table and the JSON report carries `gates`. Wording, on the owner's instruction, names the behaviour and never grades the project, since the site's table says a count is not a verdict: "reads this month's launches in every format it reads here", "only via CSV", "not in any format it reads", and for a TLE-only parser "nothing to load from this feed", with Space-Track's Alpha-5 form reported alongside; a parser that raises on the file counts as dropping every record; a format the parser does not read says "not read", a file not on disk "file absent". Every headline carries the snapshot facts and the caveat that a decoder wrong from J upward is not caught by a snapshot whose ids all begin with A. Computed from the Batch G records without re-running, the eight libraries read: PyEphem, satellite.js, Gpredict, tle.js: nothing to load from the CelesTrak TLE feed, 256 misidentified as Space-Track serves them; gods-eye-view: 1 misidentified, 255 dropped; SatDump 1.2.2: 256 dropped, master: only via CSV; libsgp4 master: only via CSV, PR #42: reads them in TLE; astroz: reads them in TLE, with the letter caveat. Tests: the four wordings, the no-grade rule, the snapshot facts from data, and the reference and naive adapters through a run (132 tests). Alternatives: a new case (rejected: the inputs and checks exist, and a case would double-count in the seventeen-case table); grading words LOADS/PARTLY/NO (rejected by the owner: a verdict on the project). Stage two, a separate decision: JSON, XML and KVN renderings of the same snapshot, so readers of those formats get a measured value.

**D-143 (2026-09-25) — The provider's recorded empty answer is handed to the parser under test as its own check, `empty-answer-yields-no-records`: zero records and no error is the pass, an exception or an invented record fails, a parser without the format skips.** Reason (the second of the four ideas under review, fail-closed semantics): a parser must keep three outcomes apart, a value it cannot represent, a file it cannot read, and an endpoint with no data; the corpus tested the first as a required refusal (the writer case, the vectors) and applied all three to itself in Batch A, but the third was never presented to a parser, because `load_source` recognised the 404 body and returned an empty result without calling the parser. Now, in the four cases that hold such a body (`analyst-objects`, `nine-digit-supgp-launch-nominals`, `six-digit-omm-saramago`, `tle-omits-six-digit-objects`; six files, five `No GP data found` and one `No SupGP data found`), the body goes to the parser first; the runner's own empty-404 handling then continues as before. Declared once in `tools/cases.py` and attached to the four cases' check lists; manifest and the four case docs regenerated; the frozen `expected.json` files embed their check lists from the last expected-value regeneration and gain the id at the next one, which does not affect the runner (it reads the manifest). Counts: the reference, naive and python-sgp4 adapters each return an empty list for the body, so each gains pass items and no failing count moves; the per-case rows of `docs/FAILURES.md` are unchanged. In CI's offline runs the raw files are absent, so the check does not run there. Tests: the four cases declare the check; the reference and naive adapters pass it on every body on disk; a parser that raises fails with "not an error" in the detail; a parser that returns a record fails; a parser without the format skips. Not done, held for their own decisions: a per-record refusal channel (item 2 of the idea) and a corrupt-input case (item 3, synthetic-derived).

**D-144 (2026-09-25) — A refusal channel in the adapter protocol, so that a record the library refuses with an error is kept apart from one it drops silently; the built-in python-sgp4 adapter adopts it; the eight Batch G runs are not re-run and the site's table carries a footnote.** Form (owner-approved as proposed): an entry in the list the adapter already returns, carrying `_refused` with the library's reason as a non-empty string, optionally `_field` (the catalog field as the input carried it) and `_input` (up to 80 characters of the line); and an optional first entry `{"_adapter": {"refusals": true}}` declaring that every record dropped with an error is reported, which is what makes "dropped" honest: with the declaration zero refusals means dropped silently, without it the report says refusals were not reported. Owner's addition: the reason is required and non-empty; a refusal without one is counted as a drop and fails the file's `refusals` item, since a refusal that gives no reason is no better than a drop for the thing this change separates. Runner: refusals are split out at the one normalisation point before any check sees them; `compare_values` credits a refusal whose field decodes to an expected id (digits or Alpha-5, decoded by the corpus) and writes "N expected record(s) not returned: R refused with a reason (e.g. ...), S silently"; the counts gain `refused`, `refused_matched`, `refused_without_reason`, `refusals_reported` and `top_refusal_reason`, and `dropped` excludes matched refusals; a per-file `refusals` item lists reasons with counts (info, or fail when a reason is missing); the empty-answer check (D-143) fails a refusal of the 404 body, since an empty answer is not an unreadable one; the gate reports "R refused (reason)" and "D dropped silently" or "D dropped (refusals not reported by this adapter)". Adoption: one line in a harness's catch block; the python-sgp4 adapter now catches `ValueError` from `twoline2rv` and from `omm.initialize` per record and declares the channel, so its nine-digit result reads as a refusal with the library's own message rather than a whole-file parse failure; no failing case count moves. Not re-run, on the owner's instruction: the eight Batch G harnesses, whose recorded "dropped" counts may include refusals (libsgp4 master's `TleException`, astroz's `InvalidCharacter`, Gpredict's rejected sets); the site's hand-run table says so in a footnote (S-043). Where the library returns a value rather than an error (satellite.js, tle.js, PyEphem's 0) or drops inside itself with no signal (SatDump's registry, gods-eye-view's dedupe) there is nothing to adopt, and misidentified or dropped is the finding. Tests: the split and the reason rule, field decoding, refusals credited and named through a run, the undeclared adapter not credited with silence, the reasonless refusal counted as a drop, the reference reporting none, the refused 404 body failing, and the python-sgp4 adapter's nine-digit refusal (144 tests). Fail-closed rule added when the first regeneration showed the hole: with the adapter refusing the one record in `csv-json-omitted-mandatory-fields`' two SupGP files instead of failing the file, the parser returned zero records and every per-file check passed on nothing, flipping the case to pass; now a file the reference reads records from, for which the parser returned none, gets a failing `records-returned` item that names the refusals or the silent drops, with the same counts the gate reads, and no other check runs on it (the same discipline as D-113 for the reference reader). With it the failing case counts are unchanged: reference none, naive 14, python-sgp4 7. One item count moves and is meant to: python-sgp4's nine-digit case has five failing items instead of four, because `starlink-all.csv` now loads its five-digit records around the 27 refused ones and the classification-C check runs on it and finds C not preserved, a result the whole-file failure had hidden. Held for its own decision: item 3, a corrupt-input case.

**D-145 (2026-09-25) — v0.3.0 planned as the fail-closed release; the plan is a private note at `docs/handoff/v0.3.0-plan.md`, outside the export allowlist; nothing built, no date.** Shape: D-142 to D-144 are already on main (the gate and structured counts, the provider's empty answer handed to the parser, the refusal channel with a required reason and fail-closed on a file returned empty); the release's remaining new work is one case, corrupt input, as a fourth input class feeding the same four counts, loaded, misidentified, refused, dropped, rather than a new concept, and the case count moves from seventeen to eighteen in the same release, because the protocol additions already call for a minor bump under the README's rule and the count change then costs one rebuild of everything that carries it instead of two. The case tests the parser under test on input the corpus has never fed it, where D-113 and D-117 hardened the corpus's own readers; it checks behaviours the Batch G reports recorded as documented but unverified on corrupt input. It needs the owner's provenance approval for synthetic-derived inputs under the D-096 precedent, each a frozen real record with one stated edit, placed between valid records so that resynchronisation is measured. Downstream: the version constants, the site's count sentences and data files, release notes, the Zenodo description, the failures table, and eight Batch G re-runs against the new shape producing a second round of findings and second contacts with the maintainers, which the note argues are the point of the release. The note marks every unverified claim. Alternatives: a corrupt-input case as its own concept with its own vocabulary (rejected: the D-144 counts already describe every outcome it can produce); adding the case before or after the protocol release (rejected: two rebuilds of the count).

**D-146 (2026-09-25) — Identity over time stated as an open question in the coverage gaps, not answered and not a new section (the fourth of the four ideas under review, documentation only).** A nine-digit SupGP id is a launch nominal CelesTrak serves for the days between launch and 18 SDS's first GP data, after which the object is expected under a catalog number of 100000 or above; what a tracker keeps as its stable key across that change, and how the nominal's history merges into the catalogued object's, is a question the corpus cannot answer. Checked first, so the text extends what was there: the nine-digit and classification-C cases already call their files perishable, the research note holds the two CelesTrak quotations the inference rests on, and it recorded, without interpreting, that 72000 and 799501621 share OBJECT_ID 2026-219A. Two facts checked for the text from the frozen records: the corpus holds no object in both forms (the 27 nominals are launch 2026-219; the same day's last-30-days group lists launches 2026-156 to 2026-220 but not 2026-219), and the shared designator is not one-to-one (72000 and 72001 carry 2026-219A and 2026-219B, as do two per-satellite nominals). Placed where the other gaps live: a gap entry on the nine-digit case, a one-sentence cross-reference on the classification-C case, both in `tools/cases.py` with the case docs and manifest regenerated, and the README's existing nine-digit bullet extended. The text says what the corpus covers (both forms parse; the nine-digit case is perishable and tied to a launch window), what it does not (correlation, merging, key stability), and why that is out of scope rather than missing: a data-relationship question for the provider's documentation, not a parsing property a fixture can freeze. Every claim in it is marked tested or inferred; the "no way to map" sentence in CelesTrak's text is about the TLE field width and is not used. No code changed.

**D-147 (2026-09-25) — Propagation-equivalence grading (grading two TLE renderings by whether they propagate to the same TEME position within a tolerance) is not built; an optional information-only figure in metres is left as a stated possibility. The fourth of the four ideas under review, closed.** Premise corrected in review: the idea as put said the writer case grades bytes. It does not. `tle-writer-round-trip` reads the written lines back through the corpus's own reference reader and accepts each field at its own resolution, quantised by truncation or by rounding half up (D-070, D-071), and `tle-writer-matches-provider-rendering` reports byte-identity for information only. The "two writers, different but equally valid TLEs" problem the idea addressed is therefore already handled for every freedom the format is known to allow. That correction came from the agent's review of the proposal, not from the owner. What settles the decision, measured on 2026-09-25 with python-sgp4 2.27 (WGS72, mode `i`) on the corpus's frozen ISS record (25544, epoch 2026-09-20T12:42:37.141056), no network: each TLE field moved by one unit in its last printed digit, position difference against the unmoved record at the same instant, in metres:

| field, one unit in its last digit | epoch | +1 h | +1 day | +7 days |
|---|---|---|---|---|
| mean anomaly (1e-4 deg) | 11.861 | 11.856 | 11.872 | 11.870 |
| argument of perigee (1e-4 deg) | 11.872 | 11.852 | 11.861 | 11.859 |
| RA of ascending node (1e-4 deg) | 11.870 | 9.274 | 11.858 | 11.800 |
| inclination (1e-4 deg) | 0.020 | 9.426 | 3.298 | 23.179 |
| epoch (1e-8 day) | 6.616 | 6.613 | 6.622 | 6.621 |
| eccentricity (1e-7) | 0.769 | 1.299 | 0.745 | 0.678 |
| mean motion (1e-8 rev/day) | 0.003 | 0.018 | 0.428 | 2.999 |
| BSTAR (1e-8, last mantissa digit of 0.15975e-3) | 0.000 | 0.000 | 0.237 | 11.661 |
| first and second derivatives of mean motion | 0 | 0 | 0 | 0 |

(1) A tolerance that accepts every valid writer must admit one unit in an angle's last digit, about 12 m at every horizon and up to 23 m at a week: a floor of roughly 12 m, derivable per record from its own resolution, not a guess. (2) The three precision defects the corpus has found are all smaller than that floor: satellite.js's millisecond epoch truncation, 6.19 m at 30 minutes for an 808 µs loss (D-132); Gpredict's dropped mean-motion digit, 0.04 m at 1 h, 0.86 m at 24 h and 6.0 m at 7 days (D-133); libsgp4's epoch rounding, 8 µs, about 6 cm along track (D-138, reported in dnwrnr/sgp4#45). A propagation grade wide enough for valid writers passes all three; the field-resolution checks catch them. (3) Propagation is blind by construction to identity and to both derivatives: in python-sgp4 at 8126f77, `sgp4()` in `sgp4/propagation.py` never reads `ndot` or `nddot` and `sgp4init` only stores them (the zero row above confirms it), and no identity field (catalog number, international designator, classification, element set number, name) enters the propagation at all. The eight Batch G findings were identity and epoch bugs; propagation grading would have missed every identity finding and, by (2), the epoch findings too. Also weighed: the runner is standard-library only (README), so the dependency would be required (breaking that for every user and CI), optional (reports differing by machine) or vendored (numerical code to maintain); and python-sgp4 is under test here, so using its propagator would put code from a tested project into the oracle, although for a relative comparison of two renderings a shared propagator error largely cancels, and an absolute claim would need a second implementation or published SGP4 verification vectors, whose terms are unread [untested]. Stated possibility, not built: an information-only figure in metres (the propagated position difference at epoch, +1 day and +7 days) on the writer case and on `tle-vs-omm-precision-loss`, skipping visibly when SGP4 is not installed, never passed or failed; worth doing only if the site wants to show metres. Alternatives: propagation grading replacing field-resolution grading (rejected by (1) to (3)); alongside it as pass/fail (rejected: at the tolerance valid writers require it can fail nothing the existing checks pass).

**D-148 (2026-09-26) — Statuses the runner reported without having earned them, in the spirit of Batch A: a provider file that is not on disk reports `not-fetched`, never `skip`; the count line says how many cases need fetched data and names a case that ran on part of its files; the fetch command names a script that exists; and the gate's headline claims nothing about a format it never tried, dates its snapshot from the frozen capture, and judges a freshly fetched CSV against its own count. The last of these is the one to remember: the gate would have shown the corpus's own reference reader as unable to read CSV on the first fresh fetch by any public user. No published count moved.** Found while measuring what a pip-installed runner would show before fetching (the v0.3 packaging proposal): the public export, which carries no provider data, run offline with the three built-in adapters. Five faults, all reproduced before the change. (1) A case whose provider files were absent reported `skip`, the status the site's library page defines as meaning "the library has no reader for the case's format"; python-sgp4's offline run printed 14 skips, one of them a real missing KVN reader and 13 missing data under the same word. D-119 had moved these cases from `not-exercised` to `skip` for the same reason, that the word claimed something about the data; `skip` in turn claimed something about the parser. (2) A case that ran on part of its files passed without a word: after a first fetch the three re-capture files are not yet on disk, and `analyst-objects`, `bstar-and-derivative-forms` and `tle-omits-six-digit-objects` reported `pass` with the gap visible only as a skip count in a column the default output does not explain. (3) The missing-file detail and the drift messages said "run tools/fetch.py", a path relative to a clone's root, which a user outside the root or with an installed runner does not have. (4) The gate headline said "reads this month's launches in every format it reads here" when the CSV capture was absent, and for a parser that misread the TLE lines it said "nothing to load from this feed", a claim that the parser reads TLE only, when its CSV reading had never been tried; a run selected with `--case` did the same, since the CSV case was not run. CI's offline naive run printed that TLE-only headline on every push. (5) Two faults in the gate found while fixing (4), not in the proposal: its snapshot date came from the local fetch's metadata, falling back to `expected.json`'s generation date, so an offline run printed 2026-09-23 for a capture of 2026-09-21 and a fresh fetch would print the user's fetch date beside the frozen count; and every format was judged against the frozen 256, so a CSV fetched on a later day was misjudged. Simulated in a scratch copy with ten rows removed from the CSV (246 records, live tier): the reference reader loaded 246 of 246 and the headline said "only via TLE". The last-30-days group is a rolling window, and the site's tracker, which fetches it daily, recorded 256 records at the frozen capture and then 231 later on 2026-09-21, 203 on 2026-09-23, 228 on 2026-09-24 and 204 on 2026-09-25 (its fetch of 2026-09-22 failed): never 256 again. So on the first fresh fetch by any public user the gate would have shown the corpus's own reference reader, the control every other result is read against, as unable to read CSV. The published results never showed it only because they are run on the original capture. Changes: `runner.py` gains the status `not-fetched`, ranked above `skip` and below `not-exercised` in a case's status, a `not-fetched` key in every case's counts, `CaseResult.missing()`, `fetch_hint(root)` (the clone's own script by a path that works from the working directory, quoted when it holds a space, or the repository URL when the root has no script), `is_provider_data(path)` (a file under `fixtures/<case>/raw/`) and `CorpusIncomplete`: a missing file that ships with the corpus (`derived/`, `vectors/`) cannot be fetched, so "need fetched data" would be false; the run stops with exit status 2 instead, and the per-case handler lets the error through rather than recording it as a parser failure. `__main__.py` adds the `n/f` column, "N need fetched data" in the count line, a line naming cases with no provider files, a line naming cases that ran on part of theirs (with the re-capture timing when one is missing) and the fetch command. `gates.py`: the states `not fetched` and `not run` limit the claim to "every format measured here", "only via X of the formats measured here" or "not in any format measured here", with the format left out named before the numbers, and forbid the TLE-only headline; `not read`, a format the parser declared unsupported, is a fact about the parser and keeps the old wording; the snapshot date is the source's `retrieved_at` in `expected.json`; each format is judged against its own values item's expected count, and a live capture is labelled "live capture fetched <date>, N objects, not the snapshot", or "every record dropped" when the parser raised on it. Two judgement calls, confirmed by the owner on 2026-09-26: a case that ran on part of its files keeps the result of those files and is named below the count line, rather than reporting `not-fetched` over its passes, because the passes are real results on the files present and a failure among them must still show; and a missing file that ships with the corpus stops the run with exit status 2. Alternatives: keeping `skip` and changing only the detail text (rejected: the site's tables read the status, not the detail); reporting a missing shipped file as `not-fetched` or as a parser failure (rejected: the first gives a false instruction, the second charges the parser for a broken copy). What moved, measured: in the full-data runs of reference, naive and python-sgp4 every case status, count, item and gate headline is identical before and after (compared field by field); `docs/FAILURES.md` differs only in its generation time; each case's JSON counts gain `"not-fetched": 0`. The site: its findings table is built from those full-data runs and does not move; its hand-run table's skips were checked against the pattern a missing-data case leaves, a skip count equal to the case's source count, and the 27 rows that match it are TLE-only libraries facing the all-XML `omm-xml-schema` or the all-CSV-and-JSON `csv-json-omitted-mandatory-fields`, whose 20 files were on disk from 2026-09-21, three days before the hand runs, so every skip there is a missing reader and the site's definition holds. Offline, the public export: reference 4 pass and 13 skip becomes 4 pass and 13 need fetched data; naive 4 fail and 13 skip becomes 4 fail and 13 need fetched data; python-sgp4 1 within tolerance, 2 fail and 14 skip becomes the same with 1 skip and 13 need fetched data; the naive gate headline changes from "nothing to load from this feed" to "not in any format measured here (CSV not measured: not fetched)". Tests: `tests/test_not_fetched.py` (new: the status, the JSON counts, the count line and table header, skip kept for a parser without hooks, a partly fetched case run from a scratch root without its re-capture, the fetch command in a clone, with a space and without the script, and exit 2 for a missing shipped file); `tests/test_gates.py` (every-format and not-in-any-format headlines with a format not fetched or not run, no TLE-only claim then, the not-read wording unchanged, only-via with an unknown third format, a live capture judged on its own count, no borrowed count when the parser raised on a live file, and the snapshot date read from `expected.json` with and without the local metadata); `tests/test_runner.py` (the D-119 tests now expect `not-fetched`). 162 tests pass in the private repository; in a copy of the public export with the new code, 160 run, 150 pass and 10 skip for want of provider data, and CI's two offline commands exit 0 and 1 as before. README ("Tolerances" and the gate paragraph), CHANGELOG and the CI comment updated.

**D-149 (2026-09-26) — v0.3.0 re-planned as the packaging release and the corrupt-input case moved to v0.4.0 (owner decision); the plan split into two private notes; two corrections recorded, one to D-145's argument and one to the proposal's own count.** After an outside review concluded that the corpus's ceiling is adoption friction, not missing cases, the agent proposed packaging on 2026-09-26 and the owner accepted it. v0.3.0 is: a PyPI package, `gpconf`, whose runner stays standard-library only, with optional extras; a `--preset` flag with four presets pip can complete (reference, naive, sgp4, pyephem) and two that need Node (satellite.js, tle.js); the five compiled and application harnesses (libsgp4, Gpredict, SatDump, astroz, gods-eye-view) kept out of the wheel as recipes; and an offline, report-only GitHub Action that exits 0 and carries no badge. The case count stays at seventeen. The corrupt-input case moves to v0.4.0 with D-145's decisions 2 to 4; decision 1, the README rule's added words "or an additive protocol change", stays with v0.3.0, which carries the protocol change it covers. The note D-145 names, `docs/handoff/v0.3.0-plan.md`, is renamed `v0.4.0-plan.md` and revised; a new `v0.3.0-plan.md` holds the packaging plan: the wheel's contents and exclusions (no raw provider data, D-019; no SupGP values, D-049; built from the public export, never from the private repository, whose `expected.json` files carry the values the export scrubs); the runner's root taken from its own file path, which points at site-packages once installed and is to be split into a read-only corpus root and a writable data root; a per-user cache folder for fetched data; the Node resolution problem, marked inferred and untested; and the Action's reason for staying offline, a fetch pattern from shared runner addresses that CelesTrak has not agreed to. Both notes stay private. Corrections, both from the agent: D-145's note argued that adding the case after a protocol-only 0.3.0 would force the rebuild of everything that carries the count twice; it does not, because v0.3.0 keeps seventeen cases and those surfaces change only when the count does, so the cost of landing later is one more release cycle, not a second rebuild. And the packaging proposal said four of the eight Batch G re-runs would become preset commands; three will (PyEphem, satellite.js, tle.js), since python-sgp4, the fourth pip preset, is a built-in adapter and not one of the eight. Alternatives: packaging and the case in one v0.3.0 (rejected: the case is unbuilt and needs its own provenance approval, and packaging should not wait for it); packaging as a 0.2.x patch (rejected: main already carries an additive protocol change, which makes the next release a minor).

**D-150 (2026-09-26) — v0.3.0 packaging, stage 1: the runner works when installed, not only from a clone. Two roots, a per-user cache folder for provider data with a `--data` flag and a `GPCONF_DATA` variable, and the fetch script as the `gpconf fetch` subcommand; no packaging metadata, build, upload or Action (owner instruction). A correction to D-148's re-capture sentence.** The problem, from the plan (`docs/handoff/v0.3.0-plan.md`, section 4): `__main__.py` and `runner.py` took the corpus root as the folder above the package, which is the repository in a clone and site-packages once installed; and the fetch script existed only as `tools/fetch.py`, a path an installed copy does not have. Changes. `gpconf/locate.py` (new) resolves two roots. The corpus root, read-only, holds what ships (manifest, every `expected.json` and `case.md`, `derived/`, `vectors/`, `tools/fetchlist.json`); first match of `--root`, a clone (the folder above the package holds `manifest.json` and `fixtures/`), and the corpus bundled as package data in `gpconf/corpus/`, found through `importlib.resources` so that an import from an archive is refused with a reason rather than failing on a missing file. The data root holds the provider files in the clone's layout, `fixtures/<case>/raw/`; first match of `--data`, `GPCONF_DATA`, the corpus root itself when that is a clone or came from `--root` (every clone and every test keeps its layout), and a per-user cache folder, one per corpus version: `$XDG_CACHE_HOME/gpconf/<version>` or `~/.cache/gpconf/<version>` (a relative `XDG_CACHE_HOME` is ignored, as the XDG specification says), `~/Library/Caches/gpconf/<version>` on macOS, `%LOCALAPPDATA%\gpconf\Cache\<version>` on Windows. The runner takes both roots and reads provider files from the data root and everything else from the corpus root; the gate reads a live capture's metadata from the data root. `gpconf/fetch.py` is `tools/fetch.py` moved into the package with its one root split in two (the fetch list and manifest from the corpus root, the files to and from the data root) and `--root` and `--data` options; its request logic is unchanged. `tools/fetch.py` is now a shim that runs the same code in a clone and, when imported, replaces itself in `sys.modules` with the package module, so the tests' `import fetch` and any patch reach the same code. `gpconf fetch` hands its arguments to that module, so it takes exactly the options `tools/fetch.py` always took. `gpconf run` prints `provider data: <folder> (<why>)` above the case table when the data root is not the corpus root, so a clone's output is unchanged; a missing corpus exits 2 with a reason; `check-tle` runs before any corpus lookup, since it checks the user's own file. D-148's fetch command now names the clone's `tools/fetch.py` where one exists and `python3 -m gpconf fetch` elsewhere, adding `--data DIR` when the data folder came from that flag and `--root DIR` for a bare corpus copy, and nothing for `GPCONF_DATA`, which reaches the fetch by itself. The fetch's dry-run summary, found while showing the installed copy, said "done: 0 request(s) made, 61 entries skipped" of the 61 requests it plans; it now says "dry run: no request made; a run would make 61 request(s) and leave 0 entries as they are". Alternatives: duplicating the fetch code in the package (rejected: two copies of the request logic under CelesTrak's policy is one too many); an environment switch to mark an installed copy (rejected: a clone must keep working with no setup, and the files beside the package already say which it is); one cache folder shared by all versions (rejected for now: a version's frozen values belong with the files it was built against; reusing matching files across versions is an open item in the plan note); printing the data folder on every run (rejected: it would change every clone's output for no information). Correction to D-148: its runner line and README sentence said a missing re-capture file "is fetched on a later run, once its original is at least two hours old". It is not: the fetch list marks the three re-capture entries as re-captures, and the fetch selects them only with `--include-recaptures`, whose help calls them maintainer-only (`tools/fetch.py` as committed at D-148, the selection filter and the flag's help text); a dry run of an installed copy plans 61 of the 64 entries. A normal fetch therefore always leaves `analyst-objects`, `bstar-and-derivative-forms` and `tle-omits-six-digit-objects` without their re-capture, and the runner's line and the README now say so; the fetch command below it is printed only when a file a normal fetch would bring is missing, since for a missing re-capture it would make no request. The error was the agent's, found while moving the fetch code. What moved: in the full-data runs of reference, naive and python-sgp4, every case, count, item and gate headline is identical to D-148's reports, compared field by field; `docs/FAILURES.md` is not regenerated. Scope, as instructed: no change to `pyproject.toml`, no build, no upload, no Action. The bundled corpus folder is assembled by the tests, which copy the shipped files into a scratch copy of the package, standing in for the build step of a later stage. Tests: `tests/test_install_layout.py` (new, 16): the cache folder on each platform, the precedence of the two roots, a clone printing no data line, and a copy outside the repository run in subprocesses with only that copy on `PYTHONPATH` and a scratch `HOME` (the copy, not the repository, is imported; an offline run finds the bundled corpus, names the cache and creates nothing; `list`; a fetch dry run plans every normal entry into the cache and writes nothing; `--data` and `GPCONF_DATA` redirect both run and fetch; with provider files copied into a data folder the copy runs every case; without a corpus, `run` exits 2 and `check-tle` still works), and `gpconf fetch` in-process with a stub where the request would be, writing to the data root and reading the list from the corpus root; `tools/fetch.py` and `gpconf.fetch` are the same module. No test makes a request. `tests/test_not_fetched.py`: the fetch command and the re-capture line updated. 178 tests pass in the private repository; a fresh public export (160 files, three new) runs 176 with 11 skipped for want of provider data, and CI's two offline commands exit 0 and 1 as before. README ("Fetching responsibly" and the not-fetched paragraph) and CHANGELOG updated.

**D-151 (2026-09-26) — v0.3.0 packaging, stage 2: the adapters move into the package and `--preset` arrives. Four presets, reference, naive, sgp4 and pyephem; each library preset records the version it was tested against and the report prints the version it found; naive is labelled a demonstration of failure. No packaging metadata, build, upload, Action or Node preset (owner instruction). No published count moved.** The three built-in adapters moved with `git mv` from `tests/adapters/` to `gpconf/adapters/` (`reference.py`, `naive.py`, `sgp4_adapter.py`), their code unchanged apart from docstrings and the python-sgp4 import message; the old files are shims that replace themselves in `sys.modules` with the package modules, so `--adapter tests.adapters.reference:Parser`, the tests, CI and the published documents that cite those names keep working and reach the same code. PyEphem's adapter came from the hand run's harness (`docs/handoff/batch-g/pyephem/adapter_pyephem.py`, corpus D-128, reported as pyephem#296) as `gpconf/adapters/pyephem_adapter.py`, its parsing unchanged; the harness's module-level list of refusals and its one stderr line per refused element set were for the findings note and are dropped, which changes no result; counted over a full run of the corpus, `readtle()` refuses no element set, so neither ever fired. It does not yet use the refusal channel (D-144), which the v0.4.0 plan gives every re-run; adopting it now would change its gate reading against the published row. `gpconf/presets.py` (new) names each preset's module, the library it exposes, the pip requirement, the optional extra and the tested version (python-sgp4 2.27, PyEphem 4.2.1). `gpconf run --preset NAME` loads it; `gpconf presets` lists the four with the installed version or the install command; the report's parser line reads, for example, "preset sgp4 (python-sgp4 2.27, the version it was tested with)", or names both versions and says results may differ from the published ones when they differ; the JSON report carries a `preset` block; a missing library is refused with exit status 2 and `pip install sgp4` or `pip install ephem`; `--preset` combined with another parser option is an error. The naive preset's line reads "preset naive (a demonstration of failure, not a parser anyone should use)". CI's two offline runs now use `--preset reference` and `--preset naive`. A conflict in the instruction, recorded rather than worked around: optional extras are declared in `pyproject.toml`, which this stage leaves alone; the registry records each extra (`sgp4`, `pyephem`) and its requirement, the install message names the library's own pip package, which is true today, and the extras go into `pyproject.toml` at the packaging stage. An export-allowlist gap, found while verifying the export and recorded for whoever adds the next folder: `PUBLIC_ALLOWLIST.txt` lists `gpconf/*.py`, a rule that was correct for as long as the package was one flat folder, and stopped being correct the moment this stage put `gpconf/adapters/` under it, because the allowlist's patterns do not recurse. Nothing failed: the private suite passed, since the private repository has every file, and the export dry run only reports a count. An export would have shipped the `tests/adapters/` shims without the modules they load, and public CI would have failed on the next push. It showed only as an export count five short of the files added (162 against 167). `gpconf/adapters/*.py` is added and the export now copies 167 files. So that the next folder cannot slip through the same way, the allowlist carries a comment above the package lines saying that patterns do not recurse and a new folder under `gpconf/` or `tests/` needs its own line, and `tests/test_export.py` now fails if any Python file under `gpconf/` or `tests/` is missing from an export. The check to make when adding a folder anywhere: does an existing allowlist pattern reach it? What moved in the published counts: nothing. The three built-in presets on full data in this repository give, for every case, count, item and gate, exactly the results of the D-148 reports behind `docs/FAILURES.md`, compared field by field; only the report's parser label differs, and `docs/FAILURES.md` is not regenerated. The PyEphem preset, run from a copy installed outside the repository (the corpus from a fresh public export, python-sgp4 2.27 and PyEphem 4.2.1 installed from PyPI into a scratch environment, the provider files of this repository as its data folder), fails the same six cases as the published hand-run row (PyEphem 4.2.1, 2026-09-24, 6 of 17); its other differences are all accounted for: D-143's empty-answer check turns three skipped cases into passes and adds a pass to `analyst-objects`; D-129 makes the SATCAT case not exercised; and the public export's scrub of SupGP values (D-049) drops the two Starlink G15-27 pairs from the pair lists of `mean-motion-derivative-convention` and `tle-vs-omm-precision-loss` (304 pairs to 302), so the installed copy never loads that CSV there and each case shows one skip fewer. The hand-run row stays as run. Alternatives: keeping the adapters in `tests/` (rejected: `tests/` is not part of an installed package); registering presets through package entry points (rejected for now: a standard-library table is enough for four, and entry points need the packaging metadata this stage leaves alone). Tests: `tests/test_presets.py` (new: the four presets and their descriptions, the naive label, the version label for a match and a mismatch, an unknown preset and a missing library refused with the install command, `--preset` not combinable, the old names being the package modules, a preset giving the adapter's results over the offline cases, the JSON block, the version printed by each installed library preset) a class in `tests/test_install_layout.py` running every preset from a copy outside the repository, and the export check in `tests/test_export.py`. 191 tests pass in this repository with one skip (PyEphem is not installed here); a fresh public export (167 files) runs 189 with 12 skipped; CI's two commands exit 0 and 1 as before. The export check fails when the `gpconf/adapters/` line is removed and passes with it.

**D-152 (2026-09-26) — The SatDump link stand-ins stay out of the MIT repository and its Zenodo archives, on the cautious reading of GPL-3.0; the public SatDump recipe will ship the harness and the list of symbol names the link needs, with the instruction to define each as an empty function or a default-constructed object; the harness's dead call to a predict function, and its redeclaration, are removed. Owner decision on the agent's analysis; analysis, not legal advice.** What the files are: `stubs.cpp` (20 lines, SatDump master f3d82ad) and `stubs-rel.cpp` (17 lines, release 1.2.2), kept with the hand run in `docs/handoff/batch-g/satdump/`, contain no line of SatDump. They include SatDump headers and define nine and seven SatDump symbols that SatDump's own compiled `tle.cpp` references and the harness never calls: the configuration object, the database pointer, a version string set to "harness", the member function `DBHandler::get_meta`, two HTTP helpers, the global logger and `Logger::logf`, an explicit specialisation of SatDump's `format_notated` template, and in the release file `main_cfg`, `saveUserConfig` and `eventBus`. Every body is empty or returns a constant. The permissive reading, which is genuinely strong: nothing expressive is copied, since the names and type signatures are dictated by the linker; names are not protected by copyright in the US under the Copyright Office's rule on words and short phrases; the Court of Justice of the EU held in SAS Institute v World Programming (2012) that a program's functionality and interfaces are not protected expression; the US Supreme Court held in Google v Oracle (2021) that reimplementing about 11,500 lines of declarations was fair use, and these are sixteen short definitions for non-commercial interoperability testing; GPL-3.0 counts as modifying only copying or adapting that needs copyright permission; the combined program is only built on the user's machine, and GPL-3.0 section 2 lets anyone make and run covered works they do not convey; MIT is GPL-compatible. On that reading the files could be published under MIT. The cautious reading: the stand-ins are not client code but replacements for parts of SatDump, defining SatDump's own objects and functions, including a member function of its `DBHandler` class and a specialisation of its template, and adapting part of a work is within GPL-3.0's definition of modifying; their selection, namespaces and types are dictated wholly by SatDump's internal structure at two commits; the US Federal Circuit held in 2014 that the structure and organisation of declarations can be protected, and the Supreme Court did not overturn that but decided fair use, a defence argued after an accusation. The deciding reasons: first, the asymmetry of the costs. Withholding the files costs a SatDump user the same sixteen empty definitions, written from the linker's errors. Publishing them wrongly would put GPL-derived material under MIT in a public repository, and every release of that repository is archived by Zenodo, where it cannot be withdrawn. Second, the corpus's policy to interpret licences strictly, never permissively (`CLAUDE.md`). The permissive reading is strong; it is not the standard the corpus sets. Consequences: the stand-in files stay private; the public SatDump recipe, in a later stage's harnesses folder, ships `harness.cpp` and `harness-rel.cpp`, which both readings accept as client code, since they call SatDump's functions the way any program uses a library and the FSF accepts GPL-compatible licences for such modules, together with the list of symbol names, which are not expression under either reading. The two harnesses redeclared `Julian_Date_of_Epoch` from SatDump's bundled predict fork and called it, but the result was discarded (`(void)jd`) and the ISO epoch was already built from a year computed locally with the fork's pivot, so the call and the redeclaration were dead code; both are removed, and the predict header stays because the harness calls `predict_parse_tle`. Verified by compiling the epoch helper on its own with clang, before and after: identical output over 63 year and day inputs. The full harness was not rebuilt, since no SatDump tree is on this machine. Open, not decided: publishing the stand-ins under GPL-3.0 in a separate repository, which would satisfy both readings; and asking SatDump's copyright holders, the one way to settle the question outright, which is the owner's contact to make.

**D-153 (2026-09-26) — The v0.3.0 plan's other five open items decided (owner decision on the agent's proposals).** (2) The Action, when a preset's library will not import, fails as a setup error with exit status 2 and a message saying that nothing ran and that this says nothing about the library. Results never fail a job, which is report-only with exit 0 for failed cases, but a run that tested nothing must not show green: that is the fault D-148 corrected in the runner, an unearned status. A library version other than the tested one is not a setup error; it runs and the report names both versions. The command line already exits 2 for a missing Python library (D-151), and its message gains the same two statements. Consequence for stage 3: a Node preset runs through the command protocol, one process per file, so a missing Node or library would otherwise show as a parse failure in every case, blamed on the library; stage 3 adds a preflight that checks both before any case runs. (3) tle.js: the preset `tle.js` reads the epoch from the raw year and day fields, reproducing the published row's 2 of 17, and `tle.js-api` reads it through `getEpochTimestamp()`, whose flooring to the millisecond (finding F3, a design choice) alone fails every TLE case in the hand run, 7 of 17. One harness serves both. (4) The `crosscheck` extra (python-sgp4, Skyfield, xmlschema, lxml) serves `tools/crosscheck.py` and `tools/validate_xml.py`, which the wheel does not carry; it is dropped from the published metadata, and maintainers install the pinned `tools/crosscheck-requirements.lock.txt`, to which the README's build paragraph will point. It was never published, so nothing breaks. It lands with the packaging metadata. (5) Fetched files are reused across corpus versions only in the stable tier and only on a SHA-256 match with the new version's manifest, copied with their metadata and marked as reused; live files are never reused, so an upgrade brings current data. Measured on the current fetch list: 22 of the 61 normal entries are stable, about 10 KB in all, and the other 39 are about 12.5 MB, so the saving is 22 requests per upgrade, a courtesy to CelesTrak rather than a gain in speed. Upstream drift is caught by the site's weekly check of the stable sources, not by a user's upgrade. It lands in the fetch code at a later stage. (6) The wheel is built by a staging script whose only input is a public export folder: it refuses a folder that holds any provider file or lacks `EXPORT-MANIFEST.txt`, walks the `gpconf` package recursively, copies the shipped corpus files into `gpconf/corpus/`, builds with plain setuptools package data and audits the wheel, every corpus file of which must match the export manifest's SHA-256. Rejected: hatchling's force-include, which maps whole folders, so a build run from the private tree would sweep in provider files, prevented only by procedure; and moving the data into `gpconf/` in the repository, which would break every published path, including the site's links and the Zenodo archives. Found while answering it: `pyproject.toml`'s `packages = ["gpconf"]` has omitted `gpconf/adapters` since D-151 and would have dropped it from a wheel, the allowlist fault of D-151 in another file; it is fixed with the staging script, at the packaging stage. The plan note records all six and the new open items.

**D-154 (2026-09-26) — v0.3.0 packaging, stage 3: the Node presets `satellite.js`, `tle.js` and `tle.js-api`, run from the working directory through `node --eval`, with a preflight that refuses a missing Node or library as a setup error, and `--module PATH`. No published result changed.** Resolution, tested before building on 2026-09-26 with Node 26.5.0 and satellite.js 7.1.0 and tle.js 5.0.3 installed in a scratch project: a harness file outside the user's project cannot import the library by bare name, even when run from inside the project (`ERR_MODULE_NOT_FOUND`), which the plan had only inferred; `createRequire`, which the plan proposed, is the wrong tool, since satellite.js 7.1.0 exports only an `import` condition, which `require` resolution does not take; code passed to `node --input-type=module --eval` resolves bare imports from the working directory, like a script in the project, and `import.meta.resolve` there gives the entry file. So each preset runs its harness as `node --input-type=module --eval <source> -- <arguments>` with the working directory as its current directory, and `--module PATH` (the environment variable `GPCONF_NODE_MODULE` inside the harness) names the library's entry file for a checkout that cannot import itself by name. The harnesses moved into the package as `gpconf/adapters/satellitejs.mjs`, `tlejs.mjs` and `tlejs_vectors.mjs` from the hand runs (D-132, D-139); their parsing bodies are byte-identical to the hand runs' (compared with `diff`), and only how each finds its library and reads its arguments changed. `tle.js` passes `fields` and `tle.js-api` passes `api` to the one harness, and both use the vector-hook harness, as the hand run did (D-153). `gpconf/adapters/node_preflight.mjs` imports the library before any case runs and reports the Node version, the entry file and the library's version, read from the nearest `package.json` above the entry that names the library; a missing Node or a library the working directory cannot import is refused with exit 2 and a message naming the folder, the `npm install` command and `--module`, and saying that nothing ran and that this says nothing about the library (D-153). Without the preflight the command protocol would have turned a missing library into a parse failure in every case, charged to the library. The Python presets' refusal gained the same two statements. `CommandParser` takes an argument list besides a shell string: a list runs without a shell, in a given working directory with a given environment, and only an element that is exactly `{fmt}` is substituted, so the harness source passed as an argument is never altered. `gpconf presets` shows, for each Node preset, whether the library imports from the working directory, with its version and Node's; the JSON report's `preset` block adds the runtime. `PUBLIC_ALLOWLIST.txt` gains `gpconf/adapters/*.mjs`, the check D-151 asked for when adding files, here a new file type in an existing folder; `tests/test_export.py` now checks every file of the package, not only Python files. Results, from a copy installed outside the repository (the package from a fresh public export) and run from the scratch project with this repository's provider files as its data: each preset fails exactly the cases of its published row, 7 of 17 for satellite.js, 2 of 17 for tle.js and 7 for the API mode; every other difference from the hand runs is six extra passes, the D-143 empty-answer check on the six recorded empty answers, which the hand runs predate. The three built-in presets' full-data results are identical to the D-148 reports behind `docs/FAILURES.md`, compared field by field. Tests: `tests/test_node_presets.py` (new, 10): the registry, `--module` only with a Node preset, argument lists with `{fmt}`, the working directory and exit 3, a shell string still working, Node missing, a folder without the library for all three presets and from the command line (exit 2, no case run), the listing's missing-library line, and, where `GPCONF_TEST_NODE_PROJECT` names a project with the two libraries, each preset's version and offline statuses, fields and API differing only in the epoch, and `--module` from another folder. 201 tests pass in this repository with four skipped (PyEphem not installed; the three library tests without the project variable), and all ten Node tests pass with it; a fresh public export (172 files) runs 199 with 15 skipped; CI's two commands exit 0 and 1 as before.

**D-155 (2026-09-26) — v0.3.0 packaging, stage 4: `harnesses/`, the recipes for the five hand-run libraries that cannot be presets, in the public repository; best-effort, not in the pip package; SatDump's recipe as D-152 set it.** One folder per library, libsgp4, Gpredict, SatDump, astroz and gods-eye-view, holds the corpus's harness files, copied from the hand runs (`docs/handoff/batch-g/`) byte for byte except one comment line in astroz's `common.zig` that said whose code it is, and a README naming: the commit it was tested at, in full (read from the GitHub API on 2026-09-26: libsgp4 master 661e057a and PR #42 head 2b8d1413, Gpredict v2.6 c9fa018f, SatDump master f3d82adb and 1.2.2 7aef0fe8, astroz main d558933e, gods-eye-view main ce671ce5); the project's licence (Apache-2.0, GPL-2.0, GPL-3.0, GPL-3.0, MIT; the last from the application's `LICENSE` file, since GitHub's detector reports it as unrecognised); what the harness compiles against, internal source files or module paths in four of the five and the library's own classes in libsgp4, and that a recipe can break silently when the project moves; the toolchain as run; the published hand-run result; the build commands; and the run command, from a corpus clone's root or anywhere `gpconf` is installed. The folder's README says the recipes are not part of the `gpconf` package, are built by nothing in CI, were built and run once, and that the runner's later checks, such as D-143's, can change their pass and skip counts. The build commands are the ones recorded when the harnesses were built on 2026-09-24, with only paths changed, and two stated differences: the SatDump release links its own tree's predict files, where the run used objects compiled from master's tree [untested]; and the SatDump harnesses lost D-152's dead predict call. None of the five was rebuilt for this stage: the projects' sources are no longer on this machine. SatDump's folder holds `harness.cpp` and `harness-rel.cpp` and no stand-in: its README lists the nine symbols master's link needs and the seven 1.2.2's needs, each with its kind, and says to define each as an empty function or a default-constructed object; it says why there are no stand-ins in one sentence, that they define SatDump's own functions and objects and the corpus keeps anything written to replace part of a GPL-licensed program out of its MIT-licensed repository, without arguing the licence question. A private-only test checks that the list names exactly the symbols the corpus's own stand-ins defined, and a public test that no recipe source defines any of them. Two recipes keep a stand-in, and the owner confirmed that judgement on 2026-09-26, with this reasoning: a stand-in for a third-party library the project calls is not the same act as a stripped replacement for part of the project itself, which is what D-152 kept out. Gpredict's folder keeps `stubs/glib.h`, one macro mapping GLib's `g_ascii_strtod` onto the C library's `strtod`, the same conversion in the C locale the harness never leaves, and an empty `stubs/glib/gprintf.h`; GLib is licensed LGPL-2.1-or-later, and Gpredict itself contributes nothing to the stand-in. gods-eye-view's folder keeps `cesium-stub.mjs`, which binds the seven Cesium names the layer's update path touches to one inert placeholder, loaded through `cesium-hook.mjs` and `register.mjs`; Cesium is licensed Apache-2.0. A `harnesses/.gitignore` keeps what the recipes create inside a clone, including any SatDump stand-ins a user writes, from being committed. `PUBLIC_ALLOWLIST.txt` gains `harnesses/**/*`, which recurses, unlike the single-level package patterns, and `harnesses/.gitignore`, since glob skips dotfiles; `tests/test_export.py` checks every file under `harnesses/`, and fails with either line removed (checked). The README's contents table names the folder. Tests: `tests/test_harnesses.py` (new): the five folders and their exact files, each README's commits, licence and limits, the folder README, the SatDump list and its reason, no stand-in definitions, and, privately, the list against the stand-ins used and the copies against the hand runs' files. 209 tests pass in this repository with four skipped; a fresh public export (195 files) runs 207 with 17 skipped.

**D-156 (2026-09-26) — v0.3.0 packaging, stage 5: the staging script and the packaging metadata; the sdist and the wheel built locally from a fresh public export and audited; nothing registered, uploaded or published (owner instruction).** `tools/stage_package.py` (new, standard library only), as D-153 set it. Its only input is a public export folder, and it refuses one that lacks `EXPORT-MANIFEST.txt`, holds any provider file under `fixtures/<case>/raw/`, or holds `docs/handoff/`, which only the private repository has; that third check is an addition to D-153's two, since a private tree would otherwise pass them. It stages `pyproject.toml`, `README.md` and `LICENSE`; the whole `gpconf` package, walked recursively, so a new subfolder cannot be missed the way D-151 and D-153 found two patterns missing one; and the shipped corpus, `manifest.json`, `tools/fetchlist.json`, every case's `expected.json` and `case.md`, `derived/` and `vectors/`, into `gpconf/corpus/`, where `gpconf/locate.py` looks for it (D-150). Every staged file must match the export manifest's SHA-256, so an export changed after it was made is refused. With `--build` it runs the `build` frontend without isolation, which makes the sdist and builds the wheel from it, and audits both: every file under `gpconf/` must match the manifest, a corpus file under `gpconf/corpus/X` against the entry for `X`, and the audit fails on a mismatch, a file the manifest does not list, a provider file, any member other than the package and the build's own metadata, or a staged file missing from the archive. `pyproject.toml`: packages are found (`include = ["gpconf", "gpconf.*"]`) instead of listed, so `gpconf.adapters` is in, and so is any later subpackage, the structural answer to the explicit list D-153 found wrong; package data `corpus/**/*` for `gpconf` and `*.mjs` for `gpconf.adapters`; the extras `sgp4` (`sgp4`) and `pyephem` (`ephem`), unpinned, since a preset reports the version it finds against the one it was tested with; the `crosscheck` extra dropped, and the README's build paragraph now names `tools/crosscheck-requirements.lock.txt`; runtime dependencies still none. The licence moves to the current declaration, `license = "MIT"` with `license-files`, which needs setuptools 77 or later to build; the older table form is deprecated there. The version stays 0.2.1: the bump belongs to the release step. The build, from a fresh public export (196 files) with build 1.6.1 and setuptools 84.0.0 installed into a scratch environment used for nothing else: the sdist `gpconf-0.2.1.tar.gz`, 90 files, 4,516,949 bytes unpacked, 499,591 bytes as built; the wheel `gpconf-0.2.1-py3-none-any.whl`, 85 files, 4,442,875 bytes unpacked, 543,380 bytes as built; the two hold the same 79 package and corpus files, the sdist adding eleven build files and the wheel six metadata files; the audit found no problem in either, 60 corpus files and 19 package files each matching the export manifest; the build printed no warning; a second build gave the same wheel file list. Installed from the wheel into another scratch environment, with no network and no dependencies, the `gpconf` command ran `--preset reference` from an unrelated folder (4 pass, 13 need fetched data, the per-user cache named, nothing created in it) and refused `--preset sgp4` as a setup error. Not done, as instructed: no name registered on PyPI, no upload to TestPyPI or PyPI, no publish workflow. The README, which is also the package's long description, still has relative links that will not resolve on PyPI; that belongs to the documentation step. Tests: `tests/test_stage_package.py` (new, 8): the staged tree against the export, each refusal, the command line's exit 2, the audit passing a faithful wheel and sdist made here, the audit catching a changed file, a missing file, a provider file, an unlisted file and an unexpected member, and the metadata. 217 tests pass in this repository with four skipped; a fresh public export (197 files) runs 215 with 17 skipped.

**D-157 (2026-09-26) — v0.3.0 packaging, stage 6: stable-tier files are reused across corpus versions on a hash match, as D-153 set it; live files are never reused; the fetch and the run report say which files were reused rather than fetched.** Where: only in the per-user cache, where each corpus version has its own folder (D-150); a data folder named by `--data` or `GPCONF_DATA`, or a clone, holds one set of files and is not searched. What: when `gpconf fetch` fills a version's folder, `gpconf/fetch.py` looks in the other version folders beside it, newest first, for each stable-tier entry the folder lacks, and reuses a copy only when its bytes hash to the SHA-256 this version's manifest records and its metadata is present; the file and its `.meta.json` are copied, the metadata keeping the original `retrieved_at`, so the two-hour rule and the provenance still describe the one request that was made, and gaining `reused_from` (the version and the source path) and `reused_at`. A stable file whose bytes differ, or one without metadata, is requested; a live file is requested even when its bytes match, so that an upgrade brings current data. How it reads: a dry run prints `REUSE <url> (stable tier: the copy in corpus <version>'s cache holds the bytes this version's manifest records; copied, not requested)` and sums up "a run would make N request(s), reuse M file(s) from an earlier corpus version's cache"; a run prints `reused from <version>` against each file and "done: N request(s) made, M file(s) reused from an earlier corpus version's cache"; the runner reads `reused_from` from each provider file's metadata, lists the reused files per case in the JSON report under `reused`, and prints below the count line "N provider file(s) were reused from corpus <version>'s cache rather than fetched: stable tier, bytes matching this version's recorded SHA-256". Measured on the current fetch list, as D-153 expected: of the 61 normal entries, 22 are stable and 39 live. Shown from a copy installed outside the repository with a scratch home, whose per-user cache held an earlier version's folder, `0.2.0`, filled with this repository's 61 fetched files: the dry run planned 22 reuses and 39 requests and wrote nothing; a real run with a stand-in in place of the request function, which copied the file this repository already holds so that no request was made, reused the 22 stable files, called the stand-in 39 times, and its drift check found all 61 files matching the tested snapshot; the run report then said that 22 provider files were reused from corpus 0.2.0's cache. Tests: `tests/test_fetch_reuse.py` (new, 7): the version folders found newest first and non-version folders ignored; a stable file with matching bytes and metadata reused, a live file with matching bytes requested, a stable file with other bytes requested, one without metadata requested; the dry run's lines and summary with no request and no folder created; a run copying and marking the reused file, keeping `retrieved_at`, and caching it on the next run; reuse not attempted outside a per-user cache; and, where this repository's provider files exist, 22 reused and 39 requested over the real fetch list and the run report naming the reused files.

**D-158 (2026-09-26) — v0.3.0 packaging, stage 7: the GitHub Action, `action.yml` with `tools/action_report.py`, built as D-153 set it; it runs the corpus from its own checkout at the tag a workflow names, not from PyPI; tested locally and then live on the owner's scratch repository, seven jobs on macOS, Ubuntu and Windows, all green, with the three platforms' summaries character-identical; the live test found one fault, fixed here: the runner's advice to fetch into the Action's data folder, which is thrown away with the job.** The owner's question first: the plan had the Action install gpconf from PyPI, which does not exist yet; can it be built and tested now, or should it wait for the package? It can be built now, and without PyPI at all. A composite action runs from GitHub's download of the whole repository at the tag the workflow names, found in `github.action_path`, and that checkout already holds the runner and the corpus at exactly that tag. So the Action runs `gpconf` from it: nothing is installed, the tag pins code and corpus together, no step needs a package index, and there is nothing to swap in at release. The plan's `pip install gpconf==<version>` is replaced (plan note, section 6). Rejected: waiting for PyPI (it tests nothing more, and adds a dependency and a version to keep in step with the tag); installing the built wheel inside the Action (it would need the wheel published somewhere the Action can reach). `action.yml` is a composite action with the inputs `preset` (required), `python` (default `python3`, the interpreter the maintainer's library is installed in), `module` (Node presets) and `working-directory` (where a Node preset resolves its library), and the outputs `report`, `failed` and `exercised`. Its one step runs `tools/action_report.py` with the inputs in the environment, never in the shell text, so an input cannot inject a command. The script runs `gpconf run --preset <name>` against the Action's checkout with an empty data folder and `GPCONF_DATA` removed, so it is offline whatever the job sets. Failed cases exit 0: a result never becomes a verdict in someone else's CI. A preset that cannot run exits 2, and the summary says "Setup error: nothing was run.", gives the runner's message, which ends "Nothing was run; this says nothing about <library>.", and adds that this is a problem with the job's setup, not a result about the library. Any other runner failure is passed on and summarised as not a result. The job summary carries the parser line with the library version found and tested, how many of the seventeen cases were exercised offline and how many need provider data, which the Action does not fetch, the case table, the gate line, and the site's sentence "A count is a result against that version on that date, not a verdict on the project.", with the site's address. No badge. Local test, the composite step's own command run by bash with GitHub's environment and a fresh public export as the action's folder: python-sgp4 2.27 exit 0 with 2 failed and 3 exercised; naive exit 0 with 4 failed; pyephem without PyEphem exit 2 with the setup message; tle.js from a Node project exit 0 with 2 failed; reference with `GPCONF_DATA` pointing at this repository's fetched files still 4 exercised and 13 needing provider data. Live test, 2026-09-26, as the owner instructed, on a scratch repository of the owner's that is none of the three in use: `hneogy/gpconf-action-test`, private, created empty in the browser pane on the owner's word after they had seen the form. It holds a public export of this working tree without the corpus's own CI workflow, and the workflow in `docs/handoff/action-scratch-test/`, whose jobs call the Action through the scratch repository's tag `gpconf-action-test-1`, as a maintainer would, so the tag pin is tested too. The tag was pushed before `main` and the workflow runs on `main` only, so there was one run (36249122884). A seventh job, `reference-ubuntu`, was added at the owner's word before the first push, so that one preset runs on all three platforms and macOS can be compared with Ubuntu case for case. All seven jobs passed; each Action step's exit code, read from its raw log: python-sgp4 (2.27, installed by the job) 0, with 2 failed and 3 exercised; naive 0, with 4 failed; pyephem without PyEphem 2, the summary opening "Setup error: nothing was run." and the job green only because it expects that failure; tle.js 5.0.3 in a Node 22 project 0, with 2 failed; reference on macos-26-arm64, ubuntu-24.04 and windows-2025-vs2026 0 each, with 4 passing and 13 needing provider data. The three reference summaries are character-identical, compared by a script over the run page rather than by eye: the seventeen case rows, the count line and the gate line. Windows, not run before: the composite step ran under Git Bash (`C:\Program Files\Git\bin\bash.EXE`) with the image's `python`, and the Action's folder as a Windows path (`D:\a\_actions\...`) worked in the shell text; the step took 2.9 s, against 0.7 s on macOS and 0.6 s on Ubuntu. The fault the live test found: the runner's plain output ended with its usual advice, "Provider data is not shipped with the corpus; fetch it with" a command naming `tools/fetch.py` in the Action's folder and `--data` with the Action's empty data folder, and every missing item in the JSON report, which is the Action's `report` output, carried the same command. In the Action that advice is wrong: the Action never fetches, and the folder is thrown away with the job; on Windows the command also named `python3` and quoted a Windows path the Unix way. It never reached the job summary. The fix: `gpconf run --no-fetch-hint` (new, in `--help`), `Runner(fetch_hints=False)` underneath, leaves the fetch command out of the closing line, out of each missing item, which now reads "not on disk: provider data is not shipped with the corpus", and out of the drift message, and changes nothing else; `tools/action_report.py` passes it. Rejected: an environment variable, which the command line in the log would not show; and leaving the log as it was because the summary was right, since the JSON report is an output a maintainer's later steps may read. Also seen, not acted on: the tle.js step took 10 s against under 1 s for the Python presets [inferred: Node starting once per call; not examined]; and every Ubuntu job carried GitHub's notice that `ubuntu-latest` moves to Ubuntu 26 from 2026-10-19, the open item already listed. The scratch repository and its run are kept (owner: delete nothing). `PUBLIC_ALLOWLIST.txt` gains `action.yml`; `tests/test_export.py` checks that it and its script are exported. README: a CI paragraph after the presets. Tests: `tests/test_action.py` (new, 12): the composite definition, inputs only through the environment, no fetch and no install, the outputs and no badge, and the script's behaviour for a passing preset, failed cases, an unknown preset and a missing library, a Node preset without its library, and offline whatever `GPCONF_DATA` says; and, for the fix, no fetch command in the Action's log, summary or JSON report with every status unchanged, the command still named by a run without the flag with the same case table, and the drift message a sentence either way. 237 tests pass in this repository: seven skipped under the system python3 (three need `GPCONF_TEST_NODE_PROJECT`, four need python-sgp4 or PyEphem), one under the repository's `.venv` with the scratch Node project set (PyEphem, not installed there); a fresh public export (201 files) runs 235 under the system python3 with 21 skipped, every Action test among those run.

**D-159 (2026-09-26) — v0.3.0 packaging, stage 8: the README, the changelog and the version constants, and the README's relative links for the PyPI page, which are made absolute in the copy the package carries and left relative in the repository; no version bump, which belongs to the release; nothing uploaded, PyPI untouched.** Links first, counted before anything changed: the README holds 13 occurrences of `](`: eleven absolute links (six to GitHub issues and pull requests, four DOIs, and the Zenodo badge's link), the badge's image, also absolute, and one relative link, `` [`AUDIT.md`](AUDIT.md) ``; no reference-style link, no HTML, no in-page anchor. The packaging plan had expected many relative links, from reading rather than counting; the count found one. About thirty repository paths are written as code (`docs/FAILURES.md`, `DECISIONS.md` and the like); they are links neither on GitHub nor on PyPI and were not changed. The fix keeps both places working. `tools/stage_package.py` stages README.md as the PyPI description, with every relative link, image, reference definition and HTML `href` or `src` made absolute and pinned to the tag of the version being built: `https://github.com/hneogy/gp-omm-conformance/blob/v<version>/<path>`, `tree/` for a folder, `raw.githubusercontent.com` for an image, an in-page anchor pointed at the README on GitHub [inferred: PyPI is not relied on to keep heading ids]. So the PyPI page links to the files of the version a user installed, while the repository's README keeps its relative link, which resolves on GitHub, in a clone, in a fork and at any tag. Code spans and fenced blocks are left alone. A relative link to a path the export does not hold, or outside the repository, is refused, since the page would carry a dead link. The audit now checks README.md in the sdist against the staged copy, and fails a build whose long description (PKG-INFO, or the wheel's METADATA, what PyPI shows) holds a relative link. Rejected: rewriting the repository's links to absolute URLs, which would send every reader of a clone, a fork or an older tag to main on GitHub; a separate long-description file, a second README to keep in step; pinning to main rather than the tag, which would drift from the version installed. Checked by a local build of this working tree's export, with build 1.6.1 and setuptools 84.0.0 in the scratch environment of D-156 and nothing uploaded: wheel 85 files, sdist 90, the same as D-156, the audit finding no problem in either; the wheel's METADATA and the sdist's PKG-INFO carry the staged README exactly as their description, as `text/markdown`, with the one link at `.../blob/v0.2.1/AUDIT.md`, which a build at the release gives as `v0.3.0`, and no relative link; the wheel's METADATA carries the four `Project-URL` lines below. Version constants, counted over every tracked file with `git grep -F 0.2.1` (23 lines in 12 files outside the private handoff folder) rather than taken from the plan's hand count of five places: set by hand in `gpconf/__init__.py`, `pyproject.toml`, `tools/make_manifest.py`'s `CORPUS_VERSION` and `CITATION.cff` (with its release date); stated in the README's status paragraph, citation sentence and version-DOI sentence; generated in `manifest.json`, `MANIFEST.md` and `docs/FAILURES.md`; the rest read at run time, history (the changelog's headings, six decision entries, the harness note on the runner of 2026-09-24, the earlier DOIs) or test data. Nothing is bumped: the release does that. `tests/test_versions.py` (new, 3) fails unless the four hand-set constants, the two generated copies and the README's two version statements carry one version, the citation date agrees in both places, and the versioning rule reads the same in the README and the changelog; a bump that misses one cannot pass. Decision 1 of 2026-09-25 is applied: the rule's Minor line, in the README and in the changelog's preamble, reads "refreshed live snapshots, added cases, or an additive protocol change". README: the quick start gains the pip path beside the clone (`pip install gpconf`; before any fetch, the four cases whose files ship in the package run and thirteen say they need provider data, as the installed wheel of D-156 showed; `gpconf fetch`; then all seventeen), a line saying the command is `gpconf` installed and `python3 -m gpconf` in a clone, the `sgp4` and `pyephem` extras beside the plain installs, a paragraph on what the pip package carries and what stays in the repository, `gpconf fetch` beside `tools/fetch.py` in the contents table, and, under Versioning, that the package carries the version of the corpus it ships. The changelog's Unreleased section is rewritten for someone installing gpconf for the first time rather than someone following this log: installing and running, testing a library without writing an adapter, CI, and the report, in terms of commands and output, each entry keeping its decision numbers; the earlier items D-125, D-129 and D-142 to D-148 stay, rewritten the same way; details that matter only inside the build (the command parser's argument lists, the staging script's internals, the re-capture wording fix) are left to this log. At the owner's word, `pyproject.toml` gains a `[project.urls]` table, the only navigation a PyPI visitor gets: Homepage, the site (https://gpconf.neogy.dev); Repository; Issues, the repository's tracker, which is enabled; and DOI, the Zenodo concept DOI, which resolves to the latest release (each answered HTTP 200 on 2026-09-26). The code-written paths stay code, by the owner's decision: they read as paths, not links, and turning some thirty of them into links would clutter the README on GitHub to fix a problem nobody has on PyPI. The release order this stage creates is written as ordered steps in the private release checklist (`docs/handoff/release-checklist-v0.3.0.md`, new; the plan note had referred to a checklist that did not exist): the tag `v0.3.0` is pushed before the package is built and uploaded, and the links are checked to resolve between the two, since the description points at `blob/v0.3.0/` and a PyPI description cannot be changed without a new release. That inverts the usual order of upload, confirm, then tag, so it is a numbered step, not a note. Tests: `tests/test_stage_package.py` gains six (14 in all): every relative form rewritten at the tag, absolute links and code left alone, a dead or outside link refused, the staged README against the repository's, the audit reading the long description, and the four sidebar links. 246 tests pass in this repository with seven skipped under the system python3; a fresh public export (202 files) runs 244 with 21 skipped.
