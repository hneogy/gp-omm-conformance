"""The adapters behind `gpconf run --preset` (D-151): reference, naive, sgp4 (python-sgp4) and pyephem (PyEphem).
gpconf/presets.py names them, the library each exposes and the version each was tested against."""
